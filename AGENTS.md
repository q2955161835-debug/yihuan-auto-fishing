# 异环自动钓鱼项目规则

## 项目状态

- 当前阶段：加权中心控制发布物已通过用户真实游戏验收；D 盘数据迁移与 100 MiB 总配额已发布。极端黄标误跟踪修复完成 358 项离线测试、单文件构建、管理员清单校验和直接烟雾，根目录 `异环自动钓鱼.exe` 已替换并合并本地 `main`；新版本极端场景仍需真实游戏复验，本地提交尚未推送 GitHub。
- 项目目标：提供 Windows 单文件可执行程序，通过画面识别和系统输入完成《异环》钓鱼循环。
- 使用前提：自动化期间游戏保持前台，用户不操作鼠标键盘；F8 为全局紧急暂停键。
- 范围边界：不读取游戏内存、不注入游戏进程、不规避反作弊，只使用屏幕截图和 Windows 标准输入接口。
- GitHub：私有仓库 `https://github.com/q2955161835-debug/yihuan-auto-fish`；默认分支为 `main`，当前本地 `main` 跟踪 `origin/main`。

## 技术栈与关键依赖

- Python 3.13.x：桌面程序与自动化逻辑；本次离线构建环境为 Python 3.13.5。
- Tkinter/ttk：小型置顶控制窗口。
- DXcam：Windows Desktop Duplication 高频截屏，目标 30 帧/秒。
- NumPy 与 OpenCV：关键区域颜色分割、连通区域和画面特征识别。
- Win32 ctypes：窗口绑定、客户区坐标、前台检查、DPI 适配、全局 F8、SendInput 和捕获排除。
- PyInstaller：生成单文件 Windows 可执行程序。
- pytest：单元、状态机和合成帧回放测试。

运行依赖固定为 DXcam 0.3.0、NumPy 2.4.1、opencv-python-headless 4.13.0.92；开发依赖固定为 pytest 9.1.0 和 PyInstaller 6.19.0。变更截屏、输入或打包依赖时，必须复测窗口、无边框和全屏模式。

## 架构与数据流

已实现模块边界如下：

- `src/auto_fishing/ui/`：窗口、状态展示和用户操作，不包含识别与按键策略。
- `src/auto_fishing/platform/`：Windows 窗口、DPI、热键、鼠标输入接口和系统屏幕键盘生命周期/按键适配。
- `src/auto_fishing/capture/`：显示器选择、30 帧/秒截图和客户区裁剪。
- `src/auto_fishing/vision/`：上钩、进度条、黄色标记、绿色区域、结算和就绪画面识别。
- `src/auto_fishing/automation/`：状态机、超时、循环计数、暂停与恢复。
- `src/auto_fishing/storage/`：本地配置、异常诊断和完整运行记录的异步落盘；共享 `StorageQuotaManager` 负责整个数据根目录的增量容量账本和超限清理。
- `packaging/`：`requireAdministrator`、`PerMonitorV2` 清单和 PyInstaller 单文件规格；规格文件必须同时设置 `uac_admin=True`，否则 PyInstaller 会把自定义清单重写为 `asInvoker`。发布物启动时请求 UAC（用户账户控制）提升，不得启用控制台子系统。
- `scripts/build.ps1`：默认从项目 `.venv` 运行完整测试后构建单文件发布物并输出 SHA256；干净环境验收可用 `-PythonPath` 参数或 `AUTO_FISHING_PYTHON` 环境变量指定解释器。
- `scripts/verify_release.py`：读取最终 EXE 的 `RT_MANIFEST` 内嵌资源，构建后强制验证 `requireAdministrator` 与 `uiAccess=false`，不得只检查源 XML。
- `try/`：测试、合成帧、回放输入和临时产物；删除后不得影响正式程序。
- `try/smoke_exe.ps1`：以启动器 PID 为根递归跟踪所属进程树，检查窗口响应并只关闭本次烟雾拥有的进程；强制结束后按条件等待进程消失，不得按可执行文件路径批量判定或终止进程。管理员清单发布物必须在当前 PowerShell 会话用 `& .\try\smoke_exe.ps1` 直接执行，禁止另起低权限 Windows PowerShell 子进程造成错误 5。
- `流程截图/`：用户提供的带标注流程参考图，不作为可直接匹配的干净模板。
- `doc/验收标准.md`：真实使用流程、验证证据和结论。
- `doc/进展记录/`：按日期记录阶段性修改和异常。
- `docs/superpowers/specs/`：已确认的设计规格。
- `docs/superpowers/plans/`：按测试先行拆分的实施计划。

主数据流：点击“绑定并开始” → 在唯一一次 3 秒倒计时内手动切到游戏 → 绑定游戏窗口 → 启动或复用 Windows 屏幕键盘并固定到游戏显示器左下角 → 立即启动自动化 → 获取客户区与显示器 → 30 帧/秒截图 → 识别器返回结构化结果 → 状态机决定输入 → 鼠标点击/按住屏幕键盘 F/A/D → 顶部进度条干净消失后随机计时并点击游戏安全空白处 → 成功计数 → 轮间等待 1 秒 → 自动抛出下一轮。应用启动后同时建立运行记录目录：正常情况下每个已处理客户端帧写入结构化观测、前后状态和低分辨率截图，键鼠请求与系统输入返回值写入同一事件流；后台写入短暂积压时允许丢弃来不及保存的日志项，恢复后写入 `logging.backpressure` 和丢弃数量，不得因此暂停主循环。已绑定后的“开始”和暂停后的“继续”默认显式激活游戏窗口；关闭“自动切回游戏”后恢复 3 秒手动切回倒计时。任何自动化异常先抬起鼠标并释放 A/D，再暂停。

游戏不接受程序直接发送的键盘 `SendInput`，但真实验证接受实体鼠标点击 Windows 屏幕键盘生成的按键。生产输入后端必须保持屏幕键盘可见，通过客户区归一化坐标定位 F/A/D：F 轻点，A/D 以鼠标左键按住，换向先抬起再移动并按下。每次绑定必须按固定外框比例 `1365:415` 重建尺寸，目标宽度为显示器宽度、1365 像素和游戏客户区宽度三分之二共同约束的安全值，不得继承屏幕键盘当前尺寸；绑定后及每次点击前必须重新读取客户区，宽度低于 900、高度低于 250 或宽高比不在 `3.2～3.9` 时拒绝点击并以 `E_OSK` 暂停，防止 Windows 响应式布局导致归一化坐标落到错误按键。所有原生 `user32` 函数必须声明准确的 `argtypes/restype`，尤其 `SetWindowPos` 的两个 `HWND` 参数必须为指针宽度，避免错误 1400。`osk.exe` 必须通过 `ShellExecuteW` 的普通 `open` 动作启动；直接子进程创建会触发 Windows 错误 740。发布物经用户授权请求管理员权限，以跨越屏幕键盘 `UIAccess`（用户界面访问）完整性边界；同时保留错误 5 降级：读取并验证现有位置，安全则继续，否则要求用户手动拖到左下角。已有屏幕键盘退出时保留；本程序启动的实例在释放输入后尽力关闭，若 Windows 拒绝关闭则安全保留，不得强制终止系统辅助功能进程。屏幕键盘消失、越界或遮挡顶部进度/右下角准备区域时以 `E_OSK` 安全暂停；不得退回直接扫描码作为静默降级。

真实《异环》上钩提示位于游戏客户区右下 `BITE_ROI=(0.89,0.79)-(0.99,0.99)` 的蓝白圆环，左上文字提示不是可用的上钩信号。第一次 F 前以该区域建立初始基线；前 45 帧（约 1.5 秒）忽略抛竿动画，同时只允许把蓝色基线降低到冷却期实际背景的低位，不得继续使用可能含右下准备鱼钩按钮的高蓝色单帧。冷却后要求蓝色比例相对动态低位基线增加大于 `0.03`，且白色比例增加大于 `0.03` 或 Canny 边缘比例增加大于 `0.02`，连续两帧才确认；白色和边缘基线保持抛竿前值以避免放宽形状门槛。顶部进度识别固定使用 `TOP_ROI` 局部纵向 40%～52% 的五条扫描线，并在排除两侧固定 HUD 图标后的整个顶部槽带可玩区分别检测黄色标记和绿色目标区；两者不得再以相邻、重叠或固定距离作为成立条件。黄标切断绿区时仍重建完整逻辑绿区，黄标在绿区外时则保留独立绿段；至少三条扫描线边界一致才输出观测。最小逻辑绿区宽度为 `max(4 像素, TOP_ROI 宽度 * 0.012)`，不得再乘以黄标宽度；有历史时以最近可靠的绿区与黄标位置分别对同色候选排序，但达到最小宽度的绿区必须优先于附近的微小噪声，历史观测本身不得直接驱动输入。第二次 F 成功并进入等待进度条后必须清空上一阶段的绿区、黄标和待确认跳变历史，因此新阶段首个可靠黄标可位于可玩区任意位置，不得受上一轮或画面中心约束。同一阶段内，相对最近可靠绿区中心跳变超过槽宽 20%，或黄标位置跳变超过槽宽 8% 的候选，均等待下一帧同位置确认；确认期间返回空观测并释放 A/D，真实极端位置连续出现后仍可恢复控制。控制目标是让黄色标记回到绿色区域中心：每帧使用 `(黄标位置 - 绿区中心) / 绿区宽度` 作为相对误差，只保留连续且有效的最近 15 帧；每个样本的权重为识别置信度乘以 `0.2^帧龄`，同等清晰度时最新一帧可压过此前 14 帧。加权误差小于 `-0.10` 时按 D，大于 `0.10` 时按 A，其余释放；不得引入速度预测、旧绿区绝对位置或第二套控制状态机。任何缺失帧、非递增时间戳或超过 0.20 秒的帧间隔都必须立即清空控制窗口并释放 A/D；每个有效决策将方向、样本数、加权误差和置信度写入 `progress.control` 运行事件。结构性歧义和完全空白均最多容忍 60 帧，任一类别恢复可靠观测后计数归零，类别切换从 1 重新计数。累计至少 15 个可靠控制帧后，若连续三帧同时满足无进度观测、零扫描线、零候选且拒绝原因为 `yellow_missing`，仍优先通过现有 `BAR_GONE` 进入等待结算；中央提示横幅只作为运行诊断字段，不得触发额外 F 或提前进入结算。

等待结算不得依赖鱼种、品质、卡片颜色或其他结算页视觉特征。累计至少 15 个可靠控制帧且顶部进度条连续三帧干净消失后，从消失确认时刻随机等待 3.10～3.60 秒，再选择未被屏幕键盘遮挡的游戏客户区安全空白点点击一次。该窗口是在 2.10～2.60 秒实机点击失败后按用户要求整体后移 1 秒；历史运行 `run-20260713T070804371619Z` 仅作为初始量测证据，不再视为当前可用时机。输入服务点击正常返回后立即记录 `signal=click_succeeded`，把当前轮计为成功并进入完成或轮间等待，不再识别结算页、卡片消失、右下初始鱼钩或其他结束画面，也不重试；点击抛错时不得计数，必须走既有 `E_INPUT` 安全暂停。所有候选点被屏幕键盘遮挡时以 `E_OSK` 暂停。随机计划、尝试坐标、计时触发和点击成功必须写入完整运行日志；等待结算期间暂停再继续时重新抽取完整随机延迟。

Tk 控制窗口的内部句柄必须先通过 `GetAncestor(..., GA_ROOT)` 解析为顶层 HWND；自窗口识别与 `WDA_EXCLUDEFROMCAPTURE` 必须使用同一个顶层句柄。捕获排除失败属于非致命警告，必须显示在控制窗口中。暂停且没有恢复令牌时，worker 必须在截帧、窗口前台/刷新和视觉识别前等待；调用 `latest()` 前保留 operation epoch，取得帧后保留 frame epoch，截图异常、陈旧帧释放失败和后续帧处理错误都只能在对应 epoch 仍有效时触发暂停。迟到错误不得清除新恢复令牌或退出可继续运行的 worker。只有用户直接点击已绑定的“开始”或“继续”且“自动切回游戏”开启时允许调用窗口激活；初次“绑定并开始”和每轮自动抛竿不得激活窗口。激活失败或激活后仍非前台时不得发送输入。F8 必须使待绑定并开始、待开始和待继续倒计时失效，且从热键线程向 Tk 交付取消提示时必须走主线程队列。窗口失效后使用“取消当前轮 → 重新绑定”路径，不得把引擎永久关闭。

## 核心功能

- “绑定并开始”在一次三秒倒计时后完成绑定并直接启动，用户只需切换游戏一次。
- 已绑定的开始和暂停后的继续默认自动激活游戏；设置可关闭并恢复三秒手动切回倒计时。
- 设置 1～999 次钓鱼循环。
- 通过左下角 Windows 屏幕键盘自动完成抛竿、等待上钩、收杠、A/D 闭环控制；关闭结算和下一轮继续使用游戏画面坐标点击。
- F 轻点在按下前随机等待 80～180 毫秒，并在 `release_all()`（释放所有输入）期间取消尚未发送的 F；A/D 长按、换向和释放不允许增加该延迟。该节奏仅为操作稳定性设计，不得声称或用于规避游戏保护。
- 支持不同分辨率、Windows 缩放、窗口、无边框和全屏截图。
- 控制窗口始终置顶；真正独占全屏下不保证可见，但自动化与 F8 暂停仍应工作。
- 控制窗口初始与最小尺寸为 400×240 像素；右侧“阶段/帧率”列必须保留左侧间距，错误文本按 305 像素换行，避免裁切。
- 识别失败、窗口异常、截图停滞、输入失败或 F8 时安全暂停。
- 每次应用启动保存完整运行记录；异常诊断最多 20 份并清理超过 7 天的文件。

## 本地数据与环境变量

- 生产数据根目录固定为 `D:\29551\异环自动钓鱼数据\`，不得回退到 `%LOCALAPPDATA%`，不得写入敏感信息；旧路径 `C:\Users\29551\AppData\Local\异环自动钓鱼\` 已在全量 SHA256 迁移通过后删除，回退只允许使用 `D:\0文件夹\备份\异环自动钓鱼-data-migration-20260714-152811\`。
- 整个数据根目录硬上限为 104,857,600 字节，包含配置、运行事件、低分辨率帧、诊断和未知普通文件。启动时扫描一次，正常写入登记字节增量；超限时按“最旧已结束运行的最旧 JPEG 帧 → 该运行事件文件与空目录 → 最旧诊断组 → 当前运行最旧 JPEG 帧 → 当前 `events.jsonl` 最旧完整行”的固定顺序按需清理，并在稳定写入返回前恢复到上限内。活动写入路径禁止对含大量截图的旧运行执行整目录递归删除；轻微超限只能释放刚好足够的旧文件。
- `config.json` 与当前运行最新一条完整事件必须保留；未知普通文件计入容量但不得自动删除。无法在不破坏这些边界的前提下恢复容量、JPEG 编码失败或真实写盘失败时抛出存储错误并通过 `E_LOGGING` 安全暂停；单纯队列积压属于可恢复背压，不得暂停自动化。
- 诊断截图目录含中文路径，`DiagnosticsStore`（诊断存储）写图必须使用 `cv2.imencode(...).tofile(...)`，不得使用不支持 Unicode 路径的 `cv2.imwrite`；异常暂停时原始帧诊断可用于校准，正常运行帧仍按 480 像素 JPEG 保存。等待/控制进度条期间只在内存保留最近 12 帧顶部窄槽；仅 `E_PROGRESS_LOST` 将其写成 3×4 JPEG 联系图并与同一诊断事件一起按 7 天/20 组规则清理。
- 完整运行记录固定写入 `D:\29551\异环自动钓鱼数据\runs\`：每次启动一个独立目录，含 `events.jsonl` 和 `frames/`；帧截图最长边 480 像素、JPEG 质量 50。原有最近 30 个运行目录清理继续保留，但 100 MiB 总配额优先确保整个根目录不失控。异步写入队列上限为 300 项；队列满时非阻塞丢弃当前日志项，恢复后写入 `logging.backpressure` 与累计 `dropped_items`。真实编码、写盘、容量恢复或关闭超时仍以 `PAUSED/E_LOGGING` 暂停并释放输入。
- 设置文件固定为 `D:\29551\异环自动钓鱼数据\config.json`；读取设置时拒绝溢出及 `NaN`、`Infinity` 等非有限数字。
- `.env` 是真实环境变量账本，已被 Git 忽略；当前项目不需要环境变量。
- `.env.example` 是可提交的变量名账本；新增、删除或改名变量时同步更新读取逻辑与文档。

## 运行、测试与构建

在项目根目录建立 Python 3.13 `.venv` 并安装固定依赖后，保持以下命令可用；若命令变化，必须同步更新本文件：

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt
.\.venv\Scripts\python.exe -m pip install -e .
.\.venv\Scripts\python.exe -m auto_fishing
.\.venv\Scripts\python.exe -m pytest try/tests -q
powershell -ExecutionPolicy Bypass -File scripts/build.ps1
& .\try\smoke_exe.ps1
```

干净虚拟环境验收可将解释器传给构建脚本，例如：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/build.ps1 -PythonPath try/output/clean-venv/Scripts/python.exe
```

构建发布物为 `dist/异环自动钓鱼.exe`；用户验收通过后可复制为项目根目录 `异环自动钓鱼.exe` 方便直接运行，两处发布物均保持 Git 忽略。构建脚本必须可重复执行；发布前除自动烟雾外，必须在无开发环境依赖的 Windows 会话中完成启动人工复核。

## 验收标准

- 自动测试必须覆盖屏幕键盘生命周期/左下角定位/遮挡拒绝、坐标缩放、F 与 A/D 鼠标按住和释放、状态超时、F8、安全暂停、循环计数和诊断清理。
- 合成动态进度条以 30 帧/秒回放，黄标穿过并切断绿条时不得丢失观测；识别与控制处理不得持续落后于最新帧。
- 实机必须分别检查窗口、无边框和全屏；无法由自动测试判断的画面识别结果标记为人工确认。
- 当前源码回归基线为 358 个 pytest 测试；极端黄标修复候选已完成单文件构建、`requireAdministrator uiAccess=false` 清单校验和启动/响应/关闭烟雾。上一发布物另有 412 个真实控制帧回放和用户真实游戏验收；新候选仍需真实游戏复验极端黄标场景。
- 完成一个阶段后同步更新 `doc/验收标准.md` 中的结果、问题和最终结论。

## 执行与报告要求

- 修改识别阈值时记录使用的截图或合成样本、命令和结果，不得只写“已优化”。
- 识别或控制失败时记录问题、原因、解决方案，并优先增加可重复测试。
- 完成报告需列出新增或修改文件、测试命令、实机待确认项，以及是否存在重复实现、临时补丁或新增技术债。
