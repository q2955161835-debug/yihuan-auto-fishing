# 《异环》自动钓鱼验收标准

## 文档状态

- 当前阶段：V1 核心闭环已经用户真实游戏验收并以 `v1.0.0` 留存；V2 GitHub 最新发布为 `v2.0.4`。该版本包含绿色连续段、Windows 系统弹窗/控制窗口抢占前台时的输入安全和正常退出清理修复；用户已完成真实游戏验证并确认其为当前最稳定版本。
- 自动测试结果：`2026-07-15` 当前 V2 全套 428 项通过；新增回归覆盖 F 随机延迟期间抢焦点、失焦后的输入释放、前台窗口分类、失效屏幕键盘句柄、退出顺序和 `E_CLEANUP`。最新包 180 张无损进度槽回放中，控制期 155 张全部保持真实绿区观测，进度结束后的 21 张全部拒绝背景；最近真实运行的前 412 个控制帧离线回放仍输出左/右/释放三种动作，最大样本窗口 15。
- 构建与烟雾结果：使用 Windows 11、Python 3.13.5、PyInstaller 6.19.0 完成 `v2.0.4` 单文件构建；最终清单为 `requireAdministrator uiAccess=false dpi=PerMonitorV2 fallback=true/pm`，大小 63,989,100 字节，SHA256 为 `402DC0003D0E5FC8E1C537D6B2D4096BF3D24E15E18CC8A7BAA0E135B4C4790E`。用户已在真实游戏验证发布物；发布操作所在终端未提升，因此未重复运行开发烟雾脚本。
- 实机结果：用户使用加权中心控制发布物完成真实游戏验收并评价“非常完美”；左端低饱和度黄标修复版再次由用户确认“成功”，旧位置滞后和持续向左问题未再出现。
- 发布结论：V1 已发布到 GitHub `v1.0.0`；V2 首次正式资产在 `v2.0.0` 保留，GitHub 最新 Release 为已经用户真实游戏验证的 `v2.0.4`。根目录、`dist` 与 Release 资产必须保持同一 SHA256。

## 1. 启动与置顶窗口

### 验收步骤

1. 双击 `异环自动钓鱼.exe`。
2. 检查窗口尺寸、拖动、最小化、置顶和关闭。
3. 检查目标次数仅允许 1～999。
4. 尝试在运行中修改次数。

### 预期与证据

- 窗口正常打开且无控制台黑窗。
- 窗口模式和无边框游戏下始终置顶；独占全屏按设计说明处理。
- 运行中目标次数被锁定。
- 证据：启动冒烟测试记录和人工截图。

### 结果记录

- 状态：发布物启动、响应、关闭和 Windows GUI 子系统离线检查通过；400×240 初始/最小宽度及右侧阶段/帧率列间距自动测试通过；窗口拖动、最小化、置顶、次数边界及真实屏幕上的文字无裁切为人工确认（未执行）。
- 反馈/问题：`try/smoke_exe.ps1` 输出 `SMOKE_OK` 且本次拥有的进程树残留数为 0；并发隔离输出 `CONCURRENT_OWNERSHIP_OK`，其他同路径实例未被检查、关闭或终止。PE 子系统值为 2，未显示控制台子系统。独占全屏可见性仍按设计限制人工确认。

## 2. 游戏窗口绑定

### 验收步骤

1. 设置次数后点击“绑定并开始”，在三秒内切换到《异环》；确认倒计时结束后直接启动，不再点击第二次“开始”。
2. 分别使用窗口、无边框和全屏。
3. 移动或调整游戏窗口尺寸后重新开始。
4. 尝试绑定无效窗口、最小化窗口和过小窗口。

### 预期与证据

- 显示正确窗口标题、客户区尺寸和绑定状态，并在同一次倒计时后直接启动。
- 分辨率或位置变化后重新计算客户区，不使用固定 2560×1440 坐标。
- 运行时至少每 0.5 秒复核客户区位置和尺寸。
- 无效状态安全暂停并显示具体原因。
- 证据：窗口坐标自动测试、状态日志和实机人工确认。

### 结果记录

- 状态：窗口坐标、DPI、顶层 HWND 解析、显示器选择、一次倒计时绑定并启动及 F8 取消路径自动测试通过；窗口、无边框和全屏绑定为人工确认（未执行）。
- 反馈/问题：应用已统一使用 `GetAncestor(..., GA_ROOT)` 得到的顶层 HWND 识别自身及排除捕获；排除失败会显示非致命警告。结构扫描确认生产代码没有固定 2560×1440 坐标；该分辨率仅作为多分辨率测试参数。

## 3. 单轮完整流程

### 验收步骤

1. 在钓鱼就绪画面设置目标 1 次并开始。
2. 观察第一次 F、上钩识别、第二次 F、进度条控制、顶部进度条消失和随机延迟后的结算点击。
3. 检查完成计数。

### 预期与证据

- 顺序与设计状态机一致，不跳过状态。
- 顶部进度条至少稳定 15 个控制帧并连续三帧干净消失后，随机等待 3.10～3.60 秒点击一次安全空白点；鱼获页内容不参与判断。
- 结算点击的输入调用正常返回后，完成数量立即从 0 变为 1，不再等待或识别后续画面；输入调用抛错时保持 0 并安全暂停。
- 证据：状态迁移测试、简短运行日志和实机人工确认。

### 结果记录

- 状态：稳定消失门槛、随机结算点击和多轮状态迁移自动测试通过；用户已确认真实多轮执行完整闭环，当前仅需复验上钩识别稳定性。
- 反馈/问题：初次使用只进行“绑定并开始”的一次 3、2、1 倒计时；已绑定后的“开始”和暂停后的“继续”默认自动激活游戏，关闭“自动切回游戏”后恢复手动倒计时。激活失败或激活后仍非前台时不得启动/恢复或发送输入；自动轮间抛竿不得激活窗口。2.10～2.60 秒版本实机未能关闭最后页面，当前仍使用 3.10～3.60 秒；点击输入正常返回即计数，不再验证结算画面。

## 4. 每秒 30 帧闭环控制

### 验收步骤

1. 运行合成帧回放，绿色区域和黄色标记以不同速度和方向移动。
2. 验证黄色标记偏左、偏右和位于中心死区时的输入；连续输入最多 15 帧，分别改变样本的新鲜度和识别置信度。
3. 分别插入仍有扫描线/候选的结构性歧义 59～60 帧，以及零扫描线/零候选的完全空白 59～60 帧；另验证三帧干净 `yellow_missing` 仍进入结算。
4. 在真实游戏快速变化场景中观察实际帧率和控制结果。
5. 跨两轮运行并让新一轮黄标从可玩区最左或最右开始；同一轮制造或回放绿区中心超过槽宽 20%、黄标超过槽宽 8% 的单帧跳变；另用低饱和度黄标和固定槽左端 14%～16% 边界回放。

### 预期与证据

- 按最多 15 帧的加权相对误差决策：黄标相对绿区中心偏左按 D、偏右按 A、进入 ±10% 中心死区释放；同等清晰度的最新帧权重大于此前 14 帧总和，较清晰的相邻旧帧可抑制单个低置信度跳变。
- 换向时先释放旧键，任意时刻不同时按 A/D。
- 一帧缺失、时间戳倒退或帧间隔超过 0.20 秒时立即清空控制样本并释放 A/D；结构性歧义和非干净完全空白均在第 60 帧暂停，恢复可靠观测或切换类别后重新计数；稳定控制后的三帧干净消失仍优先进入结算。
- 控制线程消费最新帧，不积压回放旧帧。
- 每个有效控制帧的运行日志含方向、样本数、加权误差和识别置信度。
- 第二次 F 后清空上一轮视觉历史；新一轮首个可靠黄标无论位于左端或右端都立即接受。进入控制后，绿区中心或黄标即使单帧大幅跳变，只要当前帧仍达到至少三条扫描线共识，就必须当帧参与中心追踪，不得等待下一帧确认。槽带局部 14%位置的低饱和度黄标必须保持可见并输出向右 D。
- 证据：`py -3.13 -m pytest try/tests -q`、真实运行观测回放统计和实机人工确认。

### 结果记录

- 状态：30 帧最新帧、最多 15 帧控制窗口、新鲜度/清晰度权重、缺帧清空、黄标切断绿条重建、黄标与绿区完全分离、五行多数确认、换向释放、中心死区、1.2% 最小窄绿区、阶段重置、左右极端首帧、低饱和度左端黄标及绿区/黄标大跳变首帧立即输出的自动测试通过。全套 410 项通过；真实错误截图回放得到绿区 `0.572～0.715`、黄标 `0.147` 和向右 D；用户已确认新发布物真实游戏运行成功。
- 反馈/问题：上一发布物在高品质鱼尾段连续按 A 时，绿区中心已从约 59.6% 移至 43.1%，黄标却从约 61.1% 移至 75.8%，最终出现“鱼儿溜走了”；真实日志还存在约 9%～10% 的单帧黄标跳变。原因是原控制器仅按当前点位驱动，对低清晰度跳变没有缓冲，也没有把最新画面与短历史按质量排序。现只在原控制器中保存相对误差、置信度和时间戳，以 `置信度 × 0.2^帧龄` 加权；不引入速度预测、绝对旧绿区位置、机器学习模型或第二状态机。实机需记录 `progress.control` 事件、实际帧率、成功数及每次暂停原因。
- 反馈/问题：最新运行 `run-20260714T085628804414Z` 中第二阶段首个黄标 65.5% 紧贴上一阶段末尾 65.2%，随后出现 12.4%、11.0% 和 34.6% 的黄标单帧跳变，而绿区中心基本稳定；830 个非异常相邻样本最大跳变约 7.81%。因此本次以 8% 为仅针对黄标的候选确认门，并在第二次 F 后显式重置识别历史；没有恢复黄标与绿区相对位置约束，也没有修改控制器或 A/D 映射。

## 5. 暂停、F8 与异常保护

### 验收步骤

1. 分别在等待上钩、闭环控制和结算阶段点击暂停或按 F8。
2. 将游戏切出前台、最小化或关闭；分别让控制窗口和 Windows 系统弹窗取得前台。
3. 在 F 的 80～180 毫秒随机延迟期间模拟弹窗抢焦点，并在 A/D 按住期间再次抢焦点。
4. 模拟截图停滞、识别超时和输入失败。
5. 关闭弹窗后点击“继续”，检查恢复行为。

### 预期与证据

- 所有路径立即释放 F/A/D/鼠标左键且停止新输入；每次新 F/A/D 按下或结算点击前都重新确认游戏仍为前台，F 延迟期间失焦不得发送按下事件。
- 控制窗口取得前台时显示 `E_USER_PAUSE` 且不生成自动诊断；Windows 系统弹窗或其他窗口抢占时显示明确的 `E_WINDOW` 提示并记录一次诊断。
- 弹窗关闭后不得自动恢复；用户点击“继续”后重新激活并复核游戏，保留完成数和目标数、放弃当前未完成轮并从第一 F 重开。
- 证据：按键记录测试、状态机测试、`py -3.13 -m pytest try/tests -q` 和实机人工确认。

### 结果记录

- 状态：F8、失去前台、F 延迟期间弹窗抢焦点、失焦后释放输入、控制窗口/系统弹窗分类、截图停滞、超时、输入失败、跨帧恢复令牌、迟到窗口错误、取消当前轮后重绑和退出释放的自动测试通过；全套为 428 项。真实游戏中的立即释放、系统弹窗提示和继续重开为人工确认（未执行）。
- 反馈/问题：暂停且无恢复令牌时 worker 在截帧/窗口/视觉边界前等待；阻塞中的旧错误不得清除新恢复令牌。默认“继续”会显式激活已绑定游戏并复核前台，关闭自动切回后才显示 3、2、1 手动倒计时。F8 会取消待绑定并开始、待开始和待继续倒计时，过期回调不得绑定、启动、发送输入或恢复 worker。窗口失效后可停止工作线程与截屏、释放输入、重新绑定并再次开始。前台检查与 Windows 实际输入之间无法成为同一个原子系统调用，仍需用真实弹窗复验最后的系统时序边界。

## 6. 多轮与计数

### 验收步骤

1. 分别设置 2、5 和边界值次数。
2. 在中途触发一次暂停后继续。
3. 检查最终完成状态及是否多执行一轮。

### 预期与证据

- 成功轮数等于目标数，不少执行也不多执行。
- 未完成轮次不计数，恢复后不会重复计入已成功轮次。
- 证据：状态机自动测试与实机人工确认。

### 结果记录

- 状态：目标边界、暂停恢复、完成计数、不多执行及结算点击后 1 秒自动抛出下一轮的自动测试通过；用户已确认真实多轮成功执行完整闭环。
- 反馈/问题：轮间通用超时修复已由真实多轮验证；当前反馈是部分轮次停在“等待上钩”，没有稳定触发第二次 F，已转入上钩识别专项复验。

## 7. 诊断文件自动清理

### 验收步骤

1. 在隔离的测试目录生成不同日期和数量的诊断文件。
2. 运行清理逻辑。
3. 检查 7 天边界、20 组上限和目录边界。
4. 确认 `流程截图` 未发生变化。

### 预期与证据

- 删除超过 7 天的诊断，只保留最近 20 组。
- 正常帧不额外落盘；仅 `E_PROGRESS_LOST` 保存最近 12 帧顶部窄槽联系图，且与全屏诊断一起按 7 天和 20 组清理，清理逻辑不越出程序诊断目录。
- 证据：清理测试、清理前后文件清单和 Git 状态。

### 结果记录

- 状态：隔离目录中的 7 天边界、20 组上限、进度联系图分组、并发保存和目录边界自动测试通过；真实 `%LOCALAPPDATA%` 诊断样本清理为人工确认（未执行）。
- 反馈/问题：结构检查确认 `流程截图` 未修改；自动测试不越出隔离目录。

## 8. 完整运行记录与逐帧截图

### 验收步骤

1. 在隔离测试目录启动运行记录器，写入应用事件和一帧 1920×1080 客户区画面。
2. 关闭记录器，检查运行目录中的 `events.jsonl` 与 `frames/00000001.jpg`。
3. 生成 31 个模拟运行目录后执行清理，并验证目录边界。
4. 用受阻塞的写入线程模拟队列积压，验证队列满时丢弃当前日志项但不形成运行错误；解除阻塞后检查 `logging.backpressure` 和 `dropped_items`。
5. 用模拟 Windows 输入接口发送键盘与鼠标请求，检查业务事件、发送计数和失败错误码。
6. 在真实游戏点击开始后，检查首帧目录中的 `frame.processed`、`input.request`、`sendinput.result` 和 `frames/00000001.jpg`；模拟队列背压时自动化必须继续，模拟真实写盘故障时界面必须显示 `PAUSED/E_LOGGING` 且 F/A/D/鼠标均已释放。

### 预期与证据

- 每次运行目录包含 UTF-8 逐行结构化事件与最长边 480 像素、JPEG 质量 50 的低分辨率截图。
- 清理只删除 `runs` 下最旧的运行目录，保留最近 30 个，不触及相邻目录。
- 队列满属于非致命背压，恢复后事件流记录丢弃数量且自动化不暂停；后台编码、写盘或容量恢复失败仍必须暂停并释放输入。
- 成功的 `SendInput`（系统输入接口）结果只包含请求数和发送数；部分发送必须额外包含 Windows 错误码。
- 运行记录初始化失败时，控制窗口禁止开始；正常应用关闭在释放输入后关闭写入器。
- 证据：`py -3.13 -m pytest try/tests/test_storage.py -q`、`py -3.13 -m pytest try/tests/test_safe_input.py -q` 与 `py -3.13 -m pytest try/tests/test_engine.py try/tests/test_ui_smoke.py -q`。

### 结果记录

- 状态：屏幕键盘生命周期、输入、引擎/界面和遮挡识别自动测试已加入；全量 268 项通过。真实日志与截图保存通过，管理员版发布物已在《异环》中成功自动点击屏幕键盘 F 并出钩；真实 A/D 控制和完整单轮仍待确认。
- 反馈/问题：运行目录 `%LOCALAPPDATA%\异环自动钓鱼\runs\run-20260712T081336310655Z` 保存 845 帧、约 30 FPS 的真实钓鱼画面。第一次 F 的鼠标按下/抬起成功，游戏出钩；原先将左上文字误认为上钩提示，故全程未触发。逐帧复核确认真实信号为右下蓝白圆环：第 20～28 帧是抛竿动画，第 201～208 帧出现真实上钩提示。改为右下基线差分、45 帧冷却和连续两帧确认后，对同一运行回放首次于第 200 帧命中，冷却期前 45 帧无触发。程序自动第二次 F、真实 A/D 控制和完整单轮仍必须用新 EXE 人工确认；退出时若任何清理步骤失败，`events.jsonl` 现会记录 `application.cleanup_failed`。
- 反馈/问题：运行目录 `%LOCALAPPDATA%\异环自动钓鱼\runs\run-20260712T081336310655Z` 保存 845 帧、约 30 FPS 的真实钓鱼画面。第一次 F 的鼠标按下/抬起成功，游戏出钩；原先将左上文字误认为上钩提示，故全程未触发。逐帧复核确认真实信号为右下蓝白圆环：第 20～28 帧是抛竿动画，第 201～208 帧出现真实上钩提示。改为右下基线差分、45 帧冷却和连续两帧确认后，对同一运行回放首次于第 200 帧命中，冷却期前 45 帧无触发。F 轻点现会在按下前加入 80～180ms 受限延迟，`release_all()` 可取消延迟中的 F，A/D 仍无额外等待；自动证据通过，真实 A/D 控制和完整单轮仍必须用新 EXE 人工确认；退出时若任何清理步骤失败，`events.jsonl` 现会记录 `application.cleanup_failed`。
- 反馈/问题：运行目录 `%LOCALAPPDATA%\异环自动钓鱼\runs\run-20260712T091332174511Z` 已确认第二次 F、A/D 鼠标按住/换向、F 延迟和退出清理日志均正常；暂停原因仍是 `E_PROGRESS_LOST`。原始诊断帧证明真黄色标记高约 15 像素，旧阈值拒绝它而误配左侧圆形 UI；修复后对该帧正确得到绿色范围 `0.273–0.408`、黄色 `0.413`。该帧还显示“鱼上钩了，快点击按钮上鱼！”横幅，新增连续两帧横幅识别后会释放 A/D、轻点 F 并进入结算等待。全量自动证据通过，仍需新 EXE 实机确认收鱼、结算点击与完整单轮计数。
- 反馈/问题：运行目录 `%LOCALAPPDATA%\异环自动钓鱼\runs\run-20260712T092525110647Z` 证明此前控制器采用了过宽的绿色安全带，只追求“进入绿区”而未追踪中心，导致区域条移动时不能跟上。现改为黄色标记对绿色中心的闭环：偏差超过绿区宽度 10% 才动作，黄标左侧按 D、右侧按 A，中心小死区释放。280 项自动测试通过；仍需新 EXE 实机确认完整单轮。
- 反馈/问题：运行目录 `%LOCALAPPDATA%\异环自动钓鱼\runs\run-20260712T093308622889Z` 证明中心控制版本首次 F、第二次 F 均已送达，但进度条首次被识别后约 33 毫秒，错误的横幅处理又发送第三次 F 并跳到等待结算，因而 A/D 未被执行。已删除横幅触发的控制分支；新增回归测试要求“横幅与有效进度条同帧时仍保持控制状态并按黄标中心误差发送 A/D”。全量 280 项测试通过，待重建 EXE 后人工确认 A/D 连续跟随。
- 反馈/问题：运行目录 `%LOCALAPPDATA%\异环自动钓鱼\runs\run-20260712T094153974028Z` 中 A 已成功按下，但黄标移入绿区后，第 158～163 帧连续丢失进度条并触发 `E_PROGRESS_LOST`。原始失败帧仍有完整进度条；现有单连通块识别被黄标切成 147/39 像素绿色段，均未达到约 160 像素门槛。该问题必须通过绿色分段重建与真实序列回放解决，禁止继续仅放宽单块阈值；重构设计和实机复验尚未批准。
- 反馈/问题：已用上述原始失败帧生成 `try/fixtures/progress/progress_split_marker.png`，新识别器在固定槽带先确认黄标，再以五条扫描线合并其两侧绿色段；至少三条边界一致才输出。真实夹具和合成黄标扫动均通过，单帧大跳变等待确认且当前帧释放。每帧日志新增扫描线数、候选数和拒绝原因；若实机仍触发 `E_PROGRESS_LOST`，诊断目录会额外生成最近 12 帧顶部窄槽联系图。289 项自动测试通过，等待新 EXE 实机完整单轮。
- 反馈/问题：运行 `run-20260712T103051228738Z` 已实机确认进度控制成功；第 324 帧起进度槽整体消失并进入成功转场，旧核心却在六帧后暂停。现要求至少 15 个可靠控制帧，随后连续三帧无观测、零扫描线、零候选且 `yellow_missing` 才进入等待结算；两帧消失后恢复仍继续控制，未稳定或有候选的六帧丢失仍暂停。结束切换不发送 F。293 项自动测试通过，等待新 EXE 验证结算识别、关闭与计数。
- 反馈/问题：运行 `run-20260712T111131293562Z` 两次 F 成功，但高品质鱼绿区缩至顶部裁剪宽度约 9.8%，遗留的 12% 固定门槛使 231 帧都停在等待进度条。真实窄绿区夹具、跨三种分辨率的黄标边缘/中央合成测试和短噪声拒绝现均通过；全量 303 项通过。仍需新 EXE 实机确认高品质鱼进入控制、稳定结束、结算关闭和计数。
- 反馈/问题：运行 `run-20260713T055318102726Z` 已确认高品质鱼从等待进度条进入控制并持续完成 A/D 中心跟随。第 438 帧正常进入等待结算，但第 442 帧仍是紫色漩涡时就被现有中央蓝/白比例规则误判为结算；程序在 `(384, 792)` 仅点击一次，Windows 返回 `requested=2, sent=2`。真正鱼获卡片约第 500 帧后才出现，因没有重试而在第 678 帧关闭结算超时。屏幕键盘顶部为 `y=921`，首次点击未落入键盘；当前证据只能证明点击过早，不能证明游戏过滤正确时机的模拟鼠标。
- 反馈/问题：运行 `run-20260713T070804371619Z` 中真实鱼获卡片于第 420 帧确认，两次随机延迟点击 `(2048, 792)` 均返回 `requested=2, sent=2`；第 452 帧起卡片持续消失并恢复钓鱼画面，证明关闭成功。旧核心仍等待不适用于该真实背景的 `ready` 标志，在第 668 帧以 `E_TIMEOUT` 暂停且完成数保持 0。现改为点击后连续三帧卡片消失即确认关闭；两帧短暂消失后卡片重现会清零，不会误计数。
- 反馈/问题：运行 `run-20260713T072447533967Z` 第 281 帧进入等待结算，第 320 帧起出现青绿色圆盘的“黑魔鬼”鱼获卡片并持续到超时；顶部奖励栏符合条件，但中央蓝色比例仅约 10.6%，旧 40% 蓝色门槛使所有帧 `result_candidate=false`，因此没有点击。现中央圆盘接受蓝色或青绿色大于 40%，仍强制顶部奖励栏；同一运行的绿色转场顶部奖励栏不成立，保持拒绝。按用户建议，点击后不再以卡片消失计数，改为真实右下初始鱼钩连续三帧确认；真实上钩圆环为反例。
- 反馈/问题：连续三次实机日志显示屏幕键盘从可正确点击的外框 `1706×519`、客户区 `1688×472`，缩为 `1028×435`、客户区 `1010×388`，最终缩为 `650×203`、客户区 `632×156`；最后一次 F 目标 `(188, 1360)` 实际落到 H。根因是定位函数把“当前窗口尺寸”当作下一次缩放基准，累计继承 Windows 响应式布局。现改为每次绑定按固定 `1365:415` 外框比例重新计算；2560 像素显示器目标宽度稳定为 1707，1920 像素显示器为 1365。客户区小于 `900×250` 或宽高比不在 `3.2～3.9` 时，绑定和每次点击前均拒绝输入。29 项屏幕键盘专项测试和全量 326 项测试通过；真实键盘尺寸恢复及 F/A/D 命中仍为人工确认。
- 反馈/问题：最新运行 `run-20260713T082932994675Z` 从第 527 帧至第 620 帧持续处于“等待上钩”，画面字段 `ready=true` 但 `bite=false`，完成数保持 0，随后因游戏失去前台以 `E_WINDOW` 暂停。用户要求整体恢复到 `run-20260713T070804371619Z` 成功点击鱼获卡片时的 `27fa29a` 识别行为，仅保留当前稳定屏幕键盘布局；点击成功后的卡片消失和初始鱼钩识别全部删除，点击正常返回立即计数。此回退同时撤销青绿色与任意色相鱼获卡片支持，属于用户确认的历史版本取舍。

## 9. D 盘数据目录与总容量配额

### 验收步骤

1. 在隔离目录构造旧运行、诊断、配置、未知文件和活动运行，执行 `py -3.13 -m pytest try/tests/test_storage.py -q`。
2. 启动应用装配测试，确认配置、诊断和运行日志共享同一个配额实例，默认根目录精确等于 `D:\29551\异环自动钓鱼数据\`。
3. 迁移前为旧 C 盘数据与根目录 EXE 创建时间戳备份，对 C→备份和 C→D 分别比较所有文件的相对路径、长度和 SHA256。
4. 对 D 盘副本执行生产 `StorageQuotaManager.initialize()`，检查整个根目录不超过 104,857,600 字节、`config.json` 存在、最新完整事件仍可读取。
5. 运行新发布物后复核稳定写入返回时总容量仍在上限内；运行日志写入失败必须进入 `PAUSED/E_LOGGING` 并释放输入。
6. 构造旧运行 20 张各 10 字节截图、10 字节事件和仅超限 5 字节的新活动事件；配额执行后旧运行必须仍存在，只删除最旧 1 张截图并保留其余 19 张及事件文件。
7. 复制真实 5,860 文件、104,864,281 字节的数据布局到 `try/output`，初始化后连续登记 60 次活动帧写入；记录根目录扫描次数、耗时和最终容量。

### 预期与证据

- 删除顺序固定为最旧已结束运行的最旧帧、该运行事件与空目录、最旧诊断组、活动运行最旧帧、活动事件最旧完整行；诊断同组原图/元数据/联系图原子删除。轻微超限不得递归删除整份旧运行。
- `config.json`、最新完整事件和未知普通文件不会被自动删除；未知文件仍计入总容量，无法继续清理时明确报错。
- 初始化允许扫描数据树一次；此后活动写入即使连续触发超限清理，也只能使用容量账本，不得再次扫描整个根目录或阻塞日志线程。
- 证据：存储自动测试、应用装配测试、两份全量 SHA256 比较结果、迁移前后实测字节数和运行日志错误路径测试。

### 结果记录

- 状态：配额核心、三类写入者接入、固定 D 盘路径和共享实例自动测试通过；全套 353 项、`compileall` 与 `git diff --check` 通过。C→备份、C→D 和最终备份→D 的 23,433 文件清单均为零差异；D 盘最终保留 7,159 个文件、104,853,232 字节，`config.json` 为 97 字节，真实运行 `run-20260714T063349190418Z` 保留 19,122 条完整事件和最近 7,156 张帧。
- 反馈/问题：原计划 2,500 字节异步日志夹具无法同时容纳 8 条约 413～417 字节事件和一张约 946～947 字节 JPEG，导致所有帧被正确裁掉。将测试预算校正为 5,000 字节并增加 `raise_if_failed()` 后，能够验证最旧活动帧被裁、最新帧保留且写线程无静默失败；生产上限仍严格保持 104,857,600 字节。
- 反馈/问题：首次迁移裁剪在大量帧场景每删除一个文件都重新扫描根目录，回归夹具实测 33 次根扫描。改为初始化只扫描一次并按已删除字节精确扣减后，同一测试为 1 次扫描；447,296,534 字节迁移副本裁剪到 104,853,232 字节耗时 12.491 秒。
- 反馈/问题：最新真实运行 `run-20260714T081942331895Z` 在 `16:20:15.893` 至 `16:20:47.476` 间停写约 31.58 秒，随后以“日志队列已满”暂停；数据目录同期从接近 100 MiB 降至约 6.25 MB。根因是仅超出一张截图时仍递归删除含数千文件的整个旧运行。增量清理回归现确认仅超限 5 字节时只删 1 张 10 字节旧截图；队列背压测试确认丢弃 1 项、恢复后记录 `logging.backpressure` 且不形成 `RuntimeLogError`，真实磁盘失败的引擎测试仍以 `E_LOGGING` 暂停。
- 反馈/问题：用户完成 10 轮目标中的 8 轮实测后，生产目录稳定为 104,864,281 字节，超出硬上限 6,681 字节；运行 `run-20260714T095204572440Z` 在容量临界后约每 2 秒才落一批日志，并持续记录 `logging.backpressure`。根因是 `register_write()` 虽维护容量账本，超限时却未把账本传入 `_enforce()`，导致每个帧和事件都重新遍历约 5,800 个文件；退出时 10 秒无法排空队列，可能停在文件写入和配额登记之间。修复前回归测试两次活动写入共扫描 3 次，修复后为 1 次。真实布局副本启动仅删除 1 张最旧帧，60 次 15,000 字节活动写入耗时 0.8185 秒、扫描仍为 1 次，最终 104,847,826 字节。

## 10. 构建与发布物

### 验收步骤

1. 执行构建脚本。
2. 在无项目虚拟环境依赖的 Windows 会话启动可执行程序。
3. 完成窗口打开、绑定倒计时、F8 注册和退出冒烟测试。

### 预期与证据

- 生成 `dist/异环自动钓鱼.exe`。
- 启动无控制台黑窗，关闭后无残留进程和被按住的键。
- 证据：构建命令输出、文件哈希、启动冒烟记录。

### 结果记录

- 状态：增量日志清理发布物构建门 354 项通过，清单为 `requireAdministrator uiAccess=false`，当前 PowerShell 会话直接执行 `& .\try\smoke_exe.ps1` 输出 `SMOKE_OK`；大小 63,957,832 字节，SHA256 `DED6B53147058B37A604F6FD00B495E8914663AF99BC9DF42579298E9A1B2D56`。根目录替换后大小、哈希和清单再次核对一致；旧发布物备份位于 `D:\0文件夹\备份\异环自动钓鱼-incremental-log-pruning-20260714-163733\`。
- 状态：极端黄标修复发布物构建门 358 项通过，清单为 `requireAdministrator uiAccess=false`，直接烟雾输出 `SMOKE_OK`；根目录替换后大小为 63,955,648 字节、SHA256 `47BD572C85D2C344343BDC58B9B5FCF5003946FB135AC95CFB6488D05650E035`，清单复核一致。被替换版本备份位于 `D:\0文件夹\备份\异环自动钓鱼-extreme-marker-tracking-20260714-172822\`。
- 状态：容量账本复用候选构建门 359 项通过，清单为 `requireAdministrator uiAccess=false`，直接烟雾输出 `SMOKE_OK`；候选大小 63,956,988 字节，SHA256 `9A4768D6930F939C32EE8AB23DABA3BA4B98B03351ECCEE9A7ECD96225E52CA4`。烟雾启动同时把生产数据从超限 6,681 字节恢复到 104,849,772 字节，并新增运行 `run-20260714T100936282470Z`；根目录替换待执行。
- 状态：容量账本复用发布物已替换到根目录，最终大小 63,956,988 字节、SHA256 `9A4768D6930F939C32EE8AB23DABA3BA4B98B03351ECCEE9A7ECD96225E52CA4`，与候选一致；旧极端黄标版本备份位于 `D:\0文件夹\备份\异环自动钓鱼-quota-ledger-write-20260714-181205\`。
- 反馈/问题：曾误用低权限 Windows PowerShell 子进程运行管理员清单发布物，关闭时明确返回错误 5，造成两次烟雾误报残留；精确清理各自拥有的进程树后，改为当前 PowerShell 会话直接执行即通过。烟雾脚本同时新增强制结束后的最长 5 秒条件轮询，11 项打包测试和全量 353 项通过。
- 状态：屏幕键盘输入隔离分支的 258 项构建门测试、单文件构建和发布物启动/响应/关闭烟雾通过；烟雾启动等待已覆盖单文件解压子进程竞态。真实游戏自动输入和无开发环境依赖的独立 Windows 用户会话仍待人工确认。
- 反馈/问题：分支发布物 `D:\1Folder\异环自动钓鱼\.worktrees\codex-feat-runtime-logging\dist\异环自动钓鱼.exe` 大小 63,919,181 字节，生成时间 `2026-07-11 19:10:58 +08:00`，SHA256 `762CBC69DB7949BB94C33A94A3A0B05C8A4897AEB39103E46C1A5DDDF961EAFD`。烟雾输出 `SMOKE_OK`。构建前主工作区发布物已备份到 `D:\0文件夹\备份\异环自动钓鱼-runtime-logging-prebuild-20260711-1909\`，其 SHA256 为 `27024B3FCD775E8B6818893AC4D02FFDA960AAB7B23A87E73D8E60A46A97050D`。PyInstaller 会写入时间戳和归档元数据，同一源码与工具链的不同构建哈希可能不同，验收以每次最终产物的实测大小与 SHA256 为准。DXcam/COM 实际捕获仍为真实游戏人工确认。
- 反馈/问题：进度槽重建发布物 SHA256 为 `343E170C163C82AF999BF2BA4A66EFA335DDE7D88FCA4DDC1A84598D2AA970F2`；构建前版本已备份到 `D:\0文件夹\备份\异环自动钓鱼-progress-slot-prebuild-20260712-182816\异环自动钓鱼.exe`，备份 SHA256 为 `97B372A5682F4C8073BEA975652F9D0EF80D91F4C730D0852D1FB307418FD352`。等待真实游戏完整单轮。
- 反馈/问题：稳定结束切换发布物 SHA256 为 `F89B18DCAF284BB961068DD5281E05E631923F713F98B5D92FBE684BE033AD0A`；构建前版本已备份到 `D:\0文件夹\备份\异环自动钓鱼-progress-completion-prebuild-20260712-190242\异环自动钓鱼.exe`，备份 SHA256 为 `343E170C163C82AF999BF2BA4A66EFA335DDE7D88FCA4DDC1A84598D2AA970F2`。等待真实结算识别、关闭和计数。
- 反馈/问题：自适应窄绿区发布物大小 63,942,526 字节，SHA256 为 `BFE86E6485ABB3B547DEC24F2D52DD295D64303E332831D04B429DFA2A4526E7`；构建门为 `303 passed in 16.78s`，清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`。构建前版本已备份到 `D:\0文件夹\备份\异环自动钓鱼-adaptive-width-prebuild-20260713-133821\异环自动钓鱼.exe`，备份 SHA256 为 `F89B18DCAF284BB961068DD5281E05E631923F713F98B5D92FBE684BE033AD0A`。等待真实高品质鱼进入控制、稳定结束、结算关闭和计数。
- 反馈/问题：真实鱼获卡片联合识别与随机重试发布物大小 63,944,436 字节，SHA256 为 `5FC58C3893B17452A2E2069805E900E1058157AFFA0DD1F6F49E21A408F45DAF`；构建门为 `310 passed in 19.53s`，清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`，当前提升权限的 PowerShell 会话直接运行烟雾脚本输出 `SMOKE_OK`。构建前版本已备份到 `D:\0文件夹\备份\异环自动钓鱼-result-dismiss-prebuild-20260713-145224\异环自动钓鱼.exe`，备份 SHA256 为 `BFE86E6485ABB3B547DEC24F2D52DD295D64303E332831D04B429DFA2A4526E7`。等待真实鱼获卡片关闭和计数。
- 反馈/问题：连续三帧卡片消失确认发布物大小 63,944,934 字节，SHA256 为 `BF7520B44CBCA60F4C63DE8CA78461B745451183F67656540ED0ACD22F053521`；构建门为 `312 passed in 22.09s`，清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`，烟雾脚本输出 `SMOKE_OK`。构建前版本已备份到 `D:\0文件夹\备份\异环自动钓鱼-result-closure-prebuild-20260713-151847\异环自动钓鱼.exe`，备份 SHA256 为 `5FC58C3893B17452A2E2069805E900E1058157AFFA0DD1F6F49E21A408F45DAF`。等待真实关闭后的完成计数。
- 反馈/问题：青绿色卡片与初始鱼钩确认发布物大小 63,942,764 字节，SHA256 为 `BD9A39318A64D6C17AC70108F23A81600B5060A22ADB86A05B9C30931DB775BE`；构建门为 `315 passed in 21.81s`，清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`，烟雾脚本输出 `SMOKE_OK`。构建前版本已备份到 `D:\0文件夹\备份\异环自动钓鱼-green-result-ready-hook-prebuild-20260713-154341\异环自动钓鱼.exe`，备份 SHA256 为 `BF7520B44CBCA60F4C63DE8CA78461B745451183F67656540ED0ACD22F053521`。等待真实青绿色卡片点击与初始鱼钩计数。
- 反馈/问题：色相无关卡片与初始鱼钩确认发布物大小 63,942,912 字节，SHA256 为 `C9DF9470AFB7871B3E0720B27A868C4C7B5BAFE0688B70551D9EE17075C3D6C8`；构建门为 `322 passed in 22.21s`，清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`，烟雾脚本输出 `SMOKE_OK`。构建前版本已备份到 `D:\0文件夹\备份\异环自动钓鱼-hue-agnostic-result-prebuild-20260713-160321\异环自动钓鱼.exe`，备份 SHA256 为 `BD9A39318A64D6C17AC70108F23A81600B5060A22ADB86A05B9C30931DB775BE`。等待不同鱼种卡片点击与初始鱼钩计数。
- 反馈/问题：稳定屏幕键盘尺寸发布物大小 63,943,001 字节，SHA256 为 `67AA6C2205D5E313D6B7E992710076738A81DDC7354DC3903DABB2E9D4CA6709`；构建门为 `326 passed in 22.13s`，清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`，从当前提升权限会话直接执行烟雾脚本输出 `SMOKE_OK`。构建前版本已备份到 `D:\0文件夹\备份\异环自动钓鱼-osk-geometry-prebuild-20260713-1623\异环自动钓鱼.exe`，备份 SHA256 为 `C9DF9470AFB7871B3E0720B27A868C4C7B5BAFE0688B70551D9EE17075C3D6C8`。真实屏幕键盘恢复尺寸和 F/A/D 命中待人工确认。
- 反馈/问题：历史识别回退与点击即计数发布物大小 63,944,861 字节，SHA256 为 `B42D558A47695E0808EAE4F09EB817A05E6458F041019A37807D6AEF9652BA19`；构建门为 `313 passed in 20.45s`，清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`，烟雾脚本输出 `SMOKE_OK`。构建前版本已备份到 `D:\0文件夹\备份\异环自动钓鱼-pre-ready-hook-rollback-prebuild-20260713-1648\异环自动钓鱼.exe`，备份 SHA256 为 `67AA6C2205D5E313D6B7E992710076738A81DDC7354DC3903DABB2E9D4CA6709`。完整单轮待人工确认。
- 反馈/问题：进度条消失后定时点击发布物大小 63,944,208 字节，SHA256 为 `94FCEDD67EF524A77F73EE43AAA84E60290D49E153C36D44DD64816C49FD3C0F`；构建门为 `309 passed in 28.42s`，清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`，烟雾复测输出 `SMOKE_OK`。构建前真正被覆盖的隔离分支发布物已备份到 `D:\0文件夹\备份\异环自动钓鱼-timed-result-click-prebuild-20260713-1717\异环自动钓鱼-worktree.exe`，备份 SHA256 为 `B42D558A47695E0808EAE4F09EB817A05E6458F041019A37807D6AEF9652BA19`。定时点击与点击即计数的完整单轮待人工确认。
- 反馈/问题：结算延迟整体增加 1 秒的发布物大小 63,944,482 字节，SHA256 为 `7EF2290E39B8CB5F6343D03B96A04E34AA5C00EF6DD0C7E08483112ECC0D3430`；构建门为 `309 passed in 31.40s`，清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`，烟雾输出 `SMOKE_OK`。构建前版本已备份到 `D:\0文件夹\备份\异环自动钓鱼-result-delay-plus-1s-prebuild-20260713-1736\异环自动钓鱼.exe`，备份 SHA256 为 `94FCEDD67EF524A77F73EE43AAA84E60290D49E153C36D44DD64816C49FD3C0F`。3.10～3.60 秒点击与点击即计数的完整单轮待人工确认。
- 反馈/问题：轮间自动续轮、双级进度丢失和自动切回发布物大小 64,125,790 字节，SHA256 为 `99E741A0B42EF38FF8EB4B892C0D989EA0C56681837BA523FA52726B59D85927`；构建门为 `328 passed in 33.74s`，清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`，直接烟雾复测输出 `SMOKE_OK`。构建前版本备份为 `D:\0文件夹\备份\异环自动钓鱼-multiround-auto-activation-prebuild-20260713-1828\异环自动钓鱼.exe`，SHA256 为 `7EF2290E39B8CB5F6343D03B96A04E34AA5C00EF6DD0C7E08483112ECC0D3430`。连续第二轮与 60 帧实机表现待人工确认。
- 反馈/问题：运行 `run-20260713T104137187956Z` 的第二轮基线帧 402 右下仍显示蓝色准备鱼钩按钮，低分辨率蓝色比例约 58.3%；运行 `run-20260713T103932106773Z` 的失败基线帧 891 约 71.9%，而后续真实上钩提示无法再增加 3%，导致整轮漏判。前 45 帧冷却期将蓝色基线降到实际背景低位后，五段真实序列首次命中帧为 121、558、627、1018、1395；视觉核对均已出现上钩圆环，未在普通画面提前命中。白色/边缘门槛和连续两帧确认未改变。
- 反馈/问题：动态上钩蓝色基线发布物大小 64,124,694 字节，SHA256 为 `8EEB5CD2E3BB9C0B225836FD6613126C22359351E985F73903280E7039796BAD`；构建门为 `329 passed in 35.83s`，清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`，烟雾输出 `SMOKE_OK`。上一版多轮已通过发布物备份为 `D:\0文件夹\备份\异环自动钓鱼-bite-blue-baseline-prebuild-20260713-1853\异环自动钓鱼.exe`，SHA256 为 `99E741A0B42EF38FF8EB4B892C0D989EA0C56681837BA523FA52726B59D85927`。等待实机复验第二次 F 稳定性。
- 反馈/问题：加权中心控制发布物大小 63,950,306 字节，SHA256 为 `A360CE23D2F75E0E924C222765AA8434C995505A1379160AF494BD2FC5C0FD7F`；构建门为 `340 passed in 31.49s`，清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`，烟雾输出 `SMOKE_OK`，用户真实游戏验收通过。已知实机跟随失败的上一发布物备份为 `D:\0文件夹\备份\异环自动钓鱼-weighted-control-prebuild-20260713-2301\异环自动钓鱼.exe`，SHA256 为 `EBA0E5C6203F6C5E5413B0BB7F7A90AEA846C001F50DB74475C4D386E91FE79E`。

## 11. V2 免安装、按需诊断与系统兼容性

### 验收步骤

1. 从单文件 `异环自动钓鱼V2.exe` 启动并关闭，不安装 Python 或项目依赖；检查窗口标题明确显示 V2。
2. 在隔离的 `%LOCALAPPDATA%` 完成正常启动/关闭，确认只允许生成 `config.json`，不存在 `runs`、逐帧截图或持续事件日志。
3. 注入自动化错误并点击一次“报告错误”，分别等待诊断完成；检查程序显示 ZIP 路径，“打开文件位置”可选择对应文件。
4. 解压诊断包，检查 `error.json`、最近 30 秒 `events.jsonl`、最近 30 秒 `frames/`、`progress/trace.jsonl` 和 `progress/frames/`，有当前帧时检查 `error.jpg`；连续生成 6 份后只保留最近 5 份。
5. 对 1280×720、1600×900、1920×1080、2560×1440、3440×1440、3840×2160 与 100%～200% 缩放标签运行几何矩阵；检查低分辨率屏幕键盘不遮挡右下准备区、控制窗口被夹取到最近显示器工作区、DPI 回退链可用。
6. 执行 `py -3.13 -m pytest try/tests -q`、`powershell -ExecutionPolicy Bypass -File scripts/build_v2.ps1 -PythonPath <python>`，并在管理员 PowerShell 执行 `& .\try\smoke_exe.ps1 -TargetPath .\dist\异环自动钓鱼V2.exe`。

### 预期与证据

- 最终用户只需双击单文件 EXE 并确认 Windows UAC，不需要管理员命令行、批处理或 PowerShell 脚本；提升 PowerShell 仅是开发烟雾测试前提。
- V2 复用 V1 已验收的识别、状态机和屏幕键盘输入，不复制第二套自动钓鱼逻辑。
- 正常运行仅在内存保留最近 30 秒事件、每个已处理进度帧的识别轨迹、最高 10 FPS 且最长边 480 的上下文 JPEG，以及最高 10 FPS 的原生宽度无损顶部进度槽 PNG；内存编码失败只增加对应丢弃计数，不以日志故障暂停主循环。
- 自动错误同一次暂停只报告一份；主动报告先释放所有输入并安全暂停，即使尚未绑定游戏也能生成包。ZIP 写入使用冻结快照和独立后台线程，不阻塞自动化线程。
- 诊断目录固定为 `%LOCALAPPDATA%\异环自动钓鱼V2\diagnostics`，只清理由本程序命名的 `yihuan-v2-*.zip`，最多 5 份。
- 内嵌清单同时包含管理员权限、`PerMonitorV2` 和 `true/pm` 回退；Windows 11 构建/烟雾为实测，Windows 10 因当前无真机只标记人工确认。

### 结果记录

- 状态：V2 诊断、界面、装配、DPI、窗口、屏幕键盘、打包和既有功能回归当前共 418 项通过；新增测试证明四路证据保留到 30 秒边界，快照深拷贝后不受后续写入影响，序号在并发写入下保持严格递增。412 帧真实控制回放通过；隔离正常烟雾仅生成设置文件，未生成运行日志、逐帧目录或诊断 ZIP。
- 状态：本地 `v2.0.2` 单文件大小 63,986,763 字节，SHA256 `0C1D4491C3CC6E7D508DD367C270553E8D2048F34F1E2E30E7EDC94D626BF51F`；根目录与 `dist` 同哈希，内嵌清单复核通过。提升权限启动/响应/关闭烟雾证据仍来自 `v2.0.0`，本候选待管理员会话补验；GitHub 最新发布仍为 `v2.0.1`。
- 反馈/问题：首次从中等完整性测试壳启动 `requireAdministrator` 候选后，测试壳无权发送关闭消息，留下该次 PyInstaller 父/子进程并误报烟雾失败。精确核验 PID 后在提升会话清理；正式烟雾脚本现于启动前拒绝非管理员会话，回归测试确保权限检查位于 `Start-Process` 前。该问题是测试权限边界，不是 V2 主程序退出故障。
- 人工确认：Windows 10 真机、不同厂商 125%～200% 混合缩放、真实游戏窗口/无边框/全屏和“打开文件位置”的可见交互仍需发布后人工复核。

## 12. 第二次 F 后进度入口与继续重开当前轮

### 验收步骤

1. 读取主动报告 `C:\Users\29551\AppData\Local\异环自动钓鱼V2\diagnostics\yihuan-v2-20260714T123549841476Z-MANUAL_REPORT.zip`，核对第二次 F、首个错误进度观测和真实顶部进度界面出现时序。
2. 执行 `py -3.13 -m pytest try/tests/test_state_machine.py try/tests/test_engine.py try/tests/test_ui_smoke.py -q`，检查第二次 F 后 0.60 秒启用门、连续 3 帧确认、短绿条、极端黄标、时间戳异常、计数保留和继续重开的输入安全。
3. 执行 `py -3.13 -m pytest try/tests -q`、`py -3.13 -m compileall -q src try/tests scripts` 和保留真实运行的 412 帧控制回放。
4. 执行 `powershell -ExecutionPolicy Bypass -File scripts/build_v2.ps1 -PythonPath C:\Users\29551\AppData\Local\Programs\Python\Python313\python.exe`，检查最终 EXE 内嵌清单。
5. 在管理员 PowerShell 运行 `& .\try\smoke_exe.ps1 -TargetPath .\dist\异环自动钓鱼V2.exe`；在真实游戏分别复测普通鱼、短绿区高品质鱼，以及等待上钩/等待进度/控制/等待结算四阶段暂停后点击继续。

### 预期与证据

- 第二次 F 后首张后续帧开始 0.60 秒启用期；期间即使连续出现诊断包中的错误绿/黄颜色观测也保持 `WAIT_BAR`，不发 A/D，并清空视觉历史。
- 启用期后连续 3 张新鲜完整观测才进入 `CONTROL`；缺失、非递增时间戳或超过 0.20 秒间隔会重置确认。不得添加黄标/绿区相对位置约束，现有短绿区宽度保持不变。
- 点击“继续”保留 `completed/target`，释放输入并清空上钩、进度、结算状态，从 `READY` 的第一 F 开启新一轮；错误 `progress/result/ready` 单帧不得直接恢复中间阶段。
- 迟到识别帧、窗口错误和截图错误不得清除较新的重开令牌；ABA（A-B-A）竞态只能由当前代次请求生效。

### 结果记录

- 状态：诊断包中第二次 F 约在 `20:35:40.623` 释放，约 27 毫秒后旧程序即进入 `CONTROL`；首个错误观测约为绿区 `0.100～0.278`、黄标 `0.537`，最终 94 次方向决策中 A 为 64 次、D 为 30 次。截图证明该时刻仍是“鱼上钩”横幅，真正顶部界面尚未稳定出现。
- 状态：测试先行新增诊断时序、连续帧、时间戳、窄绿条、极端位置、四类暂停来源、错误画面重开、令牌竞态和 UI 文案覆盖；全套 407 项通过，耗时 33.95 秒。412 帧真实控制回放输出 `REAL_CONTROL_REPLAY_OK 412 15 ['left', 'release', 'right']`。
- 状态：V2 构建门再次通过 407 项；候选 SHA256 为 `11105E3828BC33A4CF094196AEC29B97220F19C69AEAC3E7D6ED66F7F74F64D3`，内嵌清单为 `requireAdministrator uiAccess=false dpi=PerMonitorV2 fallback=true/pm`。
- 状态：旧根目录 V2 已按同哈希备份到 `D:\0文件夹\备份\异环自动钓鱼-v2-progress-entry-20260714-213122\`；根目录现为本次候选，大小 63,978,755 字节、SHA256 与 `dist` 一致，根目录清单复核通过。
- 反馈/问题：当前 PowerShell 未提升，正式烟雾脚本在创建进程前按设计拒绝运行，因此本候选的启动/响应/关闭烟雾和真实游戏效果均标记为人工确认，不使用上一发布物烟雾结果冒充本候选证据。

## 13. 进度控制错乱的 30 秒诊断证据

### 验收步骤

1. 审计现有 V2 诊断 ZIP，分别统计事件和截图覆盖时长，并用保存帧重新运行进度识别，对比包内 `progress.control` 的绿区/黄标坐标。
2. 构造超过 30 秒的事件、进度轨迹、上下文帧和进度槽帧，冻结快照并生成诊断 ZIP；检查 30 秒边界、严格递增序号、深拷贝和各流丢弃计数。
3. 解压 ZIP，逐帧核对 `progress/trace.jsonl` 的五条扫描线、黄/绿候选、历史参考、最终选择、拒绝原因，与 `progress/frames/*.png`、`events.jsonl` 的帧时间戳和控制请求是否可关联。
4. 在真实游戏出现错乱时立即点击“报告错误”，将新 ZIP 用于区分颜色分割错误、候选排序错误、历史参考错误和输入状态错误。

### 预期与证据

- `error.json` 使用 schema 2，记录各证据流数量、首末时间戳、覆盖秒数、丢弃计数、最近控制决策，以及当前 EXE 的文件名、大小和 SHA256；EXE 哈希读取失败时只记录元数据错误，仍成功生成 ZIP。
- `progress.control` 必须记录帧时间戳、绿区/黄标坐标、瞬时与加权误差、方向、样本数、置信度、请求按键和切换前输入状态；识别轨迹保留每个已处理进度帧，不受 10 FPS 图片采样限制。
- 用户主动报告在自动 `E_WINDOW` 包后 1 秒内完成时，只在主动包成功落盘后删除重复自动包；主动包失败必须保留自动包。

### 结果记录

- 状态：最新旧版主动包包含约 19.988 秒、1,673 条事件和约 9.835 秒、82 张 480 像素 JPEG；可确认 73 次方向切换和 11 次绿区大跳变，但有 33 个可比较事件时只有 3 个重放坐标在 0.01 内一致，且更早异常落在截图窗口之外。结论是旧包支持确认“识别坐标跳变后错误长按”的症状，不支持有把握地修复具体识别根因。
- 状态：本地 `v2.0.2` 已实现 30 秒四路证据并通过对应回归；2560×1440 等效合成帧连续 901 次处理平均 1.809 ms、P95 5.617 ms、最大 7.779 ms，共保留 901 条事件、901 条轨迹、301 张上下文 JPEG 和 301 张进度 PNG，三类丢弃计数均为 0。
- 状态：已取得本地 `v2.0.2` 新包 `yihuan-v2-20260715T022333974433Z-MANUAL_REPORT.zip`，其 29.99 秒证据含 2,131 条事件、629 条轨迹、251 张上下文图和 180 张无损进度槽，三类丢弃均为 0；已足以定位并离线验收具体识别修复。

## 14. 左移绿区与蓝灰背景连续段区分

### 验收步骤

1. 只读审计 `C:\Users\29551\AppData\Local\异环自动钓鱼V2\diagnostics\yihuan-v2-20260715T022333974433Z-MANUAL_REPORT.zip`，对齐第 591、1117、1121 帧的无损进度槽、逐帧轨迹与控制事件。
2. 执行 `py -3.13 -m pytest try/tests/test_progress.py -q`，验证最新真实颜色/几何回归、短绿区、黄标切断绿区、微小噪声拒绝和各分辨率比例。
3. 用修复后的 `ProgressRecognizer` 逐张回放包内 180 张 `progress/frames/*.png`，统计控制期完整观测和进度结束后背景拒绝结果。
4. 执行 `py -3.13 -m pytest try/tests -q`、`py -3.13 -m compileall -q src try/tests scripts`、412 帧历史控制回放和 V2 单文件构建；复核根目录与 `dist` EXE 的大小、SHA256 和内嵌清单。
5. 在管理员 PowerShell 补做 EXE 启动/响应/关闭烟雾；在真实游戏复测绿区持续左移、短绿区、A/D 回中和完整单轮计数。

### 预期与证据

- 实色绿区通过 HSV `H=70～105、S=155～255、V=100～255` 的横向连续段提供真实性证据；低饱和蓝灰轨道、树叶和建筑背景即使形成三线一致连续段，也不能独立成为绿区。
- 黄标切断绿区时，允许 `S>=80` 的宽松边界恢复完整逻辑外边界，但该候选累计获得的实色支撑必须达到现有最小逻辑绿区宽度。
- 最小逻辑绿区宽度保持 `max(4 像素, TOP_ROI 宽度 * 0.012)`；不得以增加最小长度的方式排除背景，避免漏掉高品质短绿区。
- 第 1117 帧应输出 `green=0.198198～0.340841、yellow=0.850976`；第 1121 帧后的蓝灰背景不得输出假绿区。

### 结果记录

- 状态：旧实现第 1117 帧把蓝灰背景 `x=1101～1126` 当作绿区，忽略真实高饱和青绿色连续段 `x=264～454`；第 591 帧也曾把 `x=223～286` 背景写入历史。测试先行回归在旧实现上返回 `green_left=0.6006`，修复后返回约 `0.1982`。
- 状态：定向进度测试 `43 passed`；全套源码回归 `419 passed in 34.64s`，编译和空白检查通过。180 张最新包回放中，第 579～1117 帧的 155 张全部输出真实绿区，第 1121 帧后的 21 张全部拒绝背景；412 帧历史控制回放保持 `REAL_CONTROL_REPLAY_OK 412 15 ['left', 'release', 'right']`。
- 状态：`v2.0.3` 构建门再次通过 419 项，根目录与 `dist` EXE 均为 63,985,836 字节、SHA256 `8C43403BF2AB848DDF265E0385EDFCB95FC638F1CDAEB1F1647F8BBF92862BA1`；内嵌清单为 `requireAdministrator uiAccess=false dpi=PerMonitorV2 fallback=true/pm`。
- 人工确认：当前 PowerShell 未提升，因此 `v2.0.3` 管理员烟雾、真实游戏闭环、Windows 10 真机、窗口/无边框/全屏和混合缩放仍待人工复验。

## 15. Windows 系统弹窗与干净退出

### 验收步骤

1. 绑定游戏后，在 F 的 80～180 毫秒随机延迟期间让 Windows 系统弹窗抢占前台；另在 A/D 按住和结算点击前触发一次弹窗。
2. 检查弹窗出现后没有新的 F/A/D 按下或结算点击，已经按下的键和鼠标左键仍被释放；关闭弹窗后确认程序不会自动恢复，再点击“继续”。
3. 单独点击控制窗口，确认按用户暂停分类且不生成自动错误包；用其他窗口或系统弹窗抢占，确认按 `E_WINDOW` 分类并显示关闭弹窗后继续的提示。
4. 在屏幕键盘已自行消失的情况下退出；另以测试替身模拟输入释放、屏幕键盘关闭或诊断关闭失败，检查退出顺序和清理诊断。
5. 执行 `py -3.13 -m pytest try/tests -q` 和 `powershell -ExecutionPolicy Bypass -File scripts/build_v2.ps1 -PythonPath C:\Users\29551\AppData\Local\Programs\Python\Python313\python.exe`；在提升的 PowerShell 中补做 `& .\try\smoke_exe.ps1 -TargetPath .\dist\异环自动钓鱼V2.exe`。

### 预期与证据

- 输入守卫位于实际新按下/点击边界；F 延迟结束后会再次检查前台。释放动作不经过守卫，因此弹窗出现时仍能抬起已有输入。
- 控制窗口前台为 `E_USER_PAUSE` 且不自动保存诊断；Windows 系统弹窗或其他窗口前台为 `E_WINDOW`，不会自动恢复。
- 屏幕键盘关闭返回错误 1400 时视为句柄已经失效；错误 5 的安全保留策略不变。
- 关闭窗口时先停止控制器，再保存设置并销毁 Tk 窗口。仅清理失败时尝试生成 `E_CLEANUP`，记录后正常返回；主循环本身的运行期异常仍继续抛出。
- 证据：定向输入/窗口/引擎/屏幕键盘/UI 测试、全量测试、源码空闲退出 `CLEAN_EXIT_OK`、EXE 内嵌清单和提升权限烟雾。

### 结果记录

- 状态：新增 9 项回归在旧实现上全部按预期失败，修复后全部通过；相关模块 265 项通过，全套 `428 passed`。源码空闲启动后关闭返回 `CLEAN_EXIT_OK`，清理失败测试确认生成 `E_CLEANUP` 且不重新抛出退出异常。
- 状态：本地 `v2.0.4` 单文件构建再次运行全部 428 项并通过，根目录与 `dist` EXE 均为 63,989,100 字节、SHA256 `402DC0003D0E5FC8E1C537D6B2D4096BF3D24E15E18CC8A7BAA0E135B4C4790E`；内嵌清单为 `requireAdministrator uiAccess=false dpi=PerMonitorV2 fallback=true/pm`。
- 人工确认：用户于 2026-07-15 确认 `v2.0.4` 已完成真实游戏验证且为当前最稳定版本，并授权发布。Windows 10 真机和完整窗口/无边框/全屏及混合缩放矩阵继续保留人工确认。

## 16. 公开仓库 README 与非商业许可证

### 验收步骤

1. 检查根目录 `README.md`，确认下载、UAC 启动、绑定、3 秒切回游戏、`F8` 暂停、诊断位置、源码运行、测试和构建命令均可找到。
2. 检查 README 的仓库、Release、版本、`AGENTS.md`、验收标准和 `LICENSE` 链接，确认本地链接存在，远程链接可访问。
3. 对照 PolyForm 官方文本检查根目录 `LICENSE`，确认名称、版本、官方 URL、全部标准条款及 `Required Notice` 完整存在。
4. 检查 README 明确显示 GitHub 最新正式 Release `v2.0.4`、用户真实游戏验证结论、自动测试与构建证据，并保留 Windows 10 人工确认边界。
5. 执行 `git diff --check`、全套 `pytest` 和公开历史敏感信息扫描；确认 `.env`、EXE、诊断 ZIP 和本地配置未被 Git 跟踪。
6. 检查 README 将实现描述为屏幕识别与 Windows 标准输入驱动的非侵入式自动化；允许说明操作形式接近普通玩家，但不得承诺能够绕过或避免反作弊检测。

### 预期与证据

- 新用户只读 README 即可找到已发布 EXE、基本使用流程、安全暂停方式、诊断隐私提示和开发命令。
- 许可证采用未经删减的 PolyForm Noncommercial 1.0.0 英文标准条款；README 的中文说明仅作摘要，并明确商业用途需要另行许可、该许可证不属于 OSI 开源许可证。
- GitHub 仓库状态、可见性、URL 和 Release 信息与 `AGENTS.md`、README、远端实际状态一致。
- 公开推送只包含 Git 已跟踪的源码和文档，不包含 Git 忽略的候选 EXE、本地设置、诊断包、游戏截图或真实环境变量。
- README 不使用“最保险”“不会封号”或“可规避反作弊”等不可验证承诺，明确非侵入式实现仍可能被反作弊系统识别。

### 结果记录

- 状态：README 本地链接全部存在；仓库主页、Release 列表、最新 `v2.0.4` Release 和 PolyForm 官方许可证链接均返回 HTTP 200。许可证 15 个标准章节齐全，去除 Markdown 格式和项目 `Required Notice` 后与官方 `.txt` 的 682 个词逐词一致。
- 状态：全套源码回归 `428 passed in 39.53s`，`compileall`、`git diff --check` 和常见 GitHub/AWS/私钥模式扫描通过；Git 未跟踪 `.env`、EXE、ZIP 或 `config.json`。最终 GitHub 推送结果在本阶段进展记录和会话完成报告中确认。
- 状态：免责声明已改为“操作形式上模拟普通玩家的手动操作”的非侵入式说明，同时保留无法保证不被识别、不得用于规避检测和使用者自行承担风险的边界。
- 状态：`v2.0.4` Release 资产名为 `V2.exe`；GitHub 服务器摘要及远端回下载结果均为 63,989,100 字节、SHA256 `402DC0003D0E5FC8E1C537D6B2D4096BF3D24E15E18CC8A7BAA0E135B4C4790E`，回下载文件的内嵌清单复核通过。

## 最终结论

V1 的加权中心控制和极端黄标修复已完成真实游戏验收并以 `v1.0.0` 留存。GitHub V2 最新发布为 `v2.0.4`；该版本在每次物理新输入前守卫游戏前台，区分控制窗口与 Windows 系统弹窗抢占，并把清理失败转为 `E_CLEANUP` 证据而非退出异常弹窗。版本已完成 428 项自动测试、180 张最新诊断进度槽回放、412 帧历史控制回放、Windows 11 单文件构建及管理员/DPI 内嵌清单校验；用户已完成真实游戏验证并确认其为当前最稳定版本。Windows 10 真机及完整分辨率和不同厂商混合缩放矩阵仍按上述人工验收项执行。
