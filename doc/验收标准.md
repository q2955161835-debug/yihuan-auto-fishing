# 《异环》自动钓鱼验收标准

## 文档状态

- 当前阶段：离线自动测试、单文件构建和发布物烟雾已完成，等待真实游戏人工验收。
- 自动测试结果：`2026-07-10` 在全新 `try/output/clean-venv` 中按文档安装固定依赖和项目可编辑包，非仓库根目录执行 `import auto_fishing` 成功；全套 188 项通过，耗时 17.313 秒，构建门内复跑 188 项通过。
- 构建与烟雾结果：通过 `-PythonPath` 使用上述干净解释器完成最终单文件构建，耗时 48.628 秒，构建门内 188 项通过；启动/响应/关闭烟雾输出 `SMOKE_OK`，耗时 6.040 秒。并发同路径实例前后 PID 均为 `123632,123788`，证明烟雾只关闭本次启动器进程树；测试结束后同路径残留进程数为 0。
- 实机结果：人工确认（未执行）。
- 发布结论：离线验收通过；真实游戏和无开发环境独立会话验收未完成，当前不可发布或合并主分支。

## 1. 启动与置顶窗口

### 验收步骤

1. 双击 `异环自动钓鱼.exe`。
2. 检查窗口尺寸、拖动、最小化、置顶和关闭。
3. 检查目标次数仅允许 1～999。
4. 尝试在运行中修改次数。

### 预期与证据

- 窗口正常打开且无控制台黑窗。
- 窗口模式和无边框游戏下始终置顶；独占全屏按设计说明处理。
- 运行中目标次数被锁定。
- 证据：启动冒烟测试记录和人工截图。

### 结果记录

- 状态：发布物启动、响应、关闭和 Windows GUI 子系统离线检查通过；窗口尺寸、拖动、最小化、置顶、次数边界及运行锁定为人工确认（未执行）。
- 反馈/问题：`try/smoke_exe.ps1` 输出 `SMOKE_OK` 且本次拥有的进程树残留数为 0；并发隔离输出 `CONCURRENT_OWNERSHIP_OK`，其他同路径实例未被检查、关闭或终止。PE 子系统值为 2，未显示控制台子系统。独占全屏可见性仍按设计限制人工确认。

## 2. 游戏窗口绑定

### 验收步骤

1. 点击“绑定游戏”，在三秒内切换到《异环》。
2. 分别使用窗口、无边框和全屏。
3. 移动或调整游戏窗口尺寸后重新开始。
4. 尝试绑定无效窗口、最小化窗口和过小窗口。

### 预期与证据

- 显示正确窗口标题、客户区尺寸和绑定状态。
- 分辨率或位置变化后重新计算客户区，不使用固定 2560×1440 坐标。
- 运行时至少每 0.5 秒复核客户区位置和尺寸。
- 无效状态安全暂停并显示具体原因。
- 证据：窗口坐标自动测试、状态日志和实机人工确认。

### 结果记录

- 状态：窗口坐标、DPI、显示器选择和异常路径自动测试通过；窗口、无边框和全屏绑定为人工确认（未执行）。
- 反馈/问题：结构扫描确认生产代码没有固定 2560×1440 坐标；该分辨率仅作为多分辨率测试参数。

## 3. 单轮完整流程

### 验收步骤

1. 在钓鱼就绪画面设置目标 1 次并开始。
2. 观察第一次 F、上钩识别、第二次 F、进度条控制、结算点击和返回就绪。
3. 检查完成计数。

### 预期与证据

- 顺序与设计状态机一致，不跳过状态。
- 只有结算关闭且就绪画面重新出现后，完成数量从 0 变为 1。
- 证据：状态迁移测试、简短运行日志和实机人工确认。

### 结果记录

- 状态：状态迁移、结算确认、计数和引擎合成帧测试通过；真实游戏单轮流程为人工确认（未执行）。
- 反馈/问题：自动证据不能证明真实画面阈值有效，必须实机保存日志与结果。

## 4. 每秒 30 帧闭环控制

### 验收步骤

1. 运行合成帧回放，绿色区域和黄色标记以不同速度和方向移动。
2. 验证黄色标记偏左、偏右和位于安全带时的输入。
3. 插入单帧和连续六帧识别缺失。
4. 在真实游戏快速变化场景中观察实际帧率和控制结果。

### 预期与证据

- 偏左短按 D，偏右短按 A，进入内缩安全带释放两键。
- 换向时先释放旧键，任意时刻不同时按 A/D。
- 一帧缺失立即释放；连续约 0.2 秒缺失则暂停。
- 控制线程消费最新帧，不积压回放旧帧。
- 证据：`python -m pytest try/tests -q`、回放统计和实机人工确认。

### 结果记录

- 状态：30 帧最新帧、方向、换向释放、安全带和识别缺失自动测试通过；真实游戏快速变化连续 5 轮为人工确认（未执行）。
- 反馈/问题：实机需记录实际帧率、成功数及每次暂停原因，不得用固定延时绕过识别失败。

## 5. 暂停、F8 与异常保护

### 验收步骤

1. 分别在等待上钩、闭环控制和结算阶段点击暂停或按 F8。
2. 将游戏切出前台、最小化或关闭。
3. 模拟截图停滞、识别超时和输入失败。
4. 检查恢复行为。

### 预期与证据

- 所有路径立即释放 F/A/D/鼠标左键且停止新输入。
- 错误原因显示在界面并记录一次诊断。
- 恢复前重新分类画面，无法分类时保持暂停。
- 证据：按键记录测试、状态机测试和实机人工确认。

### 结果记录

- 状态：F8、失去前台、截图停滞、超时、输入失败和退出释放的自动测试通过；真实游戏中的立即释放和恢复为人工确认（未执行）。
- 反馈/问题：实机需要在控制阶段按 F8，并检查 A/D 实际释放。

## 6. 多轮与计数

### 验收步骤

1. 分别设置 2、5 和边界值次数。
2. 在中途触发一次暂停后继续。
3. 检查最终完成状态及是否多执行一轮。

### 预期与证据

- 成功轮数等于目标数，不少执行也不多执行。
- 未完成轮次不计数，恢复后不会重复计入已成功轮次。
- 证据：状态机自动测试与实机人工确认。

### 结果记录

- 状态：目标边界、暂停恢复、完成计数和不多执行自动测试通过；真实游戏 2 轮、5 轮流程为人工确认（未执行）。
- 反馈/问题：无离线失败；尚无真实成功轮数数据。

## 7. 诊断文件自动清理

### 验收步骤

1. 在隔离的测试目录生成不同日期和数量的诊断文件。
2. 运行清理逻辑。
3. 检查 7 天边界、20 组上限和目录边界。
4. 确认 `流程截图` 未发生变化。

### 预期与证据

- 删除超过 7 天的诊断，只保留最近 20 组。
- 正常帧不落盘，清理逻辑不越出程序诊断目录。
- 证据：清理测试、清理前后文件清单和 Git 状态。

### 结果记录

- 状态：隔离目录中的 7 天边界、20 组上限、并发保存和目录边界自动测试通过；真实 `%LOCALAPPDATA%` 诊断样本清理为人工确认（未执行）。
- 反馈/问题：结构检查确认 `流程截图` 未修改；自动测试不越出隔离目录。

## 8. 构建与发布物

### 验收步骤

1. 执行构建脚本。
2. 在无项目虚拟环境依赖的 Windows 会话启动可执行程序。
3. 完成窗口打开、绑定倒计时、F8 注册和退出冒烟测试。

### 预期与证据

- 生成 `dist/异环自动钓鱼.exe`。
- 启动无控制台黑窗，关闭后无残留进程和被按住的键。
- 证据：构建命令输出、文件哈希、启动冒烟记录。

### 结果记录

- 状态：全新 Python 3.13 虚拟环境中的固定依赖安装、项目可编辑安装、导入、全套测试、单文件构建和发布物启动/响应/关闭烟雾通过；无开发环境依赖的独立 Windows 用户会话为人工确认（未执行）。
- 反馈/问题：发布物 `D:\1Folder\异环自动钓鱼\.worktrees\auto-fishing\dist\异环自动钓鱼.exe`，大小 62,851,946 字节，生成时间 `2026-07-10 20:29:10 +08:00`，SHA256 `48E6151BACB129E551AFBCE7C876FF2DB2E0DF9B2C630BADDF67CA187352CD43`。本次干净构建与前次项目 `.venv` 构建的大小和哈希不同，原因是重新构建会更新 PE/归档元数据；依赖版本、PyInstaller 版本和 Python 版本一致。首次干净构建开始时动态命名空间尚不存在，因此 `warn-auto_fishing.txt` 含 2 条 `comtypes.gen` 静态分析告警且 `Analysis-00.toc` 未收集；该次 PyInstaller 分析过程于 `20:22:33` 在干净环境生成 `comtypes/gen/__init__.py`。最终重建的警告文件不再包含 `comtypes.gen`，`Analysis-00.toc` 和 `PYZ-00.toc` 均已收集该命名空间；警告与 TOC 证据已分开记录。启动烟雾未出现缺模块错误，但 DXcam/COM 实际捕获仍为真实游戏人工确认。首次烟雾脚本只结束单文件启动器父进程，遗留子进程并导致覆盖构建出现 `WinError 5`；第一版修复又按 `ExecutablePath` 批量枚举，存在误触同时启动实例的风险。最终脚本改为保留启动器 PID 种子、按 `ParentProcessId` 递归维护所有权集合，并只操作该集合；正常和并发烟雾均通过。构建哈希表中的中文路径经 Windows PowerShell 管道显示乱码，但独立文件查询确认真实路径、大小和哈希不受影响。

## 最终结论

离线自动验收结论为通过：全新 Python 3.13 虚拟环境安装与导入成功，188 项测试通过，PyInstaller 单文件构建成功，发布物启动、响应、进程树所有权隔离和关闭烟雾通过且无测试进程残留，安全与结构扫描无生产代码命中。真实《异环》的窗口、无边框、全屏、快速进度条、F8 实际释放、多轮成功率和真实诊断清理均为人工确认（未执行）；无开发环境依赖的独立 Windows 用户会话启动也未执行。因此发布验收尚未完成，不合并 `main`、不删除任务分支、不对外发布。
