# 2026-7-11 进展记录

## 2026-07-11 00:25 ~ 2026-07-11 00:42

### 高风险与回退方案

- 即将重建会覆盖已忽略的 `build/` 与 `dist/异环自动钓鱼.exe`。构建前将当前发布物备份至 `D:\0文件夹\备份\异环自动钓鱼-start-focus-prebuild-20260711-0042\`；若构建或烟雾失败，可恢复当前大小 62,858,394 字节、SHA256 `4F2F15F79905B2424DAC384288C6BAD7F014A065C3A2678AC2D71F92F74FA353` 的发布物。

### 本阶段完成内容

- 根据“绑定后点击开始显示失败”的实机反馈，读取本地配置、诊断目录和 Windows 应用事件；未发现 DXcam 崩溃或诊断文件。DXcam 实测能枚举 `DISPLAY1 (2560×1440)` 并取得 `1440×2560×3` 帧。
- 根因确认：点击置顶控制窗口的“开始”会让它成为前台，worker 随即检查到已绑定游戏失焦并安全暂停，造成“游戏窗口已失去前台”的失败表象。
- 按测试先行新增启动焦点回归：未修复时等待 `WAIT_BITE` 超时，修复后启动顺序为“激活游戏 → 确认前台 → 启动 worker”。同步把两条旧测试从“启动前失焦”修正为更真实的“启动确认后运行期失焦”，继续覆盖 `E_WINDOW` 与输入释放诊断。
- 聚焦回归 `7 passed`，开发环境全套 `204 passed in 18.10s`。

### 新增或修改文件

- `src/auto_fishing/automation/engine.py`：启动前主动激活并复核绑定的游戏窗口；系统拒绝激活时给出明确错误。
- `try/tests/test_engine.py`：新增点击开始后的焦点恢复回归，并保持运行期失焦安全暂停覆盖。
- `doc/进展记录/2026-7-11.md`：记录根因、回归证据、高风险构建及回退方案。

### 错误汇报

- 首次全量测试在新增启动激活后有两条旧测试失败。原因是旧测试把“启动前已失焦”作为运行期失焦模拟，而新的启动前台复核会正确拒绝该不安全起点；调整模拟时机后聚焦与全量测试均通过。

## 2026-07-11 00:42 ~ 2026-07-11 00:44

### 本阶段完成内容

- 已完成 `scripts/build.ps1` 构建门：门内 `204 passed in 17.83s`，PyInstaller 单文件构建退出码为 0。
- 启动/响应/关闭烟雾输出 `SMOKE_OK`；同路径残留进程检查通过。最终发布物大小 62,857,931 字节，SHA256 `73FF69504D753B78BAB10897BD16CDB2ACC2C2E84C0B8CFDF0008445A0155A41`。

### 修改文件

- `AGENTS.md`：同步开始前激活游戏窗口的数据流与 204 项离线基线。
- `doc/验收标准.md`：同步开始焦点修复、204 项测试和最新发布物证据。
- 工作区外 `D:\0文件夹\备份\异环自动钓鱼-start-focus-prebuild-20260711-0042\异环自动钓鱼.exe`：保存覆盖构建前发布物用于回退。

## 2026-07-11 01:20 ~ 2026-07-11 01:44

### 高风险与回退方案

- 即将再次执行单文件构建，会覆盖已忽略的 `build/` 与 `dist/异环自动钓鱼.exe`。先将当前发布物备份到 `D:\0文件夹\备份\异环自动钓鱼-start-countdown-prebuild-20260711-0144\`；构建或烟雾失败时恢复该备份并回退本阶段两个功能提交。

### 本阶段完成内容

- 实机截图确认游戏窗口有效，但 Windows 前台锁策略拒绝控制窗口对游戏的 `SetForegroundWindow` 请求。此前“开始时自动激活游戏”不能覆盖此系统行为，现改为“开始倒计时 3 秒 → 用户手动切回游戏 → 前台验证 → 启动”。
- 按测试先行把开始阶段改为只验证前台：原实现的两项回归均稳定失败，修改后聚焦 2 项通过，引擎 57 项通过；暂停后的继续仍主动激活游戏。
- 新增共享倒计时门控：开始、绑定与重新绑定不会并发；关闭使迟到回调失效。开始倒计时期间不创建 worker、不启动截屏、不发送输入，并锁定次数、绑定、开始和重新绑定控件。
- UI 29 项通过，全套 `207 passed in 18.30s`。

### 新增或修改文件

- `src/auto_fishing/automation/engine.py`：开始阶段改为前台验证和明确的可重试错误；不再抢占游戏焦点。
- `src/auto_fishing/app.py`：新增受代际取消保护的开始倒计时入口。
- `src/auto_fishing/ui/main_window.py`：展示 3、2、1 开始倒计时并在失败后恢复可操作状态。
- `try/tests/test_engine.py`、`try/tests/test_ui_smoke.py`：新增开始前台、倒计时、取消与 UI 锁定回归。
- `docs/superpowers/specs/2026-07-11-start-countdown-design.md`、`docs/superpowers/plans/2026-07-11-start-countdown.md`：本次交互设计及实施计划。

### 错误汇报

- “自动激活并确认已绑定的游戏窗口”在用户真实游戏环境中被 Windows 前台锁拒绝；该失败不是绑定、截图或游戏窗口失效。解决方案改为明确的用户手动切回游戏倒计时，避免依赖会被系统拒绝的强制抢焦点调用。

## 2026-07-11 01:44 ~ 2026-07-11 01:47

### 本阶段完成内容

- 已完成 `scripts/build.ps1` 构建门：门内 `207 passed in 18.97s`，PyInstaller 单文件构建退出码为 0。
- 启动/响应/关闭烟雾输出 `SMOKE_OK`，同路径残留进程检查通过。最终发布物大小 62,859,568 字节，SHA256 `82007817447479F48B1B555F684BEBEF1FAA99DB0B506C251931A81608479A3D`。

### 修改文件

- `AGENTS.md`：同步 3 秒手动切回游戏数据流与 207 项离线基线。
- `doc/验收标准.md`：同步倒计时交互、构建、烟雾和新发布物证据。
- 工作区外 `D:\0文件夹\备份\异环自动钓鱼-start-countdown-prebuild-20260711-0144\异环自动钓鱼.exe`：保存覆盖构建前发布物用于回退。

## 2026-07-11 01:47 ~ 2026-07-11 01:58

### 高风险与回退方案

- 复审修复改变了发布代码，必须重新构建。构建会覆盖已忽略的 `build/` 与 `dist/异环自动钓鱼.exe`，执行前备份到 `D:\0文件夹\备份\异环自动钓鱼-start-countdown-review-prebuild-20260711-0158\`；若失败，恢复当前大小 62,859,568 字节、SHA256 `82007817447479F48B1B555F684BEBEF1FAA99DB0B506C251931A81608479A3D` 的发布物。

### 本阶段完成内容

- 独立审核发现并确认两处 Important 竞态：成功启动但快照未刷新时可能接受第二个开始倒计时；直接通过 Controller 重绑不会使旧开始倒计时失效。
- 先新增三项失败回归，分别覆盖运行中拒绝第二次开始、重绑取消待开始倒计时、成功启动后控件保持锁定；修改后聚焦 3 项通过，UI 32 项通过，全套 `210 passed in 16.68s`。
- Controller 现保存待开始回调，绑定/重绑会使它的代际失效并回调“开始倒计时已取消”；开始入口拒绝倒计时中、启动中或已运行状态。UI 在成功启动回调后保持运行锁，直至异步状态快照抵达。

### 新增或修改文件

- `src/auto_fishing/app.py`：开始回调取消、重绑前失效、重复开始门控。
- `src/auto_fishing/ui/main_window.py`：成功启动后的状态快照前运行锁。
- `try/tests/test_ui_smoke.py`：三项审核回归。

### 错误汇报

- 初版倒计时实现的 207 项自动测试均通过，但独立审核构造出 UI 快照到达前的重复开始窗口和直接 Controller 重绑路径。两项均已以确定性测试固定，不能以“UI 默认禁用”替代 Controller 层门控。

## 2026-07-11 01:58 ~ 2026-07-11 02:00

### 本阶段完成内容

- 已完成复审修复后的 `scripts/build.ps1` 构建门：门内 `210 passed in 16.67s`，PyInstaller 单文件构建退出码为 0。
- 启动/响应/关闭烟雾输出 `SMOKE_OK`，同路径残留进程检查通过。最终发布物大小 62,859,788 字节，SHA256 `1256EE7C2FB15053CB5AEB503DCC2A8FBE346EB18B83168F461E4EE5ACC367E8`。

### 修改文件

- `AGENTS.md`：同步 210 项离线基线。
- `doc/验收标准.md`：同步复审修复后的测试、构建、烟雾和发布物证据。
- 工作区外 `D:\0文件夹\备份\异环自动钓鱼-start-countdown-review-prebuild-20260711-0158\异环自动钓鱼.exe`：保存复审修复前发布物用于回退。

## 2026-07-11 02:00 ~ 2026-07-11 02:02

### 高风险与回退方案

- 最终自查修复了暂停重绑路径，必须再次构建发布物。构建前备份到 `D:\0文件夹\备份\异环自动钓鱼-start-countdown-paused-rebind-prebuild-20260711-0202\`；若构建失败，恢复当前大小 62,859,788 字节、SHA256 `1256EE7C2FB15053CB5AEB503DCC2A8FBE346EB18B83168F461E4EE5ACC367E8` 的发布物。

### 本阶段完成内容

- 最终自查发现审核修复的“运行中拒绝重绑”会误伤暂停轮：暂停时 worker 仍存活，正常路径应为“取消当前轮 → 重新绑定”。新增 `engine.running=True` 的暂停重绑测试先稳定失败，再改为仅在运行且**未暂停**时拒绝重绑。
- 暂停重绑、重复开始、取消旧倒计时和快照前控件锁定聚焦 4 项通过；全套 `210 passed in 16.46s`。

### 新增或修改文件

- `src/auto_fishing/app.py`：保留暂停 worker 的取消后重绑路径。
- `try/tests/test_ui_smoke.py`：暂停且 worker 存活时的重绑回归。

## 2026-07-11 02:02 ~ 2026-07-11 02:06

### 本阶段完成内容

- 已完成暂停重绑修复后的 `scripts/build.ps1` 构建门：门内 `210 passed in 16.57s`，PyInstaller 单文件构建退出码为 0。
- 启动/响应/关闭烟雾输出 `SMOKE_OK`，同路径残留进程检查通过。最终发布物大小 62,861,038 字节，SHA256 `7586D04CDF0A07DF894449C64626BE6BB14B02239FF92AFCCC8A11D83B83CBFC`。

### 修改文件

- `doc/验收标准.md`：同步最终构建门、烟雾和发布物证据。
- `doc/进展记录/2026-7-11.md`：记录暂停重绑修复后的构建与交付。
- 工作区外 `D:\0文件夹\备份\异环自动钓鱼-start-countdown-paused-rebind-prebuild-20260711-0202\异环自动钓鱼.exe`：保存暂停重绑修复前发布物用于回退。

## 2026-07-11 02:06 ~ 2026-07-11 02:23

### 高风险与回退方案

- 即将为前台与继续修复重新构建，会覆盖已忽略的 `build/` 与 `dist/异环自动钓鱼.exe`。构建前将当前发布物备份到 `D:\0文件夹\备份\异环自动钓鱼-manual-foreground-resume-prebuild-20260711-0223\`；构建或烟雾失败时恢复大小 62,861,038 字节、SHA256 `7586D04CDF0A07DF894449C64626BE6BB14B02239FF92AFCCC8A11D83B83CBFC` 的发布物，并回退本阶段代码提交。

### 本阶段完成内容

- 根据“开始没有反应、暂停后继续报错”的反馈，追踪到三条残留强制激活路径：恢复按钮的 `activate()`、核心 `READY` 首帧 `_activate()` 和每轮重新进入 `READY` 时的同一调用。Windows 前台锁会拒绝这些请求，因而即使开始倒计时已手动切回游戏仍会被后续路径安全暂停。
- 修复为全程仅用 `is_foreground()` 验证，不再由自动化调用 `SetForegroundWindow`；开始和继续都展示清晰的 3、2、1 手动切回提示。继续倒计时的控件锁定与开始一致。
- 审核发现 F8 在继续倒计时内仍会留下旧回调，3 秒后可能继续 worker。现已让 F8 使待开始、待继续倒计时的代际失效，并通过主线程回调队列更新 Tk 界面，避免热键线程直接操作 Tk。
- 测试先行：旧恢复激活、核心首帧激活、继续倒计时、开始提示及 F8 取消倒计时均先稳定失败；修复后开发环境全套 `214 passed in 16.73s`。独立审核在 F8 问题修复前给出 Important，修复后复核通过；额外回归确认 F8 热键线程只把取消回调入队，Tk 回调固定在主线程执行。

### 新增或修改文件

- `src/auto_fishing/automation/engine.py`：删除核心与恢复路径的窗口激活依赖，保留前台验证和失焦安全暂停。
- `src/auto_fishing/app.py`：新增继续倒计时、待回调取消与跨线程主界面回调队列；F8 使待开始和待继续代际失效。
- `src/auto_fishing/ui/main_window.py`：显示开始/继续倒计时的切回提示，并在倒计时中锁定暂停按钮。
- `try/tests/test_engine.py`、`try/tests/test_ui_smoke.py`：新增或调整不强制激活、继续倒计时、提示和 F8 取消回归。
- `AGENTS.md`、`doc/验收标准.md`：同步手动前台数据流、214 项开发环境测试和待构建状态。

### 错误汇报

- 初始开始倒计时仅修改启动入口，遗漏了核心 `READY` 首帧和继续路径中的旧激活调用；该遗漏会让 Windows 前台锁继续触发“无法激活”类失败。已删除所有生产自动化激活调用，并以事件序列测试确保恢复后不再调用该路径。

## 2026-07-11 02:23 ~ 2026-07-11 02:33

### 本阶段完成内容

- 独立复审确认 F8 修复后无 Critical/Important：热键线程仅向 `SimpleQueue` 入队，`_drain_snapshots()` 在 Tk 主线程执行取消回调；重绑、关闭和早退状态均未回归。补充子线程触发 F8 且回调在主线程执行的确定性回归。
- 执行 `scripts/build.ps1`：构建门内 `214 passed in 16.61s`，总耗时 50.2 秒，单文件构建退出码 0。
- 执行 `try/smoke_exe.ps1`：输出 `SMOKE_OK`，耗时 8.6 秒。将工作树发布物同步到 `D:\1Folder\异环自动钓鱼\dist\异环自动钓鱼.exe`，两份文件大小均为 62,861,075 字节，SHA256 均为 `27024B3FCD775E8B6818893AC4D02FFDA960AAB7B23A87E73D8E60A46A97050D`。

### 修改文件

- `AGENTS.md`、`doc/验收标准.md`：更新 214 项构建门、烟雾、交付路径与哈希证据。
- `doc/进展记录/2026-7-11.md`：记录复审、备份、构建、烟雾和交付结果。
- 工作区外 `D:\0文件夹\备份\异环自动钓鱼-manual-foreground-resume-prebuild-20260711-0223\异环自动钓鱼.exe`：保留覆盖构建前发布物用于回退。

## 2026-07-11 02:33 ~ 2026-07-11 02:36

### 本阶段完成内容

- 按用户授权将已验证的任务分支推送至私有 GitHub 仓库 `https://github.com/q2955161835-debug/yihuan-auto-fish`，远程跟踪分支为 `origin/root/feature-auto-fishing-impl`，推送提交为 `5d994a6`。
- 远程仓库此前为空、没有默认分支，因此不存在可作为草稿 PR 基线的目标分支；为遵守真实游戏验收前不合并主分支的规则，本次仅推送任务分支，未创建 PR。

### 修改文件

- `AGENTS.md`：记录 GitHub 私有仓库地址、当前远程分支和合并门槛。
- `doc/进展记录/2026-7-11.md`：记录用户授权的推送结果及未创建 PR 的原因。

## 2026-07-11 02:36 ~ 2026-07-11 02:43

### 本阶段完成内容

- 用户明确授权“合并后完整推送，以当前为基线”。已将 `root/feature-auto-fishing-impl` 快进合并到本地 `main`，合并结果以 `PYTHONPATH=src` 复验 `214 passed in 17.19s`，差异检查无输出。
- 已将 `main` 推送至私有仓库并设为远程默认分支；远程当前仅保留 `main`，提交为 `866ef4a`。已移除 `.worktrees/auto-fishing`、本地 `root/feature-auto-fishing-impl` 和本地 `root/feature-auto-fishing`，并删除远程任务分支。
- 真实游戏验收尚未执行；本次是用户明确指定的代码基线整合，不代表实机发布验收完成。

### 修改文件

- `AGENTS.md`、`doc/验收标准.md`：同步用户授权的 `main` 基线、私有仓库默认分支及未完成的实机验收边界。
- `doc/进展记录/2026-7-11.md`：记录合并、复验、远程默认分支与任务分支清理结果。

## 2026-07-11 18:52 ~ 2026-07-11 18:58

### 本阶段完成内容

- 在隔离分支按测试先行新增完整运行记录存储：每次启动独立目录、UTF-8 逐行结构化事件、单一异步写入线程和最长边 480 像素、JPEG 质量 50 的逐帧缩略图。
- 新增最近 30 个运行目录保留策略和根目录边界保护；写入队列固定上限由调用方通过 `RuntimeLogError` 检查，队列满不会静默丢弃记录。
- 先运行目标测试，确认因新模块不存在而正确失败；实现后运行 `py -3.13 -m pytest try/tests/test_storage.py -q`，结果为 `14 passed in 1.18s`。

### 新增或修改文件

- `src/auto_fishing/storage/runtime_logging.py`：完整运行记录器、异步写入、缩略图、保留清理和错误传播。
- `src/auto_fishing/storage/__init__.py`：导出运行记录器接口。
- `try/tests/test_storage.py`：覆盖事件、JPEG 缩略图、保留清理、目录边界和队列满。
- `doc/验收标准.md`：新增运行记录存储阶段验收项。
- `doc/进展记录/2026-7-11.md`：记录本阶段测试先行与验证结果。

### 错误汇报

- 无生产异常。队列满测试初稿若只写入两项不能填满队列：首项已被受阻塞写入线程取走，第二项仍可入队；已以第三项入队稳定复现“日志队列已满”。

## 2026-07-11 18:58 ~ 2026-07-11 19:01

### 本阶段完成内容

- 为 `SafeInput`（安全输入封装）记录 F、A、D 与鼠标的业务请求；为 Windows 输入后端记录鼠标定位结果以及 `SendInput`（系统输入接口）的请求数、实际发送数和失败错误码。
- 成功发送不读取或记录 Windows 错误码，避免把旧错误误认为当前输入异常；部分发送先记录结果，再保持原有 `InputFailure` 异常文本。
- 先运行新增成功发送测试，确认构造器尚不支持记录器而正确失败；实现并补充鼠标回归后运行 `py -3.13 -m pytest try/tests/test_safe_input.py -q`，结果为 `19 passed in 0.20s`。

### 新增或修改文件

- `src/auto_fishing/platform/input.py`：可选记录器、键鼠请求、光标定位和系统输入结果事件。
- `try/tests/test_safe_input.py`：覆盖成功发送无陈旧错误码、部分发送错误码、F/A/D 请求、鼠标点击清理与光标定位记录。
- `doc/验收标准.md`：补充输入记录验收证据。
- `doc/进展记录/2026-7-11.md`：记录测试先行与阶段验证结果。

### 错误汇报

- 无生产异常。补充光标与鼠标请求测试时先移除未测试的记录分支，分别确认测试因预期事件缺失而失败，再以最小实现恢复，避免仅凭实现后的通过结果误判覆盖有效性。

## 2026-07-11 19:01 ~ 2026-07-11 19:06

### 本阶段完成内容

- 应用启动时创建并启动完整运行记录器，向输入后端、安全输入封装和自动化引擎注入同一实例；关闭顺序为停止热键、关闭引擎、释放输入、关闭记录器。
- 引擎对每个已处理客户端帧记录观测、处理前状态、处理后快照、帧时间和客户端截图；状态变化、绑定、开始、继续、暂停与截屏生命周期也记录结构化事件。
- 记录器报告故障时，工作线程以 `E_LOGGING` 暂停并释放输入；运行记录初始化失败时界面锁定开始但仍可退出。
- 新增测试先因引擎与服务构造器不接受运行记录器、以及初始化失败未锁定开始而正确失败；实现后运行 `py -3.13 -m pytest try/tests/test_engine.py try/tests/test_ui_smoke.py -q`，结果为 `98 passed in 16.35s`。

### 新增或修改文件

- `src/auto_fishing/automation/engine.py`：逐帧遥测、运行状态和 `E_LOGGING` 安全暂停接入。
- `src/auto_fishing/app.py`：运行记录器构造、启动、依赖注入、初始化失败锁定与关闭。
- `try/tests/test_engine.py`：覆盖正常逐帧记录和日志失败释放输入。
- `try/tests/test_ui_smoke.py`：覆盖应用记录器生命周期、初始化失败锁定和共享注入。
- `AGENTS.md`：同步长期架构、数据流和本地运行记录约束。
- `doc/验收标准.md`、`doc/进展记录/2026-7-11.md`：同步自动验证与真实游戏检查步骤。

### 错误汇报

- 无生产异常。运行记录初始化失败分支在首次实现时尚无直接回归，已先移除锁定分支并让测试稳定复现缺失提示，再以最小实现恢复，固定“不可写时不能开始自动化”的安全边界。

## 2026-07-11 19:06 ~ 2026-07-11 19:09

### 高风险与回退方案

- 即将执行单文件构建，会覆盖隔离工作树的已忽略 `build/` 与 `dist/异环自动钓鱼.exe`。隔离工作树当前没有旧发布物，因此构建前备份主工作区 `D:\1Folder\异环自动钓鱼\dist\异环自动钓鱼.exe` 到 `D:\0文件夹\备份\异环自动钓鱼-runtime-logging-prebuild-20260711-1909\`；若构建或烟雾失败，可保留主工作区发布物并回退本功能分支提交。

### 本阶段完成内容

- 运行 `py -3.13 -m pytest try/tests -q`，结果为 `228 passed in 17.92s`，全量回归通过。

### 新增或修改文件

- `doc/进展记录/2026-7-11.md`：记录全量测试、高风险构建范围与回退方案。

## 2026-07-11 19:09 ~ 2026-07-11 19:11

### 本阶段完成内容

- 已将主工作区旧发布物备份到工作区外 `D:\0文件夹\备份\异环自动钓鱼-runtime-logging-prebuild-20260711-1909\异环自动钓鱼.exe`，大小 62,861,075 字节、SHA256 `27024B3FCD775E8B6818893AC4D02FFDA960AAB7B23A87E73D8E60A46A97050D`。
- 执行 `scripts/build.ps1 -PythonPath C:\Users\29551\AppData\Local\Programs\Python\Python313\python.exe`：构建门内 `228 passed in 18.39s`，总耗时 69.8 秒，PyInstaller 单文件构建成功。
- 执行 `try/smoke_exe.ps1`，输出 `SMOKE_OK`。隔离工作树发布物大小 63,919,181 字节，SHA256 `762CBC69DB7949BB94C33A94A3A0B05C8A4897AEB39103E46C1A5DDDF961EAFD`。

### 新增或修改文件

- `AGENTS.md`、`doc/验收标准.md`：更新完整运行记录分支的离线验证、构建、烟雾、发布物路径和真实游戏验收边界。
- `doc/进展记录/2026-7-11.md`：记录备份、构建、烟雾与哈希证据。
- 工作区外 `D:\0文件夹\备份\异环自动钓鱼-runtime-logging-prebuild-20260711-1909\异环自动钓鱼.exe`：保存构建前主工作区发布物，用于回退。

### 错误汇报

- 隔离工作树没有 `dist/异环自动钓鱼.exe` 旧产物，无法按工作树路径备份；已确认主工作区存在已验证发布物并备份该文件，随后隔离构建和烟雾均通过。

## 2026-07-11 20:13 ~ 2026-07-11 20:29

### 本阶段完成内容

- 在真实《异环》前台执行一次完整运行记录验收，运行目录为 `%LOCALAPPDATA%\异环自动钓鱼\runs\run-20260711T121316419566Z`；共记录 671 个已处理帧及对应低分辨率截图，确认截取的是正确游戏客户区。
- 日志显示程序在 20:13:27 请求轻按 F，按下与抬起两次 `SendInput`（系统输入接口）均为 `requested=1, sent=1`；状态从 `READY`（就绪）进入 `WAIT_BITE`（等待上钩），但游戏画面没有响应。随后使用电脑控制插件发送 F 和点击画面右下角钓鱼图标也没有响应，而用户实际按下实体键盘 F 后立即成功进入钓鱼，确认问题不在按键调用失败，而在游戏未接受 Windows 合成输入。
- 检索 GitHub（代码托管平台）现有方案：PyDirectInput（直接输入封装）及其派生实现仍基于 `SendInput` 和扫描码；`PostMessage`（窗口消息）没有可用于原始输入游戏的成功证据；Interception（键盘过滤驱动）与 ViGEm（虚拟手柄驱动）需要安装驱动；MyInput（输入模拟项目）依赖进程注入与输入接口钩子；Arduino/Raspberry Pi（微控制器）USB HID（人机接口设备）属于额外实体硬件。未找到《异环》专用且同时满足“不提权、不装驱动、不注入、不规避反作弊”边界的实现。
- 本阶段仅完成实机诊断和只读检索，没有修改程序代码；完整运行记录分支继续保留且不合并 `main`，等待用户决定是否保持现有架构边界并改为半自动辅助。

### 新增或修改文件

- `doc/进展记录/2026-7-11.md`：记录真实游戏输入证据、GitHub 检索结论与架构边界冲突。
- `doc/验收标准.md`：把真实游戏输入和完整运行记录验收从“未执行”更新为实际失败结果。

### 错误汇报

- 自动输入失败的直接表现：Windows 报告两次键盘事件均已成功送入系统输入流，但游戏不执行动作；相同画面下实体键盘 F 成功。根因判断为游戏对 Windows 合成输入和实体输入采用了不同处理或过滤，继续改虚拟键码、扫描码或按键时长没有证据能解决。
- 视觉识别另有独立问题：671 帧中 `result_candidate`（结算候选）全部为真，`result`（结算确认）有 669 帧为真，说明夜间场景被结算识别器大面积误判。该问题尚未修改，必须在输入方案确定后使用本次真实截图增加回放测试并单独修复。
- 20:13:51 游戏失去前台后程序按设计以 `E_WINDOW`（窗口错误）暂停；此项不是输入失败根因。

## 2026-07-11 20:33 ~ 2026-07-11 20:48

### 本阶段完成内容

- 用户在真实游戏前台用实体鼠标点击 Windows 屏幕键盘 F，屏幕键盘按键出现按下高亮，游戏成功进入钓鱼控制画面；该截图证据推翻了“所有 Windows 虚拟键盘路径均被游戏拒绝”的宽泛判断，证明系统屏幕键盘生成的按键可被游戏接受。
- 使用电脑控制插件尝试点击屏幕键盘时，插件因屏幕键盘属于不可激活窗口而拒绝直接点击；改以游戏为目标点击屏幕键盘覆盖坐标时，又因命中窗口不是游戏而被安全检查拒绝。两次操作均未真正点击屏幕键盘，不能作为屏幕键盘失败证据。
- 从后台命令会话调用项目现有 `SetCursorPos`（设置鼠标位置）接口被 Windows 以返回值 0 拒绝，且错误码仍为 0；向 `OSKMainClass`（屏幕键盘主窗口类别）直接发送鼠标按下和抬起窗口消息没有退出钓鱼。由此确认后台窗口消息不能替代用户已验证的可见鼠标点击，正式方案需要在交互桌面进程中移动并点击/按住屏幕键盘按键。
- 本阶段没有修改生产代码。下一阶段若采用屏幕键盘适配器，必须先验证 F 轻点、A/D 按住与换向释放，并解决屏幕键盘覆盖游戏画面导致截图识别区域受污染的问题。

### 新增或修改文件

- `doc/进展记录/2026-7-11.md`：记录系统屏幕键盘成功证据、自动测试工具限制和剩余技术风险。
- `doc/验收标准.md`：将屏幕键盘人工点击列为已通过的独立输入路径，同时保持自动化路径为未通过。

### 错误汇报

- 电脑控制插件提示屏幕键盘无法激活，或坐标命中窗口与指定目标窗口不一致；这是插件的窗口安全约束，不是游戏或屏幕键盘错误。
- 后台命令会话无法通过 `SetCursorPos`（设置鼠标位置）访问交互桌面，不能用该会话验证正式桌面程序的鼠标点击能力。
- 直接发送 `WM_LBUTTONDOWN/WM_LBUTTONUP`（鼠标按下/抬起窗口消息）未触发屏幕键盘按键，不能采用无可见点击的后台消息方案。
