# 2026-07-14 进展记录

## 2026-07-14 14:42 ~ 2026-07-14 14:44

### 本阶段完成内容

- 用户使用加权中心控制发布物完成真实游戏验收，评价“非常完美”，并选择本地合并 `main`。
- 合并前在功能工作树重新执行全量测试，340 项通过，耗时 30.18 秒；功能分支与主工作区均无未提交变更。
- 根目录当前没有同名可执行文件；新增 `/异环自动钓鱼.exe` 精确忽略项，使验收通过的发布物可放在项目根目录直接运行，同时保持 Git 工作区干净。

### 新增或修改文件

- `.gitignore`：忽略项目根目录的验收发布物，不扩大到其他可执行文件。
- `AGENTS.md`、`doc/验收标准.md`：记录真实游戏验收通过、根目录发布物位置和合并授权。
- `doc/进展记录/2026-7-14.md`：记录本次验收、合并前证据和回退方案。

### 高风险与回退方案

- 下一步将功能分支合并到本地 `main`，把 SHA256 为 `A360CE23D2F75E0E924C222765AA8434C995505A1379160AF494BD2FC5C0FD7F` 的发布物复制为项目根目录 `异环自动钓鱼.exe`，通过合并后全量测试再移除 `.worktrees/codex-feat-runtime-logging` 并删除功能分支。若合并测试失败，保留功能工作树并回退合并提交；若复制哈希不一致，删除根目录副本并保留工作树原发布物。不得在测试通过、哈希一致前清理工作树或分支。

### 数据位置检查范围

- 合并完成后只读核对代码配置与实际目录，列出运行日志、低分辨率逐帧截图、异常诊断、配置文件和清理策略；不迁移、不删除用户运行数据。若发现数据写入 C 盘，按项目存储规则明确报告冲突，未经用户授权不改动存储架构。

## 2026-07-14 14:44 ~ 2026-07-14 14:48

### 合并与发布物结果

- `origin/main` 已是最新；功能分支以合并提交 `82f319e` 合入本地 `main`，无冲突。合并后的全量测试 340 项通过，耗时 31.09 秒。
- 验收通过的发布物已复制到项目根目录 `D:\1Folder\异环自动钓鱼\异环自动钓鱼.exe`，大小 63,950,306 字节；源与副本 SHA256 均为 `A360CE23D2F75E0E924C222765AA8434C995505A1379160AF494BD2FC5C0FD7F`。根目录精确忽略规则生效，不污染 Git 工作区。
- 最终提交状态再次执行 340 项测试并通过，耗时 30.98 秒；`compileall`、`git diff --check` 和根目录发布清单校验通过。确认功能工作树干净且位于项目 `.worktrees` 后，从主工作区移除 `codex-feat-runtime-logging` 工作树并删除已合并分支；根目录发布物保持不变。
- 本地 `main` 尚未推送 GitHub；按项目规则在会话结果中询问用户是否推送。

### 存储位置只读检查

- 当前生产代码固定使用 `C:\Users\29551\AppData\Local\异环自动钓鱼\`，与最新“禁止存储在 C 盘”规则冲突。本次只读检查，没有迁移、删除或改写任何用户运行数据；迁移会改变既有存储架构，需另行确认。
- 完整运行记录位于 `runs\run-*\`：每次启动生成 `events.jsonl` 和 `frames\*.jpg`，截图最长边 480 像素、JPEG 质量 50，自动只保留最近 30 个运行目录。当前 30 个目录共 23,387 个文件、约 359.40 MiB。
- 最新真实验收运行位于 `runs\run-20260714T063349190418Z\`：11,191 张帧截图、19,122 条事件、4,595 条 `progress.control` 控制事件，共约 154.84 MiB；结尾记录 `application.cleanup_finished` 且 `error_count=0`。
- 异常诊断位于 `diagnostics\`：原始 PNG、JSON 元数据和可选进度联系图，按 7 天与最多 20 组清理。当前 44 个文件、约 67.17 MiB。
- 当前设置文件为 `config.json`，大小 97 字节；同目录 `settings.json` 是 2026-07-10 遗留文件，现有生产代码不再读取。整个数据根目录当前约 426.58 MiB。

### 错误汇报

- 数据位置问题：运行日志、逐帧截图、诊断和配置全部位于 C 盘，违反最新默认存储规则。原因是应用装配仍以 `%LOCALAPPDATA%`（本地应用数据）为固定根目录，且运行日志保存每个处理帧，单次长运行可达约 154.84 MiB。建议后续单独设计迁移到 `D:\29551\异环自动钓鱼数据\`，同时处理旧数据迁移、跨盘失败回退和配置兼容；本阶段不自行扩大范围修改。

## 2026-07-14 14:49 ~ 2026-07-14 15:25

### 本阶段完成内容

- 按用户确认的设计和实施计划，在 `codex/feat-d-drive-storage-cap` 隔离工作树以测试先行实现整个数据根目录的 104,857,600 字节硬上限。
- 新增共享 `StorageQuotaManager`：启动全量扫描一次，正常写入只登记字节增量；超限按最旧已结束运行、最旧诊断组、活动运行最旧 JPEG、事件最旧完整行的顺序清理。配置、最新完整事件和未知普通文件保持不可自动删除。
- `SettingsStore`、`DiagnosticsStore`、`RuntimeLogStore` 接入同一配额实例；应用默认路径改为 `D:\29551\异环自动钓鱼数据\`，不再读取 `%LOCALAPPDATA%`。
- 测试先确认配额模块缺失、三个构造函数不接受 `quota`、默认 D 盘常量缺失等预期失败，再完成最小实现。存储与引擎相关 116 项、界面装配与存储 82 项通过；最终全套 351 项通过，耗时 30.41 秒，`compileall` 与 `git diff --check` 通过。

### 新增或修改文件

- `src/auto_fishing/storage/quota.py`：整个数据根目录的线程安全容量账本、清理顺序、安全边界和错误类型。
- `src/auto_fishing/storage/settings.py`、`diagnostics.py`、`runtime_logging.py`：写入后向共享配额登记大小变化。
- `src/auto_fishing/app.py`：固定 D 盘生产数据根目录并装配共享配额。
- `try/tests/test_storage.py`、`try/tests/test_ui_smoke.py`：覆盖删除优先级、原子诊断组、最近帧/事件保留、未知文件、路径越界、三类写入者和应用装配。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-14.md`：同步长期规则、可验证验收项和本阶段证据。

### 错误汇报

- 异步日志夹具最初使用 2,500 字节预算，实测每条事件约 413～417 字节、每张测试 JPEG 约 946～947 字节，8 条事件加任一帧客观上无法同时保留，因此所有帧被正确裁掉。根因是测试预算错误，不是配额顺序错误；预算校正为 5,000 字节并增加写线程错误检查后通过。
- pytest 的 `tmp_path` 按系统默认临时目录在 `C:\Users\29551\AppData\Local\Temp\pytest-of-29551\` 生成和清理测试数据；这是测试框架临时写入，不是生产数据。生产默认路径已改为 D 盘，本阶段尚未修改或删除旧 C 盘生产目录。

### 高风险迁移与回退方案

- 下一阶段将读取旧目录 `C:\Users\29551\AppData\Local\异环自动钓鱼\`（只读源，现状 447,296,534 字节），先把完整数据和根目录旧 EXE 备份到 `D:\0文件夹\备份\异环自动钓鱼-data-migration-YYYYMMDD-HHmm\`，并对 C→备份、C→D 的每个文件比较相对路径、长度和 SHA256；同类备份最多保留最新两份。
- 只有备份清单、D 盘复制清单、D 盘 `config.json`、100 MiB 配额裁剪、新 EXE 构建/清单/烟雾、合并后测试和发布物哈希全部通过，才允许精确删除旧 C 盘目录。任一环节失败立即停止，保留旧 C 盘数据和当前根目录 EXE。
- 回退时从最新迁移备份的 `data\` 恢复旧数据，从 `pre-migration.exe` 恢复根目录发布物；删除操作只允许对已解析并核对的精确绝对路径执行，不使用通配符或跨 Shell 拼接路径。

## 2026-07-14 15:25 ~ 2026-07-14 16:00

### 本阶段完成内容

- 确认程序未运行、D 盘目标不存在且空间充足后，为旧 C 盘 23,433 个文件、447,296,534 字节数据和根目录旧 EXE 创建完整备份。C→备份、C→D、最终备份→D 三次逐文件相对路径/长度/SHA256 比较均为零差异。
- 用优化后的生产配额把 D 盘副本裁剪为 7,159 个文件、104,853,232 字节；保留 `config.json`、真实运行 `run-20260714T063349190418Z` 的 19,122 条完整事件和最近 7,156 张低分辨率帧。
- 使用 D 盘英文临时目录和 Python 3.13.5 构建单文件发布物；构建门 352 项通过，管理员清单校验通过。新 EXE 大小 63,957,255 字节，SHA256 为 `9648B67036CEE60DDF5753293D1B24D359528A776F84F5EDC473B8DB4F695906`；直接烟雾输出 `SMOKE_OK`。
- 配额性能和烟雾关闭回归加入后，最终全套 353 项通过，耗时 29.99 秒；`compileall`、`git diff --check` 和 PowerShell UTF-8 BOM 检查通过。

### 新增或修改文件

- `src/auto_fishing/storage/quota.py`：清理过程由逐文件全树重扫改为单次根扫描和删除字节精确扣减。
- `try/tests/test_storage.py`：新增大量帧裁剪只扫描一次根目录的回归测试。
- `try/smoke_exe.ps1`、`try/tests/test_packaging.py`：强制结束后按条件等待所属进程树消失，并验证该顺序。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-14.md`：记录迁移、构建、烟雾和错误证据。
- 工作区外新增 `D:\0文件夹\备份\异环自动钓鱼-data-migration-20260714-152811\`：完整旧数据、`source-manifest.json` 和 `pre-migration.exe`。
- 工作区外新增 `D:\29551\异环自动钓鱼数据\`：最终生产数据目录；`D:\29551\auto-fishing-build-temp\` 为待最终清理的构建临时目录。

### 错误汇报

- 首次裁剪从 447,296,534 字节降到约 161 MiB 后进展极慢。根因是每删除一个候选都重新遍历约 1.1 万个文件，回归夹具实测 33 次根扫描。精确终止仅作用于本次 D 副本裁剪的 Python 子进程，从完整备份恢复并再次校验；实现改为初始化只扫描一次、删除后扣减已知大小，最终裁剪耗时 12.491 秒。
- 首次两次烟雾通过低权限 Windows PowerShell 子进程启动管理员发布物，关闭时 `Stop-Process` 明确返回错误 5，脚本误报所属两个进程残留。精确终止每次烟雾拥有的 PID 树后，按计划在当前 PowerShell 会话直接执行通过；脚本另将强制结束后的固定 250 ms 等待改为最长 5 秒条件轮询。
- 烟雾会创建生产运行目录并按配额淘汰旧运行，因此烟雾通过后从完整备份最后恢复 D 盘副本、全量哈希校验并重新裁剪；最终目录只保留真实验收运行，不含烟雾日志。

### 回退状态

- 旧 C 盘数据和当前根目录旧 EXE 尚未删除或替换；完整回退备份已验证。下一阶段只有合并后全量测试、新旧发布物哈希复制校验和 D 盘最终复核均通过，才删除旧 C 盘源。

## 2026-07-14 16:00 ~ 2026-07-14 16:04

### 本阶段完成内容

- 功能分支最新 353 项测试通过，耗时 30.41 秒；`compileall`、`git diff --check` 和工作树干净状态通过。
- 本地 `main` 与 `origin/main` 检查为无需拉取新提交；以合并提交纳入 `codex/feat-d-drive-storage-cap`，合并后 353 项测试通过，耗时 31.22 秒。
- 从工作树 `dist` 原子替换根目录 `D:\1Folder\异环自动钓鱼\异环自动钓鱼.exe`；源、暂存副本和最终文件 SHA256 均为 `9648B67036CEE60DDF5753293D1B24D359528A776F84F5EDC473B8DB4F695906`，大小 63,957,255 字节，最终清单为 `requireAdministrator uiAccess=false`。
- 再次确认 D 盘配置、7,159 个文件、104,853,232 字节和完整回退备份存在后，精确删除旧目录 `C:\Users\29551\AppData\Local\异环自动钓鱼\`，复核路径不存在。

### 修改文件与工作区外行为

- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-14.md`：更新本地最终发布状态、根目录发布物和旧 C 盘删除证据。
- 工作区外删除 `C:\Users\29551\AppData\Local\异环自动钓鱼\`：仅在三份全量清单、D 盘容量、配置、备份和新发布物全部验证后执行。
- 回退材料保持在 `D:\0文件夹\备份\异环自动钓鱼-data-migration-20260714-152811\`，包含完整 `data\`、源清单和旧 EXE。

### 错误汇报与回退

- 本阶段合并、发布物替换和旧数据删除均未出现新错误。若需回退，先停止程序，再从备份恢复旧 EXE 和数据；代码可通过 Git 合并提交撤销。

## 2026-07-14 16:04 ~ 2026-07-14 16:08

### 本阶段完成内容

- 最终验证重新执行 353 项测试并通过，耗时 30.85 秒；`compileall`、`git diff --check`、根目录管理员清单、D 盘容量、配置、备份清单、旧 C 路径和根目录 EXE 哈希审计通过。
- 确认功能工作树干净且严格位于项目 `.worktrees` 后移除 `codex-feat-d-drive-storage-cap` 工作树，执行 `git worktree prune` 并删除已合并功能分支；主仓库只保留 `main`。
- `main` 推送到私有仓库 `https://github.com/q2955161835-debug/yihuan-auto-fish`，本地与远端最初同步到 `97b9f79f0a9d435046b0104788de80f930775d9e`。
- 精确清理工作区外构建临时目录 `D:\29551\auto-fishing-build-temp\`；同类迁移备份仅 1 份，符合最多 2 份规则。

### 错误汇报

- 第一次 `git push origin main` 因 Windows `schannel` 未收到 TLS 握手失败。随后 `gh api rate_limit` 和 `git ls-remote` 均成功，证明认证、远端地址和网络已恢复；未修改证书或安全配置，原命令重试成功。该错误属于瞬时网络连接，不影响本地提交、数据或发布物。

### 最终回退位置

- 数据和旧 EXE 回退：`D:\0文件夹\备份\异环自动钓鱼-data-migration-20260714-152811\`。
- 代码回退：Git `main` 上的合并与最终文档提交；远端私有仓库已同步。

## 2026-07-14 16:20 ~ 2026-07-14 16:44

### 本阶段完成内容

- 检查最新真实运行 `D:\29551\异环自动钓鱼数据\runs\run-20260714T081942331895Z\events.jsonl`：事件创建时间从 `16:20:15.893` 跳到 `16:20:47.476`，后台写入停顿约 31.58 秒后以 `E_LOGGING/日志队列已满` 暂停；同期数据目录从接近 100 MiB 降至约 6.25 MB，只剩当前运行，证明轻微超限触发了整份旧运行递归删除。
- 以测试先行新增“旧运行含 20 张截图且仅超限 5 字节”的回归夹具；旧实现按预期删除整个旧运行而失败。实现改为只删除恢复容量所需的最旧截图，回归测试确认仅删除 1 张、保留其余 19 张及事件文件。
- 队列背压不再登记永久日志故障：后台短暂跟不上时丢弃来不及保存的日志项，恢复后写入 `logging.backpressure` 及 `dropped_items`；JPEG 编码、磁盘写入或配额无法恢复等真实错误仍通过 `E_LOGGING` 暂停并释放输入。
- 存储专项 33 项、存储与引擎 118 项通过；候选构建门全套 354 项通过，耗时 32.19 秒。候选 EXE 大小 63,957,832 字节，SHA256 为 `DED6B53147058B37A604F6FD00B495E8914663AF99BC9DF42579298E9A1B2D56`，管理员清单验证通过，直接烟雾输出 `SMOKE_OK`。
- 当前根目录旧 EXE 已备份到 `D:\0文件夹\备份\异环自动钓鱼-incremental-log-pruning-20260714-163733\异环自动钓鱼.exe`，备份与旧文件哈希均为 `9648B67036CEE60DDF5753293D1B24D359528A776F84F5EDC473B8DB4F695906`。候选经同卷暂存哈希校验后覆盖移动到根目录，最终大小 63,957,832 字节、哈希 `DED6B53147058B37A604F6FD00B495E8914663AF99BC9DF42579298E9A1B2D56`，管理员清单复核一致；同类备份现为 1 份。
- 任务分支以合并提交 `f7dce75` 纳入本地 `main`，合并后全套 354 项再次通过，耗时 32.28 秒；隔离工作树与已合并任务分支已安全删除，主仓库只保留 `main`。最新提交是否推送私有 GitHub 等待用户确认。

### 新增或修改文件

- `src/auto_fishing/storage/quota.py`：旧运行由整目录递归删除改为按需增量删除最旧截图，并只清理已空目录。
- `src/auto_fishing/storage/runtime_logging.py`：队列背压降级和恢复后的丢弃数量事件。
- `try/tests/test_storage.py`：轻微超限的有界清理回归与队列背压不暂停测试。
- `try/tests/test_engine.py`：继续验证真实磁盘写入故障会以 `E_LOGGING` 暂停。
- `docs/superpowers/plans/2026-07-14-incremental-log-pruning.md`：本次测试先行实施计划。
- 工作区外生产数据目录新增烟雾运行 `D:\29551\异环自动钓鱼数据\runs\run-20260714T083551730733Z\`；烟雾后整个数据目录为 209 个文件、6,251,895 字节，远低于 100 MiB 上限。
- PyInstaller 构建期间按工具默认行为临时使用 `C:\Users\29551\AppData\Local\pyinstaller\` 缓存；构建完成后已确认该路径不存在，没有保留 C 盘构建数据。

### 错误汇报

- 根因：`StorageQuotaManager._enforce()` 在只需释放一张截图空间时仍对最旧已结束运行执行 `shutil.rmtree()`，Windows 删除数千个小文件耗时约 31.58 秒；日志生产端继续以约 30 FPS 入队，最终填满 300 项队列并触发主循环暂停。
- 构建首次被工具设置的 1 秒超时主动中断，pytest 随输出管道关闭报 `OSError 22`；未修改代码，使用正常长超时重跑后 354 项通过并成功构建。
- 首次尝试使用 `.NET File.Replace` 原子替换时，PowerShell 把空备份参数转换为空路径，接口在修改目标前报“路径为空”；哈希门确认旧根目录 EXE 未变化、候选暂存和独立备份完整。随后改用同卷 `.NET File.Move(..., overwrite=true)` 覆盖移动，根目录哈希和管理员清单验证通过。
- 解决方案：旧运行截图逐项按需删除，使单次轻微超限的清理工作量有界；队列满仅作为可恢复背压，不再等同磁盘损坏，同时在恢复后保存丢弃数量，保留排查能力。

### 高风险发布与回退方案

- 根目录当前已验收 EXE 的 SHA256 为 `9648B67036CEE60DDF5753293D1B24D359528A776F84F5EDC473B8DB4F695906`；候选哈希为 `DED6B53147058B37A604F6FD00B495E8914663AF99BC9DF42579298E9A1B2D56`。
- 替换前把当前根目录 EXE 备份到 `D:\0文件夹\备份\异环自动钓鱼-incremental-log-pruning-YYYYMMDD-HHmm\`；同类最多保留 2 份。仅在候选清单、烟雾和哈希均通过后执行原子替换。
- 替换后若根目录哈希、管理员清单、全量测试或合并后验证失败，立即从该备份恢复旧 EXE；代码回退到变更前检查点 `51ac93e`。生产数据不删除、不迁移。

## 2026-07-14 16:55 ~ 2026-07-14 17:06

### 本阶段完成内容

- 只读检查最新两次真实运行 `run-20260714T085533116547Z` 与 `run-20260714T085628804414Z` 的事件、低分辨率帧和异常诊断；后一运行最终原始诊断画面明确显示“鱼儿溜走了”，暂停原因为连续 60 帧 `no_consensus` 后的 `E_PROGRESS_LOST`。
- 排除日志与输入故障：两次运行均无 `logging.backpressure`；A/D 合计 46 次按下/释放，所有 `sendinput.result` 请求数与发送数一致，屏幕键盘鼠标按下/抬起均为成功。正常控制阶段的短按方向响应正确，键位映射不存在整体颠倒。
- 重建控制阶段时间线：最新运行第一阶段第 193～373 帧稳定，第二阶段前连续 322 帧无进度观测；第二阶段首个黄标位置 65.5% 与上一阶段末尾 65.2% 几乎相同。随后第 706→707、733→734、750→751 帧分别出现 12.4%、11.0%、34.6% 的黄标单帧跳变，而绿区中心只变化约 1.1%、0.9%、0%。
- 代码核对确认 `ProgressRecognizer` 在无观测和跨阶段时不清空 `_history`，黄色候选始终优先选择最接近上一历史位置、无历史时最接近中心的一条；现有跳变确认只检查绿区中心超过 20%，未检查黄标跳变。因此极端初始黄标会暴露陈旧历史与背景浅黄竖线候选切换问题，并把错误位置直接交给控制器。

### 修改文件与临时检查

- `doc/进展记录/2026-7-14.md`：记录本次只读故障证据、排除项和根因结论。
- 临时在 `try/extreme-top-contact.jpg` 生成 8 帧顶部槽带放大联系图用于肉眼核对，检查后已精确删除，工作区无该临时文件。
- 工作区外仅只读访问 `D:\29551\异环自动钓鱼数据\runs\` 与 `diagnostics\`；未修改生产数据、发布物、识别代码或控制参数。

### 错误汇报

- 直接用 `cv2.imread` 读取中文数据路径失败，未生成输出文件；按项目既有 Unicode 路径规则改用 `numpy.fromfile + cv2.imdecode` 后完成临时联系图检查。
- 根因不是极端位置本身，而是黄色候选跟踪策略：跨阶段陈旧历史未重置，初始候选存在中心偏置，且黄标单帧大跳变无确认门。绿色跳变门和控制器最多 15 帧加权窗口均无法拦截这一类“绿区稳定、黄标换候选”的错误。
- 本阶段仅完成诊断，没有实施修复。推荐后续以失败回放测试先覆盖“跨阶段极端初始黄标”和“绿区稳定但黄标大跳变”，再最小化加入阶段重置与黄标跳变确认；不得重新引入黄标必须接近绿区的约束。

## 2026-07-14 17:08 ~ 2026-07-14 17:33

### 本阶段完成内容

- 在 `codex/fix-extreme-marker-tracking` 独立工作树执行测试驱动修复；变更前全套 354 项通过。新增左右极端首帧、黄标单帧跳变和第二次 F 阶段重置测试，旧实现分别因缺少重置接口、直接接受跳变和未建立阶段边界而按预期失败。
- `ProgressRecognizer` 新增显式重置；第二次 F 成功并进入等待进度条后立即清空上一阶段绿区、黄标和待确认跳变历史。新阶段首个可靠观测不受旧位置或画面中心限制。
- 同一阶段黄标相对最近可靠帧跳变超过顶部槽宽 8% 时返回 `jump_pending` 并释放 A/D；下一帧位置一致才接受。阈值依据最新日志中 830 个非异常相邻样本最大约 7.81% 与故障跳变 11.0%～34.6% 的分界确定；未增加黄标/绿区相对位置约束、速度预测或第二控制状态机。
- 识别与核心专项 124 项通过；全套 358 项通过，耗时 33.13 秒。构建门再次通过 358 项，耗时 32.74 秒；候选大小 63,955,648 字节，SHA256 `47BD572C85D2C344343BDC58B9B5FCF5003946FB135AC95CFB6488D05650E035`，清单为 `requireAdministrator uiAccess=false`，直接烟雾输出 `SMOKE_OK`。

### 新增或修改文件

- `src/auto_fishing/vision/progress.py`：识别历史重置和 8% 黄标跳变二帧确认。
- `src/auto_fishing/vision/scenes.py`：公开进度识别重置边界。
- `src/auto_fishing/automation/engine.py`：第二次 F 后建立新进度阶段。
- `try/tests/test_progress.py`、`try/tests/test_engine.py`：极端首帧、跳变确认和状态边界回归测试。
- `docs/superpowers/plans/2026-07-14-extreme-marker-tracking.md`：测试先行实施计划。
- `AGENTS.md`、`doc/验收标准.md`、本进展记录：同步长期规则、可验证验收项和执行证据。
- 工作区外烟雾运行：`D:\29551\异环自动钓鱼数据\runs\run-20260714T092529910365Z\`；测试后整个数据目录为 2,109 个文件、49,625,081 字节，低于 100 MiB 上限。
- PyInstaller 构建期间按默认行为临时使用 `C:\Users\29551\AppData\Local\pyinstaller\`，构建后该路径不存在，未保留 C 盘构建缓存。

### 错误汇报

- 首次在链接工作树调用不存在的相对 `.venv` 路径，测试未执行；确认项目根目录当前没有 `.venv` 后改用已安装固定依赖的 Python 3.13.5，全套测试与构建均通过。
- 候选低分辨率运行截图最长边仅 480 像素，无法可靠重跑原始顶部五扫描线识别，因此未把降采样截图伪装成真实识别回放证据；8% 阈值使用日志中已提取的原始结构化位置统计与合成帧回归验证，新发布物仍标记实机人工确认。

### 高风险发布与回退方案

- 当前根目录发布物大小 63,957,832 字节，SHA256 `DED6B53147058B37A604F6FD00B495E8914663AF99BC9DF42579298E9A1B2D56`；替换前备份到 `D:\0文件夹\备份\异环自动钓鱼-extreme-marker-tracking-YYYYMMDD-HHmmss\`，同类最多保留 2 份。
- 仅在候选哈希、管理员清单、烟雾、全套测试和 Git 状态均通过后执行同卷覆盖替换。替换后复核根目录大小、SHA256 与清单；任一验证失败立即从上述备份恢复旧 EXE，代码回退到检查点 `ecf4bf4`，生产数据不删除、不迁移。
- 发布执行结果：旧根目录 EXE 已备份到 `D:\0文件夹\备份\异环自动钓鱼-extreme-marker-tracking-20260714-172822\异环自动钓鱼.exe`，备份与旧文件 SHA256 均为 `DED6B53147058B37A604F6FD00B495E8914663AF99BC9DF42579298E9A1B2D56`；同类备份为 1 份。候选经同卷暂存哈希校验后覆盖移动，根目录最终大小 63,955,648 字节、SHA256 `47BD572C85D2C344343BDC58B9B5FCF5003946FB135AC95CFB6488D05650E035`，与候选一致，最终内嵌清单复核通过。
- 任务分支以合并提交 `17b1093` 纳入本地 `main`；合并后全套 358 项再次通过，耗时 32.49 秒，最终提交后复验 358 项耗时 32.65 秒。已安全移除项目 `.worktrees` 下的已合并工作树并删除任务分支，仓库只保留 `main`；私有 GitHub 未经本次推送确认保持未推送。

## 2026-07-14 17:52 ~ 2026-07-14 18:10

### 本阶段完成内容

- 用户确认极端黄标修复实测通过，并授权在存储与清理复核无误后推送。GitHub CLI 已安装且账户 `q2955161835-debug` 认证有效；本地 `main` 初始比 `origin/main` 超前 15 个提交，工作区干净。
- 生产数据目录稳定为 5,860 个文件、104,864,281 字节，比 104,857,600 字节硬上限多 6,681 字节；程序已退出，连续三次采样不变，排除瞬时写入。最新真实运行 `run-20260714T095204572440Z` 共 3,866 个文件、56,940,147 字节，容量临界后约每 2 秒记录一次 `logging.backpressure`，丢弃量持续为 49～168 项。
- 根因确认：`register_write()` 已准确维护 `_known_total`，但超限时调用 `_enforce()` 没有传入该账本，清理器每个帧和事件都重新扫描约 5,800 个文件；日志线程无法跟上 30 FPS，退出时 10 秒无法排空，进程可能结束在文件落盘和配额登记之间。
- 以测试先行新增连续活动写入扫描次数回归；旧实现两次写入共扫描 3 次而失败。最小修复仅把 `_known_total` 传给 `_enforce()`，不改变清理顺序、保护文件、背压或错误策略；存储专项 34 项、全套 359 项通过。
- 在 `try/output/quota-audit-real-20260714-1802` 复制真实 5,860 文件布局：初始化仅删除 1 张最旧帧并降至 104,849,389 字节；60 次 15,000 字节活动写入耗时 0.8185 秒，根目录扫描仍为 1 次，最终 104,847,826 字节。
- 构建门 359 项通过，耗时 35.66 秒；候选大小 63,956,988 字节，SHA256 `9A4768D6930F939C32EE8AB23DABA3BA4B98B03351ECCEE9A7ECD96225E52CA4`，清单为 `requireAdministrator uiAccess=false`，直接烟雾输出 `SMOKE_OK`。烟雾正常启动清理生产目录后为 5,860 个文件、104,849,772 字节，恢复到硬上限内。

### 新增或修改文件

- `src/auto_fishing/storage/quota.py`：活动写入超限时复用容量账本，禁止重复扫描数据树。
- `try/tests/test_storage.py`：连续活动写入根目录扫描次数回归。
- `docs/superpowers/plans/2026-07-14-quota-write-scan.md`：本次测试先行修复与发布计划。
- `AGENTS.md`、`doc/验收标准.md`、本进展记录：同步长期约束、生产证据和验收状态。
- 工作区外只读审计 `D:\29551\异环自动钓鱼数据\`；候选烟雾按正常启动规则删除一张足以恢复配额的最旧运行帧，并新增 `runs\run-20260714T100936282470Z\`，未删除配置、诊断、事件文件或整份运行。
- PyInstaller 构建临时使用 `C:\Users\29551\AppData\Local\pyinstaller\`，构建后该路径不存在。

### 错误汇报

- 首次用 `Copy-Item -LiteralPath` 携带 `*` 复制真实布局失败，PowerShell 把通配符作为字面路径；后续空目录脚本结果已明确判定无效。改用 `Get-ChildItem | Copy-Item` 并强制校验副本至少 5,000 个文件后，取得上述有效结果。
- 配额算法本身的增量删除顺序正确，问题位于活动写入调用边界遗漏 `total` 参数；本次没有扩大为日志架构重构，也没有修改自动化主循环。

### 高风险发布与回退方案

- 当前根目录 EXE 大小 63,955,648 字节，SHA256 `47BD572C85D2C344343BDC58B9B5FCF5003946FB135AC95CFB6488D05650E035`；替换前备份到 `D:\0文件夹\备份\异环自动钓鱼-quota-ledger-write-YYYYMMDD-HHmmss\`，同类最多保留 2 份。
- 仅在候选哈希、管理员清单、烟雾、全套测试和 Git 状态均通过后同卷覆盖替换。任一复核失败立即恢复上述 EXE 备份，代码回退到检查点 `99729c0`；生产数据不做额外手工删除或迁移。
