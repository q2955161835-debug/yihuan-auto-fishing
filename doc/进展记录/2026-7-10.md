# 2026-7-10 进展记录

## 2026-07-10 15:23 ~ 2026-07-10 15:28

### 本阶段完成内容

- 检查四张 2560×1440 流程截图并确认抛竿、上钩、进度条控制和结算流程。
- 与用户确认多分辨率和窗口模式、30 帧/秒闭环、异常暂停、F8 紧急暂停、游戏窗口绑定及诊断自动清理策略。
- 确认使用窗口截图、颜色识别和状态机方案；明确不读取游戏内存或注入进程。
- 初始化 Git，提交原始流程截图基线，并创建 `root/feature-auto-fishing` 任务分支。
- 建立项目长期规则、设计规格和可验证的验收清单。

### 新增或修改文件

- `.gitignore`：忽略真实环境变量、构建产物、缓存和运行诊断。
- `.env.example`：记录当前没有环境变量及后续维护规则。
- `AGENTS.md`：项目目标、架构、目录职责、命令和验收要求。
- `docs/superpowers/specs/2026-07-10-auto-fishing-design.md`：用户已确认设计的完整书面规格。
- `doc/验收标准.md`：自动测试、构建和实机人工验收清单。
- `doc/进展记录/2026-7-10.md`：本阶段记录。

### 错误汇报

- 初始只读检查发现项目根目录没有 Git 仓库和历史记录。按项目规则初始化 `main`，将四张原始截图提交为基线 `47e8a34` 后再建立任务分支；未丢失或覆盖用户文件。

## 2026-07-10 15:29 ~ 2026-07-10 15:39

### 本阶段完成内容

- 用户确认书面设计规格，设计复核门已通过。
- 固定 Python 3.13.14 及截屏、图像识别、测试和打包依赖版本。
- 将实施拆成 10 个可独立测试和提交的任务，覆盖基础模型、存储清理、安全输入、窗口截屏、视觉识别、状态机、引擎、界面、打包和实机验收。
- 完成规格覆盖、占位符和跨任务接口自查，修正诊断文件时间测试、上钩交叉特征和引擎结算序列。

### 新增或修改文件

- `docs/superpowers/plans/2026-07-10-auto-fishing.md`：完整测试先行实施计划、接口、命令和预期结果。
- `docs/superpowers/specs/2026-07-10-auto-fishing-design.md`：状态更新为用户已确认。
- `AGENTS.md`：补充实施计划路径并更新当前阶段。
- `doc/进展记录/2026-7-10.md`：记录本阶段计划工作。

### 错误汇报

- 初稿中的单次白色闪烁可能同时改变亮度和边缘，无法满足“不误判上钩”的测试意图。改为必须出现蓝色环变化，并同时满足白色或边缘变化。
- 初稿引擎回放在进入等待结算后缺少后续确认帧。补全连续结算帧，确保状态依次经过等待结算、确认、点击和返回就绪。

## 2026-07-10 19:55 ~ 2026-07-10 20:09

### 本阶段完成内容

- 按测试先行新增 5 项打包契约测试，完整测试由 181 项增加到 186 项；最终构建门内结果为 `186 passed in 16.07s`。
- 新增 `asInvoker`、`PerMonitorV2` 应用清单和 DXcam 完整收集的 PyInstaller 单文件规格，发布物为 Windows GUI 子系统，不启用管理员权限、控制台或 UPX。
- 完成最终可覆盖构建，耗时 48.697 秒；生成 `D:\1Folder\异环自动钓鱼\.worktrees\auto-fishing\dist\异环自动钓鱼.exe`，大小 62,854,796 字节，SHA256 为 `98DDD85BA061E33ABC9221A7ED537ED6D58547A50553DA58C4074BA10F940506`。
- 完成发布物启动、响应、关闭和残留进程烟雾，输出 `SMOKE_OK`，耗时 7.177 秒，结束后同路径进程数为 0；PE 子系统复核值为 2。
- 完成结构与安全扫描：生产代码没有 `pyautogui`、`keybd_event`、`mouse_event`、固定 2560×1440 控制坐标、`TODO/TBD` 或临时阈值补丁；分辨率命中只位于多分辨率测试。仅发现一个 `FishingStateMachine`，没有重复状态机或新增业务技术债。
- 更新项目规则和逐项验收结果；真实《异环》窗口、无边框、全屏、快速进度条、F8、多轮与真实诊断清理均保持“人工确认（未执行）”。由于真实游戏和无开发环境独立会话验收未完成，本阶段不合并 `main`、不删除任务分支。
- 烟雾启动读取并在正常关闭时更新工作区外 `C:\Users\29551\AppData\Local\异环自动钓鱼\settings.json`，最终修改时间为 `2026-07-10 20:09:23 +08:00`；未生成诊断文件，未修改 `流程截图`。

### 新增或修改文件

- `packaging/app.manifest`：声明普通用户权限和 Per-Monitor V2 DPI 感知。
- `packaging/auto_fishing.spec`：单文件、无控制台 PyInstaller 配置，收集 DXcam 数据、二进制和隐藏导入。
- `scripts/build.ps1`：UTF-8 兼容的测试门、清理构建、发布物存在检查和 SHA256 输出。
- `try/smoke_exe.ps1`：发布物启动、响应、主窗口关闭、强制兜底和零残留验证。
- `try/tests/test_packaging.py`：清单、规格、构建脚本、PowerShell 编码和烟雾清理契约测试。
- `AGENTS.md`：同步实际阶段、依赖版本、目录职责、命令和离线基线。
- `doc/验收标准.md`：记录自动测试、构建、哈希、大小、警告和全部待人工确认项。
- `doc/进展记录/2026-7-10.md`：记录本阶段实施、异常、外部文件行为和回退边界。

### 错误汇报

- 第一次构建在测试前出现 Windows PowerShell 5.1 中文脚本解析错误。原因是无 BOM 的 UTF-8 脚本被按本地编码读取；新增失败测试后为两个 PowerShell 脚本加入 UTF-8 BOM 和 UTF-8 输出设置，聚焦测试通过。
- 第二次构建在 186 项测试通过后报告入口 `packaging\src\auto_fishing\__main__.py` 不存在。原因是 PyInstaller 相对入口按规格文件目录解析；新增失败测试后将入口、`pathex` 和清单锚定到仓库根目录。
- 首次成功烟雾只停止 PyInstaller 单文件启动器父进程，遗留带窗口子进程 PID 120504，后续覆盖构建出现 `PermissionError: [WinError 5]`。新增残留进程失败测试后，脚本改为按发布物绝对路径跟踪全部父子进程、优先关闭主窗口、超时强制终止并断言零残留；清理该任务创建的残留进程后，可覆盖构建与烟雾均通过。
- PyInstaller 生成 `build/auto_fishing/warn-auto_fishing.txt`，内容包括非 Windows 条件模块、NumPy 动态导出、`comtypes.gen` 动态命名空间和可选开发依赖；启动烟雾没有缺模块异常，但 DXcam/COM 实际捕获仍纳入真实游戏人工验收。构建哈希表的中文路径经 Windows PowerShell 管道显示乱码，已通过独立文件查询核对真实路径、大小和哈希，未影响产物。

## 2026-07-10 20:14 ~ 2026-07-10 20:30

### 本阶段完成内容

- 根据任务 10 审查修复烟雾进程所有权：删除按 `ExecutablePath` 枚举、拒绝和批量清理逻辑，改为以 `Launcher.Id` 为永久种子，轮询 `Win32_Process.ParentProcessId` 递归维护所有权 PID 集合；响应、主窗口关闭、强制终止和残留检查均只作用于该集合。
- 新增构建解释器覆盖和安装顺序契约；`scripts/build.ps1` 支持 `-PythonPath` 参数或 `AUTO_FISHING_PYTHON` 环境变量，默认仍使用项目 `.venv`。`AGENTS.md` 按顺序增加 `pip install -e .`。
- 在此前不存在的忽略目录 `try/output/clean-venv` 新建 Python 3.13.5 虚拟环境；固定依赖安装耗时 101.330 秒，可编辑安装耗时 9.040 秒，从非仓库根目录导入 `auto_fishing` 成功，干净环境全套 `188 passed in 16.70s`。
- 使用干净解释器执行构建脚本，首次干净构建门内 `188 passed in 16.12s`，总耗时 47.910 秒；审查修复完成后的最终复验构建门内 `188 passed in 16.11s`，总耗时 48.628 秒。最终发布物大小 62,851,946 字节，生成时间 `2026-07-10 20:29:10 +08:00`，SHA256 为 `48E6151BACB129E551AFBCE7C876FF2DB2E0DF9B2C630BADDF67CA187352CD43`。
- 最终正常烟雾输出 `SMOKE_OK`，耗时 6.040 秒。并发同路径隔离验证中，外部实例 PID 在烟雾前后均为 `123632,123788`，输出 `CONCURRENT_OWNERSHIP_OK`；烟雾未观察或终止外部实例，测试结束后同路径进程数为 0。
- 对比两次干净构建：首次分析开始时 `comtypes.gen` 尚不存在，警告文件含 2 条静态分析告警且 TOC 未收集；该次 PyInstaller 分析于 `20:22:33` 生成 `clean-venv/Lib/site-packages/comtypes/gen/__init__.py`。最终重建的警告文件中 `comtypes.gen` 命中为 0，`Analysis-00.toc` 和 `PYZ-00.toc` 均已收集该命名空间；启动烟雾无缺模块错误，DXcam/COM 捕获继续保持实机人工确认。
- 真实游戏和独立 Windows 用户会话仍未验收，因此继续停留任务分支，不合并 `main`、不删除分支。

### 新增或修改文件

- `try/tests/test_packaging.py`：增加进程树所有权、禁止路径批量清理、所有权终止、构建解释器覆盖和可编辑安装顺序契约。
- `try/smoke_exe.ps1`：改为递归 PID 所有权模型，并修正响应检查使用 `Get-Process.Id`。
- `scripts/build.ps1`：增加可选 Python 解释器参数和环境变量入口。
- `AGENTS.md`：补充可编辑安装、干净构建命令和烟雾所有权规则。
- `doc/验收标准.md`：更新干净环境、188 项测试、新发布物及并发所有权证据。
- `doc/进展记录/2026-7-10.md`：记录审查修复、干净环境和错误处理。
- `.superpowers/sdd/task-10-report.md`：同步任务 10 审查修复报告；该文件按既有规则保持 Git 忽略。

### 错误汇报

- 审查发现第一版零残留修复按可执行文件路径枚举全部实例，可能关闭同时启动但不属于本次烟雾的进程。原因是把“同发布物路径”错误等同于“本次启动所有权”；改为启动器 PID 递归子孙集合，并通过真实并发实例验证隔离。
- 干净构建后的首次新烟雾报错 `Get-Process -Id` 收到空值。原因是响应循环已改用 `Get-Process` 对象，但仍读取旧 CIM 对象字段 `ProcessId`；先增加失败回归断言，再改为 `Item.Id`，聚焦测试和真实烟雾通过。
- `pip install -e .` 在 `src/` 生成未跟踪 `n_te_auto_fishing.egg-info`。该目录是安装元数据而非项目源码，已通过补丁删除其中 5 个生成文件；干净虚拟环境本身位于已忽略的 `try/output/`。
- 依赖安装通过阿里云 PyPI 镜像完成，无网络失败；pip 提示当前 25.1.1 可升级到 26.1.2。为保持本次工具链可复现性未升级，提示已记录。

## 2026-07-10 20:34 ~ 2026-07-10 21:04

### 高风险与回退方案

- 终审修复完成后需用全新虚拟环境重建发布物；`scripts/build.ps1` 会清理并覆盖已忽略的 `build/` 和 `dist/`。构建前将现有 `dist/异环自动钓鱼.exe` 备份到 `D:\0文件夹\备份\异环自动钓鱼-final-fix-prebuild-20260710-2059\`；若构建失败，可从该目录恢复原发布物。源码回退使用本任务提交的父提交。

### 本阶段完成内容

- 按测试先行完成四组终审修复：Tk 内部句柄统一解析为顶层 HWND 且捕获排除失败显示非致命提示；恢复前激活并复核前台、令牌跨无法分类帧保留；新增暂停态取消当前轮并重绑路径；拆分原始结算候选与三帧最终确认；设置拒绝溢出/非有限数字并改用 `config.json`。
- 每组均观察到预期 RED 后再实现：顶层 HWND 组 3 项失败→3 项通过；恢复/重绑组 5 项失败→引擎与界面 76 项通过；候选层级组 2 项失败→场景与引擎 68 项通过；设置组 3 项失败→存储与界面 37 项通过。开发环境全套为 `198 passed in 16.26s`。
- 新建 `try/output/final-fix-venv`，安装固定依赖及可编辑项目；最终构建门内 `198 passed in 17.18s`，总耗时 50.605 秒。发布物大小 62,858,688 字节，SHA256 `2A28B6F7D70B3BAB8D8B353A20CBE395002EF42D64860493D9B46B65D2E566F9`。
- 发布物烟雾输出 `SMOKE_OK`，耗时 6.435 秒；结束后同路径残留进程数为 0。工作区外 `C:\Users\29551\AppData\Local\异环自动钓鱼\config.json` 已生成，未写入敏感信息。
- 生产扫描无 `pyautogui`、弃用输入、固定 2560×1440、`TODO/TBD` 命中；仅有一个 `FishingStateMachine` 定义，`git diff --check` 无输出，四张 `流程截图` 哈希不变。真实游戏验收仍为人工确认，按终审任务要求不合并 `main`、不删除分支。

### 新增或修改文件

- `.gitignore`：忽略可编辑安装生成的 `*.egg-info/`。
- `src/auto_fishing/platform/windowing.py`、`app.py`、`ui/main_window.py`：顶层 HWND、非致命捕获排除提示及暂停态重绑界面路径。
- `src/auto_fishing/automation/engine.py`、`automation/state_machine.py`：恢复激活/令牌生命周期及可复用取消当前轮。
- `src/auto_fishing/model.py`、`vision/scenes.py`：原始 `result_candidate` 观测层级。
- `src/auto_fishing/storage/settings.py`：非有限数字防护；应用装配改为 `config.json`。
- `try/tests/test_capture_window.py`、`test_engine.py`、`test_scenes.py`、`test_storage.py`、`test_ui_smoke.py`：上述行为与并发回归测试。
- `AGENTS.md`、`doc/验收标准.md`：同步长期规则、198 项基线和新发布物证据。
- `.superpowers/sdd/final-fix-report.md`：完整 RED/GREEN、构建、扫描与关注点报告；该目录按规则保持 Git 忽略。
- 工作区外 `D:\0文件夹\备份\异环自动钓鱼-final-fix-prebuild-20260710-2059\异环自动钓鱼.exe`：覆盖构建前旧发布物备份，大小 62,851,946 字节，SHA256 `48E6151BACB129E551AFBCE7C876FF2DB2E0DF9B2C630BADDF67CA187352CD43`。

### 错误汇报

- 顶层句柄首轮 GREEN 的事件顺序断言仍按旧装配顺序编写，导致 1 项测试失败；根因是测试预期未包含“创建窗口后立即解析顶层句柄”，修正断言后通过，生产逻辑未回退。
- 恢复激活失败首轮仍保留旧 `E_USER_PAUSE`；根因是核心暂停默认保护已有暂停原因。为恢复前窗口复核增加显式 `replace_existing=True`，确保失败稳定呈现 `PAUSED/E_WINDOW`。
- 全新环境执行 `pip install -e .` 在 `src/` 生成 `n_te_auto_fishing.egg-info`；删除生成文件并在 `.gitignore` 增加 `*.egg-info/`，避免后续污染。依赖安装成功，pip 升级提示未影响固定工具链。

## 2026-07-10 21:07 ~ 2026-07-10 21:22

### 高风险与回退方案

- 最终复审修复后再次运行洁净构建，构建脚本会覆盖已忽略的 `build/` 与 `dist/`。覆盖前将当前发布物备份至 `D:\0文件夹\备份\异环自动钓鱼-final-review-prebuild-20260710-2117\`；若构建失败，可恢复大小 62,858,688 字节、SHA256 `2A28B6F7D70B3BAB8D8B353A20CBE395002EF42D64860493D9B46B65D2E566F9` 的上一发布物。源码通过最终复审提交的父提交回退。

### 本阶段完成内容

- 按测试先行修复暂停恢复线性化：新增迟到窗口错误确定性竞态，先观察恢复超时失败，再让 worker 在 `pause_lock` 下原子读取暂停态、恢复令牌和 epoch；暂停且无令牌时在截帧、窗口前台/刷新和视觉识别前等待。帧错误仅在其 epoch 仍有效时暂停，迟到 `E_WINDOW` 不再清除新令牌。聚焦竞态回归 4 项通过。
- 按测试先行修复控制状态候选优先级：两项新测试先在第六帧候选时错误进入 `E_PROGRESS_LOST`，随后实现“当前帧是候选则不走普通六帧暂停；连续两帧候选优先 `BAR_GONE`；候选中断再按累计缺失暂停”。相关 4 项通过。
- 删除 `.env.example` 文件末尾多余空行。开发环境引擎 53 项通过，全套为 `201 passed in 16.45s`。
- 新建 `try/output/final-review-venv` 并安装固定依赖及可编辑项目；洁净构建门内 `201 passed in 17.11s`，总耗时 50.663 秒。新发布物大小 62,858,451 字节，SHA256 `E6704FC0C0F925E433312A63333546BA2868101D86350262B573F2ED39EC4322`。
- 启动烟雾输出 `SMOKE_OK`，耗时 6.067 秒，同路径残留进程数为 0。真实游戏验收继续保持人工确认；按任务要求不合并 `main`、不删除任务分支。

### 新增或修改文件

- `src/auto_fishing/automation/engine.py`：暂停无令牌门控、帧 epoch 错误有效性检查、结算候选优先级。
- `try/tests/test_engine.py`：迟到 `E_WINDOW` 竞态和第六/七帧候选边界回归。
- `.env.example`：移除 EOF 多余空行。
- `AGENTS.md`、`doc/验收标准.md`：同步 worker 线性化规则、201 项测试与新发布物证据。
- `.superpowers/sdd/final-fix-report.md`：追加最终复审 RED/GREEN、构建与扫描证据；该目录保持 Git 忽略。
- 工作区外 `D:\0文件夹\备份\异环自动钓鱼-final-review-prebuild-20260710-2117\异环自动钓鱼.exe`：覆盖构建前发布物备份。

### 错误汇报

- 首次删除 `.env.example` 尾部空行后组合命令仍输出旧提交范围的 EOF 警告。原因是 `git diff --check <base>..HEAD` 只检查已提交历史，不包含当前工作区修复；单独 `git diff --check -- .env.example` 已确认工作区净差异。最终提交后重新以 `git merge-base main HEAD` 到新 `HEAD` 检查并记录准确结果。
- 在 Windows PowerShell 中直接执行未加引号的 `git diff --check $(git merge-base main HEAD)..HEAD` 时，`..` 被 PowerShell 解析为范围运算符，Git 返回退出码 129 和用法提示；未发生文件修改。将提交范围整体加引号后执行 `git diff --check "$(git merge-base main HEAD)..HEAD"`，退出码为 0 且检查本身无输出。

## 2026-07-10 21:27 ~ 2026-07-10 21:37

### 高风险与回退方案

- 本轮同类 epoch 漏口修复后需再次覆盖已忽略的 `build/` 与 `dist/`。覆盖前检查发现外层复审构建已把当前发布物更新为 62,859,099 字节、SHA256 `C753D4D4016506E839B76A6663D33364BF69A014FCAD00C88DC5574079689B4A`；先备份至 `D:\0文件夹\备份\异环自动钓鱼-epoch-review-prebuild-20260710-2133\`。本轮只在确认前一构建进程结束后启动独立洁净重建；失败时可恢复该备份，源码通过本轮提交父提交回退。

### 本阶段完成内容

- 按测试先行增加第二次 `frame_source.latest()` 阻塞竞态：首帧进入 `WAIT_BITE`，旧调用阻塞期间暂停并继续、发布新令牌，随后旧调用迟到抛错。测试先恢复超时，再让调用前保存 operation epoch；迟到 `E_CAPTURE` 仅在 epoch 仍有效时暂停，过期时继续循环而不退出 worker。与迟到窗口错误聚焦测试 2 项通过。
- 按测试先行增加陈旧帧释放失败竞态：通过真实 `AutomationCore.release_inputs()` 形成输入边界异常，在引擎字符串化错误前阻塞，再暂停并继续。测试先恢复超时，随后为 `E_INPUT` 传入 frame epoch；过期错误不再清除令牌。四类迟到错误/ABA 聚焦测试 4 项通过。
- 开发环境引擎测试 `55 passed in 14.74s`，全套 `203 passed in 16.95s`。
- 新建 `try/output/epoch-review-venv`，安装固定依赖及可编辑项目；洁净构建门内 `203 passed in 17.31s`，总耗时 49.518 秒。最终发布物大小 62,858,394 字节，SHA256 `4F2F15F79905B2424DAC384288C6BAD7F014A065C3A2678AC2D71F92F74FA353`。
- 烟雾输出 `SMOKE_OK`，耗时 6.723 秒，同路径残留进程数为 0。真实游戏验收仍为人工确认；不合并 `main`、不删除任务分支。

### 新增或修改文件

- `src/auto_fishing/automation/engine.py`：`latest()` operation epoch、过期 `E_CAPTURE` 继续循环、陈旧帧 `E_INPUT` frame epoch 防护。
- `try/tests/test_engine.py`：阻塞截图异常和迟到释放失败的确定性回归测试及完整测试边界替身。
- `AGENTS.md`、`doc/验收标准.md`：同步 operation/frame epoch 长期规则、203 项基线、最终构建和烟雾证据。
- `.superpowers/sdd/final-fix-report.md`：追加本轮 RED/GREEN、构建异常与最终产物证据；该目录保持 Git 忽略。
- 工作区外 `D:\0文件夹\备份\异环自动钓鱼-epoch-review-prebuild-20260710-2133\异环自动钓鱼.exe`：独立重建前发布物备份。

### 错误汇报

- 上游复审第一次在主线程使用短超时等待构建时发生 `OSError 22`。这是外层等待/输出通道异常，不能证明构建成功或失败；没有并行重复启动。确认前一构建进程已退出后，本轮在新虚拟环境中第二次独立重建成功。
- 覆盖前外层构建产物 SHA256 为 `C753D4D4016506E839B76A6663D33364BF69A014FCAD00C88DC5574079689B4A`，本轮同工具链独立构建为 `4F2F15F79905B2424DAC384288C6BAD7F014A065C3A2678AC2D71F92F74FA353`。PyInstaller 单文件包含构建时间戳及归档元数据，字节级哈希不是可重复构建保证；因此不得复用历史哈希，必须对最终文件重新计算并记录。
