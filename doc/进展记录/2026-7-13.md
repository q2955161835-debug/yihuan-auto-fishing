# 2026-07-13 进展记录

## 2026-07-13 13:24 ~ 2026-07-13 13:43

### 本阶段完成内容

- 用户确认自适应窄绿区规格后，完成测试先行实施计划并复核真实运行根因：高品质鱼逻辑绿区约占 `TOP_ROI` 宽度 9.8%，被候选生成与最终校验中重复的 12% 固定门槛拒绝。
- 从 `C:\Users\29551\AppData\Local\异环自动钓鱼\diagnostics\20260712T111434429873Z_E_TIMEOUT.png` 裁出 1332×216 的真实顶部夹具。旧代码在真实夹具与 9 组跨分辨率/黄标位置用例上按预期失败，证明回归测试命中根因。
- 每个黄标候选现按 `max(image_width * 0.02, yellow_width * 4)` 计算最小绿区宽度，门槛随候选进入多行共识并用于最终校验与置信度；删除生产识别路径中的 12% 固定门槛。
- 黄标覆盖绿区边缘时，单段候选的逻辑边界包含相邻黄标，避免遮挡后返回不包含黄标的截断绿区。结构候选在共识后统一校验，使短噪声明确记录 `bar_too_narrow`，无结构仍记录 `no_consensus`。
- 进度识别定向测试 `27 passed in 0.22s`，全量测试 `303 passed in 16.72s`。稳定结束切换、输入和状态机测试未修改且继续通过。
- 构建门再次执行 `303 passed in 16.78s`，清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`；新发布物大小 63,942,526 字节，SHA256 为 `BFE86E6485ABB3B547DEC24F2D52DD295D64303E332831D04B429DFA2A4526E7`。

### 新增或修改文件

- `docs/superpowers/plans/2026-07-13-adaptive-progress-width.md`：测试先行的夹具、实现、回归、备份和构建步骤。
- `try/fixtures/progress/progress_narrow_high_quality.png`：高品质鱼 9.8% 窄绿区真实回归夹具，仅含顶部识别区域。
- `try/tests/test_progress.py`：真实窄绿区、三种分辨率、三种黄标位置和过窄诊断用例。
- `src/auto_fishing/vision/progress.py`：候选级自适应门槛、共识传递和边缘遮挡重建。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-13.md`：同步长期识别规则、自动证据与实机待确认项。

### 错误汇报

- 问题：高品质鱼出现后程序完全没有进入 A/D 控制。原因：游戏按品质缩小绿区，而识别器仍假设绿区至少占外层裁剪宽度 12%，合法 9.8% 绿区在形成观测前就被拒绝。解决方案：最小宽度改为 2% 分辨率噪声下限与 4 倍黄标宽度中的较大值，并通过真实夹具、跨分辨率边缘样本和短噪声反例验证。
- 问题：首次自适应实现后，300 像素样本的最右边缘黄标被识别，但返回绿区不包含黄标。原因：黄标覆盖了绿区末端，单段回退候选仍使用遮挡后的绿色像素边界。解决方案：逻辑边界合并相邻黄标区间，同一聚焦测试随后 27 项全通过。

### 技术债检查

- 未新增机器学习依赖、第二套识别器、全局阈值旁路或状态机分支；公式集中在既有 `_line_candidates`，最终校验复用候选携带的同一门槛。新增真实图片只用于测试，可随 `try/` 清理。

### 高风险与回退方案

- 下一步构建会覆盖当前 SHA256 为 `F89B18DCAF284BB961068DD5281E05E631923F713F98B5D92FBE684BE033AD0A` 的发布物。构建前已备份到工作区外 `D:\0文件夹\备份\异环自动钓鱼-adaptive-width-prebuild-20260713-133821\异环自动钓鱼.exe`，备份 SHA256 与原文件一致；若实机出现噪声误识别或控制回归，恢复备份并回退独立实现提交 `63c61ce`，保留此前进度槽重建和稳定结束切换。

## 2026-07-13 13:53 ~ 2026-07-13 14:11

### 本阶段完成内容

- 实机运行 `run-20260713T055318102726Z` 成功识别高品质鱼约 9.8% 的窄绿区，并完成 A/D 中心控制，证明自适应宽度修复有效。
- 第 438 帧由控制进入等待结算；第 442 帧仍为紫色漩涡时，现有结算颜色比例已连续三帧成立并触发唯一一次点击 `(384, 792)`。Windows 返回鼠标请求 2/2 成功，但真正鱼获卡片约第 500 帧后才出现，程序没有重试并在第 678 帧超时。
- 屏幕键盘窗口为 `(0, 921)-(1706, 1440)`，首次点击未被键盘遮挡。当前根因是漩涡误判、点击过早和单次点击状态流；是否存在游戏过滤直接模拟鼠标仍需在正确时机重试后判断。
- 用户确认真实鱼获卡片可通过点击任意游戏区域关闭，并批准采用“顶部奖励栏 + 中央卡片联合确认、首次与重试随机延迟、最多三次、视觉验证关闭”的方案。

### 新增或修改文件

- `docs/superpowers/specs/2026-07-13-result-dismiss-retry-design.md`：真实鱼获卡片联合识别、随机计划、遮挡避让、三次重试、日志和验收规格。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-13.md`：记录高品质鱼实机通过项、结算关闭根因与待实施范围。

### 错误汇报

- 问题：程序显示“关闭结算”，但鱼获卡片没有被关闭。原因：紫色漩涡满足过宽的中央蓝/白比例条件，状态机在卡片出现前就用掉唯一一次点击；之后即使真实卡片持续存在也不会重试。解决方向：增加顶部奖励栏结构作为真实卡片门槛，以单调时钟执行随机延迟点击；卡片仍存在时最多重试三次，并以视觉消失而非 Windows 发送数作为成功依据。

### 技术债检查

- 不引入机器学习依赖或固定两秒等待；继续使用现有场景识别器与状态机，新增顶部小区域特征和有限重试状态。正确时机三次点击仍失败前，不提前增加虚拟触摸等第二输入后端。

## 2026-07-13 14:11 ~ 2026-07-13 14:53

### 本阶段完成内容

- 用运行 `run-20260713T055318102726Z` 的第 442 帧紫色漩涡和第 600 帧真实鱼获卡片建立正反回归夹具；识别器现要求中央卡片与顶部奖励栏同时成立。真实漩涡返回非候选，真实卡片连续三帧后成立。
- 真实卡片确认后首次点击随机等待 0.18～0.42 秒；卡片仍存在时以 0.40～0.80 秒间隔最多重试到三次。每次点击前重新确认卡片，候选坐标按顺序避开屏幕键盘；卡片消失后不再点击，出现就绪画面才计数。
- 完整运行日志新增点击计划、尝试次数/坐标、等待关闭和三次失败事件；三次后仍存在以 `E_RESULT_DISMISS` 暂停，安全点全部被遮挡以 `E_OSK` 暂停。
- 测试先行的定向引擎测试 75 项、识别/状态机/输入组合测试 158 项、全量测试 310 项全部通过；全量耗时 19.97 秒。

### 新增或修改文件

- `try/fixtures/result/result_transition_vortex.jpg`、`try/fixtures/result/result_catch_card.jpg`：真实紫色转场反例与鱼获卡片正例，仅用于回归测试。
- `src/auto_fishing/vision/regions.py`、`src/auto_fishing/vision/scenes.py`：顶部奖励栏区域及联合卡片识别。
- `src/auto_fishing/automation/engine.py`、`src/auto_fishing/app.py`：随机点击计划、视觉重检、遮挡避让、最多三次重试和运行日志接入。
- `try/tests/test_scenes.py`、`try/tests/test_engine.py`：真实夹具、随机时序、重试停止、错误码和 30 帧异步完成回归。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-13.md`：同步长期规则、自动证据和实机待确认项。

### 错误汇报

- 问题：旧异步完成测试在加入真实随机等待后于 1 秒内未消费最后一帧。原因：测试源按 30 帧/秒节流且包含 15 帧真实卡片等待，总运行时间接近原时限；失败快照显示首次点击已经成功，只有最终就绪帧尚未消费。解决方案：仅将三项真实节奏异步测试的等待上限调整为 2 秒，产品随机等待与状态机逻辑不变，随后引擎 75 项全通过。

### 技术债检查

- 未新增计算机视觉模型、第二套鼠标后端、固定长等待或无限重试。识别仍在既有场景模块中，重试状态集中在自动化核心；真实正确时机三次点击仍失败时再依据日志评估备用输入方式。

### 高风险与回退方案

- 下一步构建会覆盖当前大小 63,942,526 字节、SHA256 为 `BFE86E6485ABB3B547DEC24F2D52DD295D64303E332831D04B429DFA2A4526E7` 的发布物。构建前已备份到工作区外 `D:\0文件夹\备份\异环自动钓鱼-result-dismiss-prebuild-20260713-145224\异环自动钓鱼.exe`，备份哈希与原文件一致；若新版本发生卡片误判、错误连点或完整流程回归，恢复该备份并回退实现提交 `02a3612` 之后的结算重试改动。

### 构建与烟雾补充（2026-07-13 14:53 ~ 2026-07-13 15:01）

- 构建脚本再次执行全量 310 项测试，耗时 19.53 秒；最终内嵌清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`。
- 新发布物大小 63,944,436 字节，SHA256 为 `5FC58C3893B17452A2E2069805E900E1058157AFFA0DD1F6F49E21A408F45DAF`。当前提升权限的 PowerShell 会话直接运行 `try/smoke_exe.ps1`，启动、响应、所有权隔离与关闭输出 `SMOKE_OK`，结束后发布物进程数为 0。
- 错误汇报：首次从旧版 Windows PowerShell 子会话运行烟雾时，管理员发布物的两个 PyInstaller 进程未在脚本的 5 秒关闭窗口内退出；精确终止本次拥有的进程后，从当前提升权限会话直接复测通过。该异常属于测试启动链权限不一致，程序日志只记录启动、捕获排除和热键注册，未收到关闭流程；未改动生产关闭逻辑，也未终止游戏或已有屏幕键盘。

## 2026-07-13 15:08 ~ 2026-07-13 15:18

### 本阶段完成内容

- 实机运行 `run-20260713T070804371619Z` 已确认真实鱼获卡片联合识别和随机点击成功：第 420 帧进入关闭结算，两次点击 `(2048, 792)` 均由 Windows 完整发送，第 452 帧起卡片持续消失并恢复钓鱼画面。
- 根因定位为关闭成功后的阶段确认仍依赖旧 `ready` 标志；真实右下蓝色冷藏箱背景使旧蓝色比例条件长期不成立，程序因此在第 668 帧触发 `E_TIMEOUT`，完成数仍为 0。
- 自动化核心现仅在已执行过卡片点击后累计消失帧：连续三帧 `result=false` 即记录 `result.dismiss_confirmed` 并完成计数；两帧以内卡片重现会将计数清零，现有明确就绪观测仍可提前完成。
- 测试先行新增稳定消失与重现清零两项用例；旧代码按预期 2 项失败，修改后引擎 77 项、全量 312 项通过，全量耗时 21.96 秒。

### 新增或修改文件

- `src/auto_fishing/automation/engine.py`：结算卡片连续三帧消失确认、抖动清零和确认日志。
- `try/tests/test_engine.py`：无就绪图标也能完成计数、两帧消失后重现不得误计数的回归测试。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-13.md`：同步真实关闭证据、根因、自动证据与待复验计数。

### 错误汇报

- 问题：鱼获卡片已经关闭，控制窗口仍显示“关闭结算超时”。原因：点击逻辑以卡片消失停止重试，但完成计数仍只接受旧就绪图标识别；真实背景中冷藏箱占据右下区域，蓝色比例始终远高于旧阈值。解决方案：使用与点击成功直接相关的连续三帧卡片消失作为关闭确认，并用重现清零防止单帧/双帧识别抖动误计数。

### 技术债检查

- 未继续放宽场景颜色阈值，也未新增固定长等待或替代输入后端。关闭确认复用已经实机验证的鱼获卡片观测，状态只增加一个有界连续帧计数；旧通用就绪识别仍保留，后续若单独改进必须使用真实就绪/上钩反例验证。

### 高风险与回退方案

- 下一步构建会覆盖当前大小 63,944,436 字节、SHA256 为 `5FC58C3893B17452A2E2069805E900E1058157AFFA0DD1F6F49E21A408F45DAF` 的发布物。构建前已备份到工作区外 `D:\0文件夹\备份\异环自动钓鱼-result-closure-prebuild-20260713-151847\异环自动钓鱼.exe`，备份哈希与原文件一致；若连续消失确认导致误计数或多轮提前抛竿，恢复该备份并回退提交 `ce39270`。

### 构建与烟雾补充（2026-07-13 15:18 ~ 2026-07-13 15:23）

- 构建脚本再次执行全量 312 项测试，耗时 22.09 秒；最终内嵌清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`。
- 新发布物大小 63,944,934 字节，SHA256 为 `BF7520B44CBCA60F4C63DE8CA78461B745451183F67656540ED0ACD22F053521`；当前提升权限会话直接执行启动、响应、所有权隔离与关闭烟雾，输出 `SMOKE_OK`，结束后发布物进程数为 0。

## 2026-07-13 15:24 ~ 2026-07-13 15:44

### 本阶段完成内容

- 实机运行 `run-20260713T072447533967Z` 第 281 帧进入等待结算，第 320 帧后持续显示青绿色圆盘的“黑魔鬼”鱼获卡片；旧识别器因中央蓝色比例不足而全程未产生候选或点击，第 581 帧以等待结算超时暂停。
- 新识别器的中央卡片允许蓝色或青绿色比例大于 40%，仍同时要求顶部奖励栏；同一真实运行的绿色转场不含奖励栏，保持非候选。蓝色旧卡片与紫色旧转场回归继续通过。
- 按用户建议撤销“卡片连续消失直接计数”：卡片消失只停止重试，点击后必须识别右下初始鱼钩连续三帧才记录 `result.dismiss_confirmed signal=ready_hook` 并进入下一轮。真实上钩蓝色圆环不会满足初始鱼钩结构。
- 新增四张低分辨率真实夹具，测试先行时青绿色卡片与初始鱼钩两项按预期失败；修改后场景 30 项、引擎与场景组合 106 项、全量 315 项通过，全量耗时 22.02 秒。

### 新增或修改文件

- `try/fixtures/result/result_catch_card_green.jpg`、`result_transition_green.jpg`：青绿色鱼获卡片正例与绿色转场反例。
- `try/fixtures/result/result_closed_hook.jpg`、`bite_prompt_real.jpg`：关闭后的初始鱼钩正例与上钩蓝色圆环反例。
- `src/auto_fishing/vision/regions.py`、`src/auto_fishing/vision/scenes.py`：初始鱼钩区域、青绿色中央圆盘和真实鱼钩边缘/白色结构识别。
- `src/auto_fishing/automation/engine.py`、`try/tests/test_engine.py`：取消卡片消失直接计数，仅接受点击后的初始鱼钩确认并记录信号。
- `try/tests/test_scenes.py`：四张真实夹具的连续帧正反验证。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-13.md`：同步新状态条件、真实证据与待复验项。

### 错误汇报

- 问题：上一版成功识别蓝色鱼获卡片，本次却完全未识别结算。原因：不同鱼种的中央圆盘主色不同；“黑魔鬼”为青绿色，蓝色比例约 10.6%，但青绿色比例约 44.6%，旧中央蓝色 40% 固定条件过窄。解决方案：中央结构允许蓝色或青绿色，同时保留顶部奖励栏联合门槛，用真实绿色转场验证不会退回宽泛颜色误判。
- 问题：上一版以卡片消失直接计数，与用户确认的真实回合边界不一致。原因：卡片消失只能证明关闭，不能证明游戏已经回到可再次抛竿的初始阶段。解决方案：卡片消失只停止点击，右下初始鱼钩连续三帧才完成计数；上钩蓝色圆环作为强制反例。

### 技术债检查

- 未新增机器学习模型、模板硬编码或第二套状态机。颜色扩展仍在既有联合结构中，初始鱼钩沿用归一化区域与连续帧机制；四张真实图片仅用于测试，可随 `try/` 清理。

### 高风险与回退方案

- 下一步构建会覆盖当前大小 63,944,934 字节、SHA256 为 `BF7520B44CBCA60F4C63DE8CA78461B745451183F67656540ED0ACD22F053521` 的发布物。构建前已备份到工作区外 `D:\0文件夹\备份\异环自动钓鱼-green-result-ready-hook-prebuild-20260713-154341\异环自动钓鱼.exe`，备份哈希与原文件一致；若青绿色扩展误认转场或鱼钩确认失败，恢复该备份并回退实现提交 `7571f8b`。

### 构建与烟雾补充（2026-07-13 15:44 ~ 2026-07-13 15:55）

- 构建脚本再次执行全量 315 项测试，耗时 21.81 秒；最终内嵌清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`。
- 新发布物大小 63,942,764 字节，SHA256 为 `BD9A39318A64D6C17AC70108F23A81600B5060A22ADB86A05B9C30931DB775BE`；启动、响应、所有权隔离与关闭烟雾输出 `SMOKE_OK`，结束后发布物进程数为 0。

## 2026-07-13 15:55 ~ 2026-07-13 16:03

### 本阶段完成内容

- 用户指出每次鱼种不同，按蓝色/青绿色分别补充仍会持续遗漏。中央卡片识别改为色相无关：高饱和彩色像素比例大于 40%、白色环形细节大于 5%，并继续强制顶部奖励栏联合成立。
- 新增红、橙、黄、绿、青、蓝、紫 7 种中央圆盘合成回归；旧代码按预期有 5 种色相失败。颜色无关实现后场景 37 项、全量 322 项通过，全量耗时 22.03 秒。
- 右下初始鱼钩按钮仍是点击后的最终回合边界，连续三帧才计数；鱼获卡片颜色只用于定位结算页面，不决定下一轮开始。

### 新增或修改文件

- `src/auto_fishing/vision/scenes.py`：用统一高饱和像素比例替代蓝色/青绿色枚举，并移除会误杀深蓝圆盘的中央暗度条件。
- `try/tests/test_scenes.py`：7 种色相参数化卡片回归。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-13.md`：同步颜色无关规则、自动证据与待复验项。

### 错误汇报

- 问题：不同鱼种可能使用不同颜色的中央圆盘，继续逐色扩展会在下一种鱼上再次失败。原因：鱼种色相不是稳定 UI 特征。解决方案：改为不限制色相的高饱和彩色圆盘，并与稳定的顶部奖励栏和白色环形细节联合；阶段完成则使用右下初始鱼钩按钮。

### 技术债检查

- 删除了蓝色/青绿色重复分支，没有继续增加鱼种特例。高饱和、白色细节和顶部栏均集中在现有结算识别器；初始鱼钩保持独立的小区域结构确认。

### 高风险与回退方案

- 下一步构建会覆盖当前大小 63,942,764 字节、SHA256 为 `BD9A39318A64D6C17AC70108F23A81600B5060A22ADB86A05B9C30931DB775BE` 的发布物。构建前已备份到工作区外 `D:\0文件夹\备份\异环自动钓鱼-hue-agnostic-result-prebuild-20260713-160321\异环自动钓鱼.exe`，备份哈希与原文件一致；若颜色无关条件误认转场，恢复该备份并回退提交 `53f3674`。

### 构建与烟雾补充（2026-07-13 16:03 ~ 2026-07-13 16:07）

- 构建脚本再次执行全量 322 项测试，耗时 22.21 秒；最终内嵌清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`。
- 新发布物大小 63,942,912 字节，SHA256 为 `C9DF9470AFB7871B3E0720B27A868C4C7B5BAFE0688B70551D9EE17075C3D6C8`；启动、响应、所有权隔离与关闭烟雾输出 `SMOKE_OK`，结束后发布物进程数为 0。

## 2026-07-13 16:09 ~ 2026-07-13 16:24

### 本阶段完成内容

- 核对三次真实运行记录：屏幕键盘外框从可用的 `1706×519` 依次缩到 `1028×435` 和 `650×203`，最终客户区仅 `632×156`；最后一次日志仍按归一化坐标计算 F 为 `(188, 1360)`，用户实测实际点击到 H。
- 屏幕键盘定位改为固定 `1365:415` 外框比例，每次绑定从显示器与游戏客户区重新计算目标宽度，不再读取或继承当前窗口尺寸。2560 像素显示器目标宽度为 1707，1920 像素显示器目标宽度为 1365。
- 绑定后与每次 F/A/D 点击前重新读取客户区；宽度小于 900、高度小于 250 或宽高比不在 `3.2～3.9` 时直接拒绝点击，交由既有 `E_OSK` 安全暂停路径处理。
- 新增累计缩小复现、1920/2560 稳定目标、重复绑定和运行中异常缩放拒绝鼠标输入回归；屏幕键盘专项 29 项、全量 326 项通过。

### 新增或修改文件

- `src/auto_fishing/platform/on_screen_keyboard.py`：固定基准尺寸定位，保存绑定显示器/游戏区域，并在每次读取几何时执行布局与位置安全校验。
- `try/tests/test_on_screen_keyboard.py`：累计缩小、宽屏重复绑定和点击前拒绝异常布局测试。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-13.md`：同步长期安全约束、验收证据、错误原因与待实机项。
- 工作区外 `D:\0文件夹\备份\异环自动钓鱼-osk-geometry-prebuild-20260713-1623\异环自动钓鱼.exe`：构建前发布物备份。

### 错误汇报

- 问题：屏幕键盘每次弹出都更低、更小，最终越出可用区域且 F 坐标误点 H。原因：`position_bottom_left()` 以当前屏幕键盘宽高为下一次目标，Windows 响应式布局产生的缩小结果被后续绑定继续继承；固定按键比例只适用于完整键盘布局，异常布局仍未被拒绝。解决方案：定位尺寸改用不可累积的固定基准比例，并在绑定及每次输入前增加最小尺寸和宽高比安全门。

### 技术债检查

- 没有新增第二套坐标映射或按键特例；仍沿用现有客户区归一化键位，只在唯一的屏幕键盘窗口适配层统一尺寸和验证。阈值由两组已成功/一组已失败真实日志及默认 Windows 完整键盘布局推导，真实不同缩放模式仍需人工复验。

### 高风险与回退方案

- 下一步构建会覆盖当前大小 63,942,912 字节、SHA256 为 `C9DF9470AFB7871B3E0720B27A868C4C7B5BAFE0688B70551D9EE17075C3D6C8` 的发布物。已备份到工作区外 `D:\0文件夹\备份\异环自动钓鱼-osk-geometry-prebuild-20260713-1623\异环自动钓鱼.exe`，备份哈希与原文件一致；若新尺寸策略无法恢复完整键盘，恢复该备份并回退本阶段实现。

### 构建与烟雾补充（2026-07-13 16:24 ~ 2026-07-13 16:30）

- 构建脚本再次执行全量 326 项测试，耗时 22.13 秒；最终内嵌清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`。
- 新发布物大小 63,943,001 字节，SHA256 为 `67AA6C2205D5E313D6B7E992710076738A81DDC7354DC3903DABB2E9D4CA6709`；从当前提升权限会话直接执行启动、响应、所有权隔离与关闭烟雾输出 `SMOKE_OK`。
- 错误汇报：第一次从旧版 Windows PowerShell 子会话运行烟雾，关闭等待结束后仍报告本次拥有的 PID 58084/64112；核对为同一发布物父子进程后仅精确终止这两个测试进程，再从当前提升权限会话直接复测通过。未终止游戏或已有屏幕键盘。
- 新发布物已启动并停留在未绑定控制窗口，供用户手动实测；用户要求停止电脑操控后未再点击、切换窗口或操作游戏。

## 2026-07-13 16:30 ~ 2026-07-13 16:49

### 本阶段完成内容

- 最新运行 `run-20260713T082932994675Z` 从第 527～620 帧始终处于“等待上钩”，`ready=true`、`bite=false`、完成数为 0，最后因游戏失去前台以 `E_WINDOW` 暂停。用户实测确认当前版再次卡在前段，并指定回退到 `run-20260713T070804371619Z` 成功点击蓝色鱼获卡片时的版本。
- 生产视觉规则恢复到提交 `27fa29a`：撤销后续青绿色/任意色相卡片与右下初始鱼钩识别；保留 `2a3bf78` 的固定屏幕键盘尺寸和每次点击前布局安全校验。
- 状态机改为鱼获点击输入正常返回后立即计数并进入完成或轮间等待，不再等待卡片消失、初始鱼钩或其他视觉状态，也不再重试；输入抛错保持未完成。
- 场景、状态机与引擎专项 132 项、全量 313 项通过，全量耗时 20.39 秒。

### 新增或修改文件

- `src/auto_fishing/vision/scenes.py`、`src/auto_fishing/vision/regions.py`：恢复 `27fa29a` 的场景与蓝色鱼获规则。
- `src/auto_fishing/automation/engine.py`、`src/auto_fishing/automation/state_machine.py`：点击正常返回立即计数，删除点击后视觉确认与重试状态。
- `try/tests/test_scenes.py`、`try/tests/test_engine.py`、`try/tests/test_state_machine.py`：恢复历史识别测试并新增点击成功/失败完成边界回归。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-13.md`：同步当前规则、验收证据与明确取舍。
- 工作区外 `D:\0文件夹\备份\异环自动钓鱼-pre-ready-hook-rollback-prebuild-20260713-1648\异环自动钓鱼.exe`：构建前发布物备份。

### 错误汇报

- 问题：稳定键盘版本实测再次停在“等待上钩”，没有进入后续控制。原因：本轮日志中上钩差分始终未成立；用户不再接受继续叠加识别修复，明确要求恢复最后一次可完成鱼获点击的历史生产逻辑。解决方案：选择性恢复 `27fa29a` 的识别文件和结算流程，保留独立的屏幕键盘布局修复，并把系统点击成功设为唯一完成边界。

### 技术债检查

- 删除了 `result_clicked` 暂存标志、右下鱼钩完成事件、点击后等待与三次重试分支，没有保留并行状态机。明确取舍是恢复蓝色鱼获卡片识别，青绿色及其他色相会再次无法识别；同时“点击正常返回”只能证明 Windows 输入调用成功，不能证明游戏一定关闭了卡片。

### 高风险与回退方案

- 下一步构建会覆盖大小 63,943,001 字节、SHA256 为 `67AA6C2205D5E313D6B7E992710076738A81DDC7354DC3903DABB2E9D4CA6709` 的发布物。已备份到 `D:\0文件夹\备份\异环自动钓鱼-pre-ready-hook-rollback-prebuild-20260713-1648\异环自动钓鱼.exe`，哈希一致；若历史识别组合无法复现成功流程，恢复该备份并回退本阶段提交。

### 构建与烟雾补充（2026-07-13 16:49 ~ 2026-07-13 16:52）

- 构建脚本再次执行全量 313 项测试，耗时 20.45 秒；最终内嵌清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`。
- 新发布物大小 63,944,861 字节，SHA256 为 `B42D558A47695E0808EAE4F09EB817A05E6458F041019A37807D6AEF9652BA19`；启动、响应、所有权隔离与关闭烟雾输出 `SMOKE_OK`。
- 按用户要求未使用电脑操控启动或点击发布物，真实测试由用户手动执行。

## 2026-07-13 16:52 ~ 2026-07-13 17:14

### 本阶段完成内容

- 用户实测反馈上一发布物仍在等待结算阶段失败，并明确要求不再识别结算页面：顶部进度条消失后按固定过程等待几秒，再随机化延迟点击任意安全空白区域。
- 只读核对工作区外运行记录 `%LOCALAPPDATA%\异环自动钓鱼\runs\run-20260713T070804371619Z`：第 362 帧确认进度条消失；第 418 帧约晚 1.86 秒，鱼获页已稳定；已知首次成功点击约晚 2.34 秒。查看第 362、400、418、420、432 帧低分辨率截图后，将随机等待定为 2.10～2.60 秒。
- 自动化核心删除结算候选/卡片确认对阶段迁移和点击的依赖。至少 15 个可靠控制帧后，连续三帧干净的顶部进度条消失直接进入 `WAIT_RESULT` 并安排一次定时点击；到时即使观测为空也点击，输入正常返回立即完成计数。
- 等待结算期间暂停再继续会重新抽取完整 2.10～2.60 秒延迟；结算页观测不能让其他暂停状态恢复。点击抛错仍不计数并由引擎按 `E_INPUT` 安全暂停，安全点击点被屏幕键盘全部遮挡仍以 `E_OSK` 暂停。
- 状态机与引擎专项 102 项通过，全量 309 项通过，全量耗时 29.45 秒。

### 新增或修改文件

- `src/auto_fishing/automation/engine.py`：进度条消失后 2.10～2.60 秒随机计时、无视觉门控的一次安全点击、恢复后重新计时及日志字段。
- `src/auto_fishing/automation/state_machine.py`：删除结算识别事件和关闭结算中间状态迁移，点击事件直接从等待结算完成本轮。
- `try/tests/test_engine.py`、`try/tests/test_state_machine.py`：覆盖定时边界、空观测点击、结算候选无效、点击失败不计数、暂停恢复及完整线程生命周期。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-13.md`：同步时间证据、长期行为和人工验收项。

### 错误汇报

- 问题：上一版仍等待识别到蓝色鱼获卡片才点击，不同鱼种、转场或颜色变化会让流程停在等待结算。原因：结束阶段继续使用不稳定的结算页视觉特征作为必需门槛，而真实日志已经表明从进度条消失到页面稳定、可点击的过程时间近似固定。解决方案：只保留已验证的顶部进度条消失作为起点，以 2.10～2.60 秒随机等待覆盖约 1.86 秒的页面稳定时刻，并在点击正常返回后立即计数。
- 测试过程中工作树没有独立 `.venv`，第一次调用该路径失败；改用系统 Python 3.13 固定环境执行，代码测试正常。该异常未修改依赖或生产文件。

### 技术债检查

- 状态机不再存在结算识别与定时点击两套并行路径，也没有新增鱼种特例或重试分支。场景识别器仍可把历史结算字段写入诊断日志，但自动化核心完全忽略这些字段；这是保留日志兼容性的非控制性输出。
- 固定过程时间可能随游戏版本、帧率或动画设置改变；当前 2.10～2.60 秒来自一条已成功关闭结算的真实运行，需要用新发布物完整单轮复核。若页面在窗口结束时仍未可点击，必须用新日志重新量测，不应恢复鱼种相关视觉门槛。

### 高风险与回退方案

- 下一步单文件构建将覆盖当前大小 63,944,861 字节、SHA256 为 `B42D558A47695E0808EAE4F09EB817A05E6458F041019A37807D6AEF9652BA19` 的发布物。构建前必须备份到工作区外 `D:\0文件夹\备份` 的带时间目录；若定时点击过早、过晚或阶段回退，恢复该备份并回退本阶段实现提交。构建与烟雾不得操控游戏或已有屏幕键盘。

### 构建与烟雾补充（2026-07-13 17:14 ~ 2026-07-13 17:21）

- 实现与测试提交为 `4692b22 fix: click result after measured delay`。构建脚本再次执行全量 309 项测试，耗时 28.42 秒；最终内嵌清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`。
- 新发布物位于隔离工作树 `dist/异环自动钓鱼.exe`，大小 63,944,208 字节，SHA256 为 `94FCEDD67EF524A77F73EE43AAA84E60290D49E153C36D44DD64816C49FD3C0F`；启动、响应、所有权隔离与关闭烟雾复测输出 `SMOKE_OK`。
- 工作区外备份目录为 `D:\0文件夹\备份\异环自动钓鱼-timed-result-click-prebuild-20260713-1717`。其中 `异环自动钓鱼-worktree.exe` 是实际被本次构建覆盖的隔离分支旧发布物，SHA256 为 `B42D558A47695E0808EAE4F09EB817A05E6458F041019A37807D6AEF9652BA19`；另保留只读核对时发现的主工作区旧发布物 `异环自动钓鱼.exe`，SHA256 为 `27024B3FCD775E8B6818893AC4D02FFDA960AAB7B23A87E73D8E60A46A97050D`。本次没有覆盖主工作区 `dist`。
- 错误汇报：第一次构建工具调用被 1 秒执行通道超时中断，pytest 随后写入已关闭的标准输出并报 `OSError 22`，构建脚本在产物阶段前停止；改用完整时限重跑后通过。第一次烟雾从旧版 Windows PowerShell 子会话运行时，本次拥有的 PID 71460/68964 未在 5 秒内退出；核对父子关系后只精确终止这两个测试进程，从当前会话直接复测通过。未绑定或操控游戏，未启动、移动或关闭已有屏幕键盘。

## 2026-07-13 17:21 ~ 2026-07-13 17:35

### 本阶段完成内容

- 用户实测确认 2.10～2.60 秒版本在最后阶段点击失败，要求将等待延长 1 秒试验。结算起点、随机区间宽度、单次安全点击和点击即计数均保持不变，仅把窗口整体后移为 3.10～3.60 秒。
- 测试先行将旧窗口结束后的 4.099 秒设为“仍不得点击”，旧实现产生两项预期失败；修改常量后边界、暂停恢复、点击失败、遮挡回退 6 项通过。
- 第一次全量测试有 1 项合成流程仍只提供 3 秒等待，状态正确停留在 `WAIT_RESULT`；将测试夹具增加一帧等待后，全量 309 项通过，耗时 31.91 秒。

### 新增或修改文件

- `src/auto_fishing/automation/engine.py`：结算随机等待由 2.10～2.60 秒改为 3.10～3.60 秒。
- `try/tests/test_engine.py`：更新新延迟边界、暂停恢复边界和线程测试时限；合成单轮夹具增加一帧等待。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-13.md`：记录上一版本实机失败、新窗口和待人工验收项。

### 错误汇报

- 问题：进度条消失后 2.10～2.60 秒执行的结算点击未成功。原因尚不能由现有自动证据确定；按用户观察，最可能是目标页面在该窗口内尚未接受点击。解决方案：不新增视觉识别或重试，把相同随机窗口整体后移 1 秒，以新完整运行日志验证。
- 测试问题：直接核心夹具的最后观测只覆盖进入等待后的 3 秒，小于新最短 3.10 秒。解决方案：增加一帧空观测覆盖新边界，生产代码无需为该测试问题修改。

### 技术债与回退方案

- 未新增并行流程、鱼种特例或固定页面识别；只有两个集中常量发生变化。若 3.10～3.60 秒仍失败，应查看新运行的 `result.dismiss_scheduled`、`result.dismiss_attempt` 和截图，判断是点击时机还是坐标/输入失败，不再盲目叠加延迟。
- 下一步构建将覆盖 SHA256 为 `94FCEDD67EF524A77F73EE43AAA84E60290D49E153C36D44DD64816C49FD3C0F` 的隔离分支发布物；必须先备份到 `D:\0文件夹\备份`。新版本点击过晚或流程回退时恢复该备份并回退本阶段提交。

### 构建与烟雾补充（2026-07-13 17:35 ~ 2026-07-13 17:38）

- 实现与文档提交为 `7e58ad5 fix: delay result click by one second`。构建前将旧发布物备份到工作区外 `D:\0文件夹\备份\异环自动钓鱼-result-delay-plus-1s-prebuild-20260713-1736\异环自动钓鱼.exe`，大小 63,944,208 字节，SHA256 为 `94FCEDD67EF524A77F73EE43AAA84E60290D49E153C36D44DD64816C49FD3C0F`，源文件与备份哈希一致。
- 构建脚本再次执行全量 309 项测试，耗时 31.40 秒；最终内嵌清单校验为 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`。
- 新发布物大小 63,944,482 字节，SHA256 为 `7EF2290E39B8CB5F6343D03B96A04E34AA5C00EF6DD0C7E08483112ECC0D3430`；启动、响应、所有权隔离与关闭烟雾输出 `SMOKE_OK`，未绑定或操控游戏及屏幕键盘。

## 2026-07-13 17:57 ~ 2026-07-13 18:28

### 本阶段完成内容

- 核对真实运行 `run-20260713T094453947780Z`：第一轮结算点击已成功并计数，但 `INTER_ROUND` 进入约 1.003 秒后被通用超时抢先暂停；移除该状态的通用超时，保留专用 1 秒轮间分支，自动进入 `READY` 并在下一帧抛出第二轮。
- 将控制阶段丢失拆为两类：仍有扫描线或候选的 `bar_too_narrow`/`no_consensus` 等结构性歧义最多容忍 60 帧；零扫描线且零候选的完全空白仍为 6 帧。任一缺失帧立即释放 A/D，可靠观测恢复或类别切换后重新计数；连续三帧干净结束仍优先进入结算。
- 设置新增默认开启的“自动切回游戏”，严格保存为布尔值。已绑定后的“开始”和暂停后的“继续”会显式激活并复核游戏前台，失败时不启动、不恢复、不发送输入；关闭设置后保留 3 秒手动切回路径。
- 初始按钮改为“绑定并开始”：只进行一次 3 秒倒计时，绑定成功后直接启动。F8 可使该倒计时失效，迟到回调不会绑定或启动。
- 全量 328 项测试通过，耗时 35.69 秒。测试环境缺少固定依赖 DXcam 0.3.0，按项目允许的常用包规则安装到当前用户 Python 3.13 包目录后完成全量测试。

### 新增或修改文件

- `src/auto_fishing/automation/state_machine.py`、`src/auto_fishing/automation/engine.py`：轮间专用延迟和结构/空白双级丢失门槛。
- `src/auto_fishing/storage/settings.py`、`src/auto_fishing/platform/windows.py`、`src/auto_fishing/app.py`、`src/auto_fishing/ui/main_window.py`：自动激活设置、显式激活接口、一次绑定并启动及 UI 行为。
- `try/tests/test_state_machine.py`、`try/tests/test_engine.py`、`try/tests/test_settings.py`、`try/tests/test_ui_smoke.py`：对应回归、安全和设置测试。
- `docs/superpowers/specs/2026-07-13-multiround-resilience-auto-activation-design.md`、`docs/superpowers/plans/2026-07-13-multiround-resilience-auto-activation.md`：已确认设计与实施计划。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-13.md`：同步长期规则、验收步骤、问题原因和待实机项。
- 工作区外环境变化：当前用户 Python 3.13 包目录安装 `dxcam==0.3.0`，未修改项目依赖版本或敏感配置。

### 错误汇报

- 问题一：结算点击后没有进入第二轮。原因：`INTER_ROUND` 同时受 1 秒专用分支和 1 秒通用超时约束，通用超时在同次检查中先执行。解决方案：该状态不再使用通用超时，只保留轮间分支。
- 问题二：A/D 阶段连续 6 帧暂停。原因：真实失败帧仍有 3～5 条扫描线、5～9 个候选，主要是高难度窄条暂时被标为 `bar_too_narrow`，旧实现把这种结构性歧义与画面完全空白合并。解决方案：结构性歧义放宽到 60 帧，完全空白保持 6 帧，并始终先释放输入。
- 问题三：初次绑定后还要再次点击开始和等待。原因：绑定、开始是两个独立倒计时，自动激活设置此前仅保存未接入操作路径。解决方案：新增原子化绑定并启动命令，并将设置接入显式开始/继续。

### 技术债检查

- 没有新增第二套状态机、识别器或输入后端；双门槛集中在现有控制器，一次绑定启动复用现有绑定和启动命令。自动激活仅由用户直接操作触发，不进入 worker 或轮间路径。
- 当前自动证据不能证明游戏实际接受第二轮 F，也不能证明 60 帧窗口适合所有高品质鱼；两项均列为新 EXE 实机验收，不合并 `main`。

### 高风险与回退方案

- 下一步构建会覆盖隔离工作树当前 SHA256 为 `7EF2290E39B8CB5F6343D03B96A04E34AA5C00EF6DD0C7E08483112ECC0D3430` 的发布物。构建前备份目标为 `D:\0文件夹\备份\异环自动钓鱼-multiround-auto-activation-prebuild-20260713-1828\异环自动钓鱼.exe`；若第二轮、A/D 或启动交互回退，恢复该备份并回退本阶段提交。构建和烟雾不得绑定、操控游戏或已有屏幕键盘。
