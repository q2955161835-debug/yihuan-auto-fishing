# 2026-07-12 进展记录

## 2026-07-12 12:12 ~ 2026-07-12 12:21

### 本阶段完成内容

- 用户重新打开真实《异环》后启动屏幕键盘版发布物，控制窗口正常显示且 F8 注册成功；点击“绑定游戏”并在倒计时内切回游戏后，程序没有发送输入，而是在准备屏幕键盘阶段明确报错。
- 错误为 `WinError 740`：普通 `subprocess`（子进程接口）直接创建 `%WINDIR%\System32\osk.exe` 需要提升权限。项目保持 `asInvoker`（普通用户权限），没有通过管理员运行绕过。
- 先新增失败测试，确认旧 `OskLauncher`（屏幕键盘启动器）不接受 Windows 外壳接口；随后改用 `ShellExecuteW`（外壳执行接口）以 `open` 动作启动系统辅助功能程序，并检查返回值大于 32。屏幕键盘测试结果为 `19 passed in 0.19s`。
- 通过精确发布物路径向本次测试启动器及子进程发送正常关闭请求，两个进程均退出；没有关闭或终止《异环》。

### 新增或修改文件

- `src/auto_fishing/platform/on_screen_keyboard.py`：屏幕键盘启动从普通子进程创建改为 Windows 外壳启动，不请求管理员权限。
- `try/tests/test_on_screen_keyboard.py`：覆盖 `ShellExecuteW` 参数和小于等于 32 的失败返回码。
- `doc/进展记录/2026-7-12.md`：记录真实绑定失败、根因、解决方案和复测边界。

### 错误汇报

- 问题：真实绑定时报“屏幕键盘准备失败：启动 Windows 屏幕键盘失败：[WinError 740] 请求的操作需要提升”。原因：系统屏幕键盘带辅助功能启动约束，直接 `CreateProcess`（创建进程）路径被 Windows 拒绝。解决方案：使用 `ShellExecuteW` 的普通 `open` 路径交由系统外壳启动；不改变清单权限，不请求 UAC（用户账户控制）提升。

### 高风险与回退方案

- 下一次构建会覆盖当前 SHA256 为 `665FED58E50544E05B5D047CEEDD72A2392861533B0F9BB1F44C4B7CA153A4FF` 的屏幕键盘发布物。构建前备份到 `D:\0文件夹\备份\异环自动钓鱼-osk-shell-prebuild-20260712-1221\`；若构建或新实测失败，可回退到提交 `1641757` 及该发布物。

## 2026-07-12 12:21 ~ 2026-07-12 12:31

### 本阶段完成内容

- `ShellExecuteW` 修正版执行构建门，结果 `260 passed in 19.18s`；单文件构建成功，SHA256 `6183EF3FED58922C0C617D7B7000E85104753DF76600D48643559E328D1FDA42`，烟雾输出 `SMOKE_OK`。
- 真实绑定确认系统屏幕键盘能够启动，但普通权限程序移动高完整性辅助功能窗口时，`SetWindowPos`（设置窗口位置）返回 Windows 错误 5“拒绝访问”。程序没有请求管理员权限，也没有进入开始状态。
- 先新增失败测试，再把自动定位改为尽力而为：若移动被拒绝，读取屏幕键盘实际矩形；现有位置在目标显示器内且不遮挡顶部/右下角关键区域则继续，否则提示“请手动拖到游戏左下角后重新绑定”。
- 修复启动后定位失败时的所有权句柄：只要已找到由本程序启动的屏幕键盘，就保留句柄供退出清理，不因定位异常泄漏进程。屏幕键盘测试结果 `22 passed in 0.19s`。

### 新增或修改文件

- `src/auto_fishing/platform/on_screen_keyboard.py`：区分窗口移动拒绝、验证现有位置、给出人工放置提示并保留所有权句柄。
- `try/tests/test_on_screen_keyboard.py`：覆盖 Windows 错误 5、安全现有位置、危险现有位置及失败后的自启动实例关闭。
- `doc/进展记录/2026-7-12.md`：记录第二轮真实绑定、错误 5 根因和兼容方案。

### 错误汇报

- 问题：系统屏幕键盘启动后仍位于左上角，绑定错误为“无法定位到左下角；Windows 错误 5”。原因：UIPI（用户界面特权隔离）阻止普通权限程序移动高完整性辅助功能窗口。解决方案：不提权，允许用户首次手动放置并由 Windows 记忆；程序每次绑定验证实际矩形，安全才继续。

### 高风险与回退方案

- 下一次构建会覆盖 SHA256 为 `6183EF3FED58922C0C617D7B7000E85104753DF76600D48643559E328D1FDA42` 的外壳启动修正版。该版本可由上一次源码提交 `06601f4` 重建，且前一发布物已有工作区外备份；若新构建失败，回退提交并重建。

## 2026-07-12 12:31 ~ 2026-07-12 12:34

### 本阶段完成内容

- 读取用户手动放置后的屏幕键盘实际窗口矩形 `[3, 810, 825, 1158]`、客户区 `[10, 840, 818, 1150]`，程序按客户区归一化比例自动换算出 `A=(106,1009)`、`D=(196,1009)`、`F=(239,1009)`，证明坐标不依赖固定分辨率或历史位置。
- 关闭屏幕键盘时再次复现 Windows 错误 5；新增失败测试并区分 `OnScreenKeyboardCloseDenied`。只对该保护性拒绝安全保留系统键盘，其他关闭错误仍上报。
- 用户明确授权发布物以管理员权限运行。清单改为 `requireAdministrator`，保留普通权限下的位置读取、位置拒绝和关闭拒绝降级；屏幕键盘与权限清单定向测试结果为 `32 passed in 0.22s`。

### 新增或修改文件

- `packaging/app.manifest`：发布物请求管理员权限并保持 `uiAccess=false` 与 `PerMonitorV2`。
- `src/auto_fishing/platform/on_screen_keyboard.py`：区分并安全处理系统拒绝关闭屏幕键盘。
- `try/tests/test_packaging.py`、`try/tests/test_on_screen_keyboard.py`：覆盖管理员清单与错误 5 关闭降级。
- `AGENTS.md`、屏幕键盘设计规格与实施计划：同步管理员授权、普通权限降级和尽力关闭约束。

### 错误汇报

- 问题：普通权限程序不仅无法移动屏幕键盘，也无法发送 `WM_CLOSE`（窗口关闭消息），均返回错误 5。原因：系统屏幕键盘是受 `UIAccess` 保护的辅助功能窗口，普通程序完整性级别较低。解决方案：发布物按用户授权请求管理员权限；代码仍不强制终止系统进程，并对保护性关闭拒绝安全保留。

### 高风险与回退方案

- 管理员清单会使每次发布物启动出现 UAC 提示；若实机兼容性或启动体验不合格，可回退本阶段提交前状态，恢复 `asInvoker` 并继续使用实际位置自动识别和用户首次手动摆放方案。下一次构建前继续保留当前发布物备份。

## 2026-07-12 15:28 ~ 2026-07-12 15:37

### 本阶段完成内容

- 重新启动 SHA256 为 `9D029B1328B1320F79DE2BAB8925F1F1EE86DEADF5A550CB722D75271B20CC8E` 的发布物，真实绑定仍提示屏幕键盘准备失败且键盘保持左上角。
- 读取进程令牌确认发布物实际为中完整性级别 `8192`、屏幕键盘为高完整性级别 `12288`；随后读取最终 EXE 的 `RT_MANIFEST` 资源，发现实际仍为 `asInvoker`，与源 `packaging/app.manifest` 的 `requireAdministrator` 不一致。
- 根因定位为 PyInstaller 6.19：规格文件未设置 `uac_admin=True` 时，打包器会在合并自定义清单时按默认值重写权限等级。新增失败测试后，在规格文件显式开启管理员权限。
- 新增最终发布物验证器并接入构建门，直接读取打包后 EXE 的内嵌清单，要求 `requireAdministrator` 且 `uiAccess=false`；权限清单定向测试结果 `10 passed in 0.10s`。

### 新增或修改文件

- `packaging/auto_fishing.spec`：显式设置 `uac_admin=True`。
- `scripts/verify_release.py`：读取并验证最终 EXE 的内嵌权限清单。
- `scripts/build.ps1`：构建后、计算 SHA256 前执行最终发布物清单验证。
- `try/tests/test_packaging.py`：覆盖规格文件管理员开关、构建门调用及清单通过/拒绝逻辑。
- `AGENTS.md`：记录 PyInstaller 清单重写陷阱与最终发布物验证要求。

### 错误汇报

- 问题：源清单和 XML 单元测试均显示管理员权限，但发布物没有 UAC 提示，仍无法移动屏幕键盘。原因：PyInstaller 规格文件缺少 `uac_admin=True`，最终 EXE 内嵌资源被重写回 `asInvoker`。解决方案：同时设置规格参数并把最终二进制资源检查纳入构建门；以后不得用源 XML 代替发布物权限验收。

### 高风险与回退方案

- 下一次构建将首次生成实际请求 UAC 的发布物。当前错误权限版本已由构建前备份和提交保留；若新版本无法启动，可回退本阶段提交并恢复前一发布物，但该版本不能控制高完整性屏幕键盘，不能作为最终交付品。

## 2026-07-12 15:40 ~ 2026-07-12 15:47

### 本阶段完成内容

- 用户将 Codex 提升为管理员后，已可直接激活并读取管理员版控制窗口；重新绑定复现屏幕键盘定位失败，状态为 Windows 错误 `1400`，而非权限拒绝。
- 检查发现 `Win32KeyboardApi`（Windows 屏幕键盘接口）未给 `SetWindowPos` 及同组窗口 API 声明 `ctypes`（Python 原生接口）参数/返回类型。为避免 64 位窗口句柄与特殊 `HWND_TOPMOST`（置顶窗口）参数发生错误封送，补齐全部声明。
- 新增失败测试，要求真实接口的 `SetWindowPos` 使用两个指针宽度 `HWND` 参数和 `BOOL` 返回类型；修复后屏幕键盘定向测试结果 `25 passed in 0.21s`。

### 新增或修改文件

- `src/auto_fishing/platform/on_screen_keyboard.py`：为屏幕键盘相关 `user32` 调用补齐原生参数与返回值声明。
- `try/tests/test_on_screen_keyboard.py`：覆盖 `SetWindowPos` 的 64 位调用声明。
- `AGENTS.md`：记录窗口句柄声明约束。
- `doc/进展记录/2026-7-12.md`：记录实机错误 1400、根因与修复。

### 错误汇报

- 问题：管理员发布物仍无法定位屏幕键盘，提示 Windows 错误 1400。原因：未声明原生函数签名时，`ctypes` 按默认整数规则封送窗口参数，无法保证 64 位窗口 API 调用正确。解决方案：为所有使用到的 `user32` 函数显式声明 `argtypes/restype`，并以单元测试固定 `SetWindowPos` 的指针宽度签名。

### 高风险与回退方案

- 本次修改处于窗口移动关键链路，下一次构建前备份当前管理员发布物 SHA256 `5744AC41C0D61387FCDEE9386CA1AD21622737D3BB64546460CBED520B92426C`；如新构建无法启动，可恢复该发布物，但其定位错误 1400 未解决，不能作为最终交付品。

## 2026-07-12 15:55 ~ 2026-07-12 16:11

### 本阶段完成内容

- 用户实机确认管理员发布物自动鼠标点击 Windows 屏幕键盘 F 成功，游戏已出钩；运行日志记录 F 目标 `(488,1216)`、鼠标按下/抬起的 `SendInput` 成功结果以及约 30 FPS 截图。
- 实机随后停在“等待上钩”。排查运行目录 `C:\Users\29551\AppData\Local\异环自动钓鱼\runs\run-20260712T075551079206Z` 的 710 帧与结构化事件：状态机、截帧和输入均正常；旧检测器仅检查右下准备区域，真实上钩提示实际位于左上。
- 对实机截图量化后，提示出现时左上区域白色比例 `0.028+`、边缘比例 `0.24+`、暗色比例 `0.49~0.58`；抛竿前白色比例不足 `0.002`。先写失败测试，再将检测区域改为 `BITE_ROI=(0.23,0.02)-(0.32,0.14)`，使用白色/边缘/暗色组合和连续两帧确认，拒绝白闪与纯暗抛竿帧。
- 修复后视觉定向测试 `22 passed in 0.41s`、全量测试 `268 passed in 18.16s`；真实 710 帧回放首次命中第 453 帧，前 452 帧无误触发。

### 新增或修改文件

- `src/auto_fishing/vision/regions.py`：新增左上上钩提示区域。
- `src/auto_fishing/vision/scenes.py`：改为基于真实上钩提示组合特征识别。
- `try/tests/test_scenes.py`：覆盖左上提示、连续确认、白闪/纯暗拒绝和多分辨率缩放。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-12.md`：记录实机输入成功、识别根因、阈值和待验收项。

### 错误汇报

- 问题：F 出钩成功后，程序没有识别上钩。原因：视觉模型将上钩假定在右下准备区域，并要求蓝色差分；真实 UI 的上钩 F 提示在左上，且主要特征是白色圆环、边缘和暗底。解决方案：使用真实回放校准左上 ROI 与组合特征，不依赖抛竿前差分。

### 高风险与回退方案

- 下一次构建会覆盖 SHA256 `8D76B7535D7CA5F1D807D69955E832B2EB3E3C827E1EE21EB75D5789211F1BAA` 的正式发布物。构建前备份该文件；若新实测出现误触发，可回退至该 SHA，但其无法识别本轮左上上钩提示，不能作为最终交付品。

## 2026-07-12 16:21 ~ 16:36

### 本阶段完成内容

- 复核用户实测运行目录 `C:\Users\29551\AppData\Local\异环自动钓鱼\runs\run-20260712T081336310655Z`：首次 F 已成功出钩，随后 845 帧稳定在约 29～30 FPS；旧左上检测全程为 `bite=false`，最后因用户切出游戏按 `E_WINDOW` 暂停。
- 逐帧检查确认前一阶段的“左上提示”推断错误：真实上钩信号是右下蓝白圆环。该轮第 20～28 帧为抛竿动画，而第 201～208 帧为真实上钩提示。
- 先补充失败测试，再恢复右下 `BITE_ROI=(0.89,0.79)-(0.99,0.99)` 的相对基线检测：抛竿后的前 45 帧不判定，之后要求蓝色比例增加大于 `0.03` 且白色或边缘增加；连续两帧确认。该运行的离线回放首个命中为第 200 帧，前 45 帧无命中。定向测试 `63 passed in 2.64s`。
- 检查退出日志没有明确的关闭异常，且最后只记录到 `capture.stopped`，无法反查用户所见“关闭失败”。新增关闭清理事件：释放输入、关闭屏幕键盘输入或销毁窗口任一步失败时，在关闭记录器前写入 `application.cleanup_failed`，并写入失败步骤、异常类型与信息；正常清理写入 `application.cleanup_finished`。

### 新增或修改文件

- `src/auto_fishing/vision/regions.py`：恢复右下上钩圆环识别区域。
- `src/auto_fishing/vision/scenes.py`：加入 45 帧抛竿冷却并使用蓝色、白色/边缘相对基线组合确认。
- `try/tests/test_scenes.py`：覆盖冷却、右下圆环、多分辨率、连续确认和白闪拒绝。
- `src/auto_fishing/app.py`、`try/tests/test_ui_smoke.py`：覆盖并实现关闭失败事件在日志关闭前落盘。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-12.md`：纠正错误位置推断并记录回放证据及退出诊断约束。

### 错误汇报

- 问题：出钩后没有识别上钩，且用户看到关闭失败但运行日志没有对应异常。原因：左上文字被错误当作上钩提示；运行记录器在清理后才关闭，但没有为清理步骤落盘异常事件。解决方案：根据真实帧将检测恢复为右下圆环的基线差分并加入抛竿冷却；在关闭记录器前记录清理失败事件。暂不引入机器学习模型，因同一真实帧序列已能以可解释的规则稳定区分抛竿动画和上钩提示。

### 高风险与回退方案

- 下一次构建会覆盖当前发布物，构建前将精确备份 `dist\异环自动钓鱼.exe` 到 `D:\0文件夹\备份` 的时间戳目录。若新实测出现误触发，可恢复备份；但此前左上检测版本已被实测证伪，不能作为最终交付品。

## 2026-07-12 16:36 ~ 2026-07-12 16:37

### 本阶段完成内容

- 已将构建前发布物 SHA256 `E2535CE4443C171681D13178C88756…` 备份至 `D:\0文件夹\备份\异环自动钓鱼-bite-calibration-prebuild-20260712-163616\异环自动钓鱼.exe`。
- 执行 `powershell -ExecutionPolicy Bypass -File scripts/build.ps1 -PythonPath C:\Users\29551\AppData\Local\Programs\Python\Python313\python.exe`，构建门全量 `269 passed in 17.83s`，最终发布物清单校验为 `requireAdministrator uiAccess=false`。
- 新发布物为 `dist\异环自动钓鱼.exe`，大小 `63,932,151` 字节，SHA256 `932582B6E208490C7FE6AD0B18B2546470A72A58E34BE2F435134957A062F684`；待真实《异环》验收第二次 F、A/D 控制、结算和退出日志。

### 新增或修改文件

- `dist\异环自动钓鱼.exe`：已重新生成，属于 Git 忽略的发布产物。
- `D:\0文件夹\备份\异环自动钓鱼-bite-calibration-prebuild-20260712-163616\异环自动钓鱼.exe`：构建前备份，供回退使用。

### 错误汇报

- 无新增构建或发布物清单错误。

## 2026-07-12 16:40 ~ 16:49

### 本阶段完成内容

- 用户实机确认第二次 F 已自动成功触发。运行记录显示在第 355 帧进入控制阶段，屏幕键盘 D 的鼠标按下与抬起均成功；D 在 87 毫秒后因连续六帧未识别到进度条而被安全释放，最终暂停码为 `E_PROGRESS_LOST`。
- 逐帧复核表明现有进度条检测把顶部环境中的绿色条与无关黄色高亮错误配对，首次假观测位置固定、置信度仅 `0.2809`，不是可用于 A/D 的真实滑块标记。因此未以“延长盲按”掩盖错误，待保留原始帧后重新校准滑块与标记。
- 发现 `E_PROGRESS_LOST` 没有生成应有的原始诊断截图；根因是 `cv2.imwrite` 不能写入 `%LOCALAPPDATA%\异环自动钓鱼\diagnostics` 的中文路径，后台仅记录警告。新增 Unicode 路径失败测试后，改为 `cv2.imencode(...).tofile(...)`。存储定向测试 `15 passed in 1.12s`。

### 新增或修改文件

- `src/auto_fishing/storage/diagnostics.py`：使用 Unicode 安全的 PNG 编码写入。
- `try/tests/test_storage.py`：覆盖中文路径下的诊断截图保存与可解码性。
- `AGENTS.md`、`doc/进展记录/2026-7-12.md`：记录进度条假观测、A/D 实际按住证据和诊断写入约束。

### 错误汇报

- 问题：A/D 只短暂按住且无法继续控制。原因：真实按住成功，但错误的绿色/黄色候选在三帧后消失，触发安全释放；同时 Unicode 路径阻止了原始帧诊断落盘。解决方案：修复诊断写入，下一次失败可取得原始帧后校准真实滑块；不将假观测的方向无限延长。

### 高风险与回退方案

- 下一次构建仅加入诊断写入修复，仍会覆盖当前发布物；构建前备份 `dist\异环自动钓鱼.exe`。若启动或日志写入异常，恢复前一 SHA256 `932582B6E208490C7FE6AD0B18B2546470A72A58E34BE2F435134957A062F684`。

## 2026-07-12 16:52 ~ 16:55

### 本阶段完成内容

- 用户确认新增短随机 F 轻点延迟和控制窗口右侧留白。设计限定 F 按下前为 80～180 毫秒延迟；A/D 的长按、换向、释放保持即时，暂停/关闭可取消待发送 F。
- 已新增设计规格，明确该延迟不用于规避游戏保护，并要求注入式测试、全量测试、构建和最终清单验证。

### 新增或修改文件

- `docs/superpowers/specs/2026-07-12-input-jitter-layout-design.md`：输入节奏和控制窗口布局实现边界。
- `doc/进展记录/2026-7-12.md`：记录本阶段确认的范围与测试要求。

## 2026-07-12 16:55 ~ 17:00

### 本阶段完成内容

- 按确认规格新增 F 的按下前随机延迟：取值限制为 80～180 毫秒，记录 `input.delay`；`release_all()` 在延迟期递增取消代数，延迟结束后不再发送 F。A/D 长按、换向和释放不新增等待。
- 控制窗口改为 400×240 像素最小尺寸，右侧“阶段/帧率”标签增加左侧间距，错误文本换行宽度扩展为 305 像素。窗口坐标保存逻辑保持不变。
- 输入定向测试 `25 passed in 0.21s`，界面定向测试 `42 passed in 2.36s`，全量测试 `274 passed in 18.01s`。

### 新增或修改文件

- `src/auto_fishing/platform/input.py`、`try/tests/test_safe_input.py`：F 延迟、取消和 A/D 无延迟测试。
- `src/auto_fishing/ui/main_window.py`、`try/tests/test_ui_smoke.py`：400 像素窗口和右侧布局测试。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-12.md`：同步输入与布局验收边界。

### 高风险与回退方案

- 下一步构建将覆盖当前 SHA256 `0A2DCEB81F5C5C4727DB5E478DF9A3B5F9013FAF599320671BBB658FDB267913` 的发布物。构建前备份至 `D:\0文件夹\备份\异环自动钓鱼-input-jitter-prebuild-<时间戳>`；若新版本启动、按键或布局异常，可恢复该文件。

## 2026-07-12 17:10 ~ 17:11

### 本阶段完成内容

- 构建前已将 SHA256 `0A2DCEB81F5C5C4727DB5E478DF9A3B5F9013FAF599320671BBB658FDB267913` 的发布物备份到 `D:\0文件夹\备份\异环自动钓鱼-input-jitter-prebuild-20260712-171020\异环自动钓鱼.exe`。
- 执行构建门，结果为 `274 passed in 18.36s`；最终发布物清单复核为 `requireAdministrator uiAccess=false`。
- 新发布物 `dist\异环自动钓鱼.exe` 大小 `63,934,001` 字节，SHA256 `5B3CD24F18A04E7EA0562312BDEE9ADEEC4E1A56041842A36204065776B8D0F5`。待实机确认 F 短延迟、A/D 连续按住和右侧文字无裁切。

### 新增或修改文件

- `dist\异环自动钓鱼.exe`：已重新生成，为 Git 忽略的发布产物。
- `D:\0文件夹\备份\异环自动钓鱼-input-jitter-prebuild-20260712-171020\异环自动钓鱼.exe`：构建前可回退发布物。

## 2026-07-12 17:13 ~ 17:18

### 本阶段完成内容

- 实机日志确认 F 延迟按预期为 136ms，第二次 F 成功，A/D 屏幕键盘鼠标按下、抬起和换向均成功；问题是 A/D 后不久又因 `E_PROGRESS_LOST` 暂停。
- Unicode 路径诊断修复已生效，保存了 4.6MB 原始分辨率失败帧。量化后确认真进度条为绿色框 `(363,91,181,18)` 与细黄色标记 `(546,98,7,15)`；旧规则要求黄色高度至少 ROI 的 12%，拒绝真标记，并误配左侧大圆形 UI。
- 先写进度条细标记与水平配对失败测试，再将最小高度改为 6%，限制标记宽度并要求其中心位于绿色条及其 5%/12 像素边距内。该原始失败帧回放现在正确返回绿色 `0.273–0.408`、黄色 `0.413`；进度条定向测试 `11 passed in 0.29s`。
- 失败帧同时显示“鱼上钩了，快点击按钮上鱼！”横幅；新增中央横幅连续两帧识别。控制阶段识别该横幅后释放 A/D、轻点 F，并进入现有结算等待。场景/引擎定向测试 `87 passed in 15.76s`，全量测试 `278 passed in 18.12s`。

### 新增或修改文件

- `src/auto_fishing/vision/progress.py`、`try/tests/test_progress.py`：真实细黄色标记与绿色条水平配对。
- `src/auto_fishing/model.py`、`src/auto_fishing/vision/regions.py`、`src/auto_fishing/vision/scenes.py`：收鱼横幅识别结果与连续确认。
- `src/auto_fishing/automation/engine.py`、`src/auto_fishing/storage/runtime_logging.py`、`try/tests/test_engine.py`、`try/tests/test_scenes.py`：横幅后的释放、F 和逐帧记录。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-12.md`：记录真实帧校准和新增状态。

### 错误汇报

- 问题：程序只在前段短暂识别并操作。原因：真黄色标记过细，被旧高度阈值丢弃；控制完成后的收鱼横幅没有状态处理。解决方案：细标记与绿色条空间配对，并对收鱼横幅连续确认后自动 F。

### 高风险与回退方案

- 下一次构建会覆盖 SHA256 `5B3CD24F18A04E7EA0562312BDEE9ADEEC4E1A56041842A36204065776B8D0F5` 的发布物。构建前备份到 `D:\0文件夹\备份\异环自动钓鱼-reel-prompt-prebuild-<时间戳>`；若新实测失败，恢复该发布物。

## 2026-07-12 17:23 ~ 17:24

### 本阶段完成内容

- 构建前已将 SHA256 `5B3CD24F18A04E7EA0562312BDEE9ADEEC4E1A56041842A36204065776B8D0F5` 的发布物备份到 `D:\0文件夹\备份\异环自动钓鱼-reel-prompt-prebuild-20260712-172317\异环自动钓鱼.exe`。
- 构建门全量 `278 passed in 17.96s`，最终发布物清单验证为 `requireAdministrator uiAccess=false`。
- 新发布物 `dist\异环自动钓鱼.exe` 的 SHA256 为 `E4E6B65A8B0E80E4F397B0A8D884BD5238A97B11DA6B69C62D0D752DAA9E2DC8`；等待真实单轮闭环验收。

### 新增或修改文件

- `dist\异环自动钓鱼.exe`：已重新生成，为 Git 忽略发布物。
- `D:\0文件夹\备份\异环自动钓鱼-reel-prompt-prebuild-20260712-172317\异环自动钓鱼.exe`：构建前回退发布物。

## 2026-07-12 17:25 ~ 17:31

### 本阶段完成内容

- 用户澄清真实控制目标是将黄色标记移到绿色区域中央。复核本轮日志后撤回未提交的“反转方向”推断：黄标左侧应按 D、右侧应按 A。
- 原控制器以绿区两侧 15% 的大安全带为释放条件，无法持续追踪中心。先写失败测试，再改为以绿色中心为目标、绿色宽度 10% 为死区；死区外才按对应方向，死区内立即释放。进度条定向测试 `13 passed in 0.24s`，全量 `280 passed in 18.35s`。

### 新增或修改文件

- `src/auto_fishing/vision/progress.py`、`try/tests/test_progress.py`：绿色中心闭环、方向和小死区测试。
- `AGENTS.md`、`doc/验收标准.md`、`doc/进展记录/2026-7-12.md`：记录黄色标记居中控制语义。

### 错误汇报

- 问题：区域条移动时程序无法跟上。原因：控制器的释放区过宽，目标被错误建模为“任意进入绿色区”而非“黄色标记居中”。解决方案：以中心误差决定 A/D，保留小死区抑制高频换向。

### 高风险与回退方案

- 下一次构建将覆盖 SHA256 `E4E6B65A8B0E80E4F397B0A8D884BD5238A97B11DA6B69C62D0D752DAA9E2DC8` 的发布物。构建前备份至 `D:\0文件夹\备份\异环自动钓鱼-center-control-prebuild-<时间戳>`；实测异常时恢复该发布物。

## 2026-07-12 17:31 ~ 17:33

### 本阶段完成内容

- 已将上一发布物备份到 `D:\0文件夹\备份\异环自动钓鱼-center-control-prebuild-20260712-173116\异环自动钓鱼.exe`。
- 构建门全量 `280 passed in 18.24s`，最终发布物清单验证为 `requireAdministrator uiAccess=false`。
- 新发布物 `dist\异环自动钓鱼.exe` SHA256 为 `858114A8AC045CE78DC02A9E41067FBEE67762E14CD71577E4FC4ABF116EE423`，等待实机验证黄标居中控制。

### 新增或修改文件

- `dist\异环自动钓鱼.exe`：已重新生成，为 Git 忽略发布物。
- `D:\0文件夹\备份\异环自动钓鱼-center-control-prebuild-20260712-173116\异环自动钓鱼.exe`：构建前可回退发布物。

## 2026-07-12 17:33 ~ 17:39

### 本阶段完成内容

- 实机运行 `run-20260712T093308622889Z` 的完整日志证明首次 F 抛竿、上钩后的第二次 F 都已通过屏幕键盘发送；进度条出现后仅约 33 毫秒又被自动发送第三次 F，随即进入“等待结算”并超时，因此没有实际执行 A/D 跟随。
- 根因是上一阶段新增的黑色横幅识别与进度条画面重叠；它错误地把进度条刚出现时的提示当成收竿完成信号。该信号不再驱动控制状态转换或 F 输入，控制状态在同时出现横幅和有效进度条时必须继续按黄标与绿色中心的偏差发送 A/D。
- 先将该情形改写为失败测试，确认旧逻辑失败后删除错误的控制分支；定向测试通过，全量自动测试 `280 passed in 19.04s`。

### 新增或修改文件

- `src/auto_fishing/automation/engine.py`：删除控制阶段由横幅触发的自动 F 与提前进入结算逻辑。
- `try/tests/test_engine.py`：覆盖横幅与进度条同时出现时仍持续中心跟随的回归测试。
- `doc/进展记录/2026-7-12.md`、`doc/验收标准.md`：记录本次真实日志根因、修复边界和待实机复验项。

### 错误汇报

- 问题：中心控制版看起来完全没有 A/D 操作。原因：进度条出现时识别到的黑色“鱼上钩”横幅被错误解释为需要第三次 F，自动化提前离开控制状态。解决方案：横幅仅保留日志观测，不再改变控制状态；有效进度条优先进入黄标居中闭环。

### 高风险与回退方案

- 下一次构建会覆盖 SHA256 `858114A8AC045CE78DC02A9E41067FBEE67762E14CD71577E4FC4ABF116EE423` 的发布物。构建前备份至 `D:\0文件夹\备份\异环自动钓鱼-remove-premature-reel-prebuild-<时间戳>`；若新实测异常，恢复该文件。
- 已在构建前备份至 `D:\0文件夹\备份\异环自动钓鱼-remove-premature-reel-prebuild-20260712-173956\异环自动钓鱼.exe`，备份 SHA256 为 `858114A8AC045CE78DC02A9E41067FBEE67762E14CD71577E4FC4ABF116EE423`。
- 构建门再次确认 `280 passed in 18.72s` 与 `RELEASE_MANIFEST_OK requireAdministrator uiAccess=false`；新发布物 SHA256 为 `97B372A5682F4C8073BEA975652F9D0EF80D91F4C730D0852D1FB307418FD352`，待实机复验 A/D 连续跟随。

## 2026-07-12 17:43 ~ 17:47

### 本阶段完成内容

- 最新实机运行 `run-20260712T094153974028Z` 中，两次 F 均成功；第 153～157 帧识别进度条并按住 A，第 158 帧起连续六帧判空，第 163 帧以 `E_PROGRESS_LOST` 安全释放并暂停，符合用户所见“动一下就不动”。
- 原始失败帧 `C:\Users\29551\AppData\Local\异环自动钓鱼\diagnostics\20260712T094329800554Z_E_PROGRESS_LOST.png` 中进度条仍清晰存在。量化复现显示黄标把绿色颜色块切成宽 147 与 39 像素的两段，而旧识别器要求单个连续绿色块宽至少约 160 像素；因此黄标向绿区中部移动时，识别器会因自身连通域模型必然丢失目标。
- 已确认问题属于进度条视觉建模架构错误，不再继续调整单个阈值。按照连续失败超过三次的规则，暂停实现，先与用户确认重构设计。
- 用户确认顶部进度槽在整个阶段固定，仅绿色区域和黄标横向移动，并批准采用“固定槽位多行一维扫描与绿色分段重建”方案。已写入设计规格，明确时间历史只用于候选过滤、缺帧立即释放，以及原始失败帧、合成扫动序列和真实单轮闭环三层验收门槛。
- 用户批准设计后，已形成测试先行实施计划，按“真实失败夹具与五行重建 → 时间连续性与结构化诊断 → 最近 12 帧槽位诊断 → 全量构建与实机单轮”四个独立检查点执行。

### 新增或修改文件

- `doc/进展记录/2026-7-12.md`、`doc/验收标准.md`：记录最新实机证据、可重复根因和架构重构门槛。
- `docs/superpowers/specs/2026-07-12-progress-slot-reconstruction-design.md`：进度槽重建的正式设计规格。
- `docs/superpowers/plans/2026-07-12-progress-slot-reconstruction.md`：逐任务测试、提交、构建和实机验收计划。

### 错误汇报

- 问题：A 只按住约 0.14 秒后释放，控制随即暂停。原因：识别器把绿色条建模为一个不可中断的连通块，黄标覆盖绿条后将其分段，导致合法画面无法通过宽度门槛。解决方向：待用户确认后，将识别重构为“黄标与同一水平槽内的左右绿色段合并成一个逻辑绿色区”，并用真实失败帧序列先建立回放测试；不再使用单连通块阈值补丁。
