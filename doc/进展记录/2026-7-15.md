# 2026-07-15 进展记录

## 2026-07-15 02:40 ~ 2026-07-15 03:05

### 本阶段完成内容

- 审计 `%LOCALAPPDATA%\异环自动钓鱼V2\diagnostics` 现有 3 份诊断包。最新主动报告包含约 19.988 秒、1,673 条事件和约 9.835 秒、82 张 480 像素 JPEG；可确认绿区坐标发生多次大跳变并导致 A 持续按住，但缩略有损帧重放与原事件识别结果只有 3/33 组坐标在 0.01 内一致，且较早异常不在截图窗口内，因此现有包只能证明症状，不能支持有把握地定位候选选择或颜色分割根因。
- 按测试先行实现 V2 诊断包 schema 2：事件、480 像素上下文 JPEG、原生宽度无损顶部进度槽 PNG 和逐处理帧识别轨迹统一保留最近 30 秒；轨迹记录五条扫描线、黄/绿候选、历史参考、最终选择、拒绝原因和截断状态。
- `progress.control` 增加帧时间戳、绿区/黄标坐标、瞬时误差、请求方向和输入切换前状态；诊断元数据增加各证据流覆盖范围、丢弃计数、最近控制决策和 EXE 文件名/大小/SHA256。主动报告紧邻自动 `E_WINDOW` 错误时仅在主动包成功后删除重复自动包，写包失败仍保留原自动包。
- 版本标识更新为本地候选 `2.0.2`；未修改生产识别阈值、加权控制公式或状态机边界，避免在证据不足时引入猜测性控制修复。
- 全套回归通过 `418 passed`；412 帧真实历史控制回放输出 `REAL_CONTROL_REPLAY_OK 412 15 ['left', 'release', 'right']`。2560×1440 等效合成帧持续 30 秒诊断基准共处理 901 帧，平均 1.809 ms/次、P95 5.617 ms、最大 7.779 ms，三类丢弃计数均为 0。
- V2 单文件构建通过 418 项构建门和最终内嵌清单校验：大小 63,986,763 字节，SHA256 `0C1D4491C3CC6E7D508DD367C270553E8D2048F34F1E2E30E7EDC94D626BF51F`，清单为 `requireAdministrator uiAccess=false dpi=PerMonitorV2 fallback=true/pm`。

### 新增/修改文件与用途

- `src/auto_fishing/model.py`、`src/auto_fishing/vision/progress.py`、`src/auto_fishing/vision/scenes.py`：生成并透传进度识别诊断轨迹。
- `src/auto_fishing/automation/engine.py`：关联识别帧、控制决策和释放输入前状态。
- `src/auto_fishing/storage/recording.py`、`src/auto_fishing/storage/memory_diagnostics.py`、`src/auto_fishing/storage/diagnostic_bundles.py`：实现 30 秒四路内存证据、无损 PNG、schema 2 元数据和重复包合并。
- `src/auto_fishing/product.py`：本地候选版本更新为 `2.0.2`。
- `try/tests/test_progress.py`、`try/tests/test_v2_diagnostics.py`、`try/tests/test_engine.py`、`try/tests/test_ui_smoke.py`：新增候选轨迹、30 秒窗口、深拷贝、并发顺序、输入状态、EXE 哈希失败降级和重复包回归。
- `docs/superpowers/specs/2026-07-15-v2-diagnostics-30s-design.md`、`docs/superpowers/plans/2026-07-15-v2-diagnostics-30s.md`：记录已确认设计和测试先行实施计划。
- `dist/异环自动钓鱼V2.exe`：本次构建候选，Git 忽略，不作为源码提交内容。

### 错误汇报

- 问题：现有 `.venv` 不存在，默认测试命令无法运行。原因：工作区未保留项目虚拟环境。解决方案：使用已安装的 Python 3.13.5 及固定依赖执行测试和构建；未下载新包。
- 问题：审计中文路径截图时直接调用 `cv2.imread` 返回空。原因：OpenCV 该接口不可靠支持 Windows 中文路径。解决方案：审计改用 `numpy.fromfile + cv2.imdecode`；正式诊断写图继续使用既有 Unicode 安全编码路径。
- 问题：当前 PowerShell 未提升，`try/smoke_exe.ps1` 在创建进程前按设计拒绝执行。解决方案：不冒用旧版本烟雾结果，本地 `2.0.2` 的提升权限启动/响应/关闭烟雾保留为管理员会话人工补验。

### 高风险操作与回退

- 已在工作区外创建旧根目录发布物备份：`D:\0文件夹\备份\异环自动钓鱼-v2.0.2-before-20260715-030343\异环自动钓鱼V2.exe`，大小 63,978,747 字节，SHA256 `98D0204BF7985205678CF25B7315AAC4DC44BD6F625CA9588817D7E4AB710CD1`。该目录是本次唯一同类备份，未超过最多 2 份限制。
- 本记录提交后将用已校验的 `dist/异环自动钓鱼V2.exe` 覆盖根目录同名发布物。若复制、哈希或清单复核失败，立即从上述备份恢复，并以 SHA256 一致作为回退完成证据。
- 工作区外只读检查：上述 LocalAppData 诊断目录，以及 `D:\29551\异环自动钓鱼数据\runs\run-20260714T095204572440Z\events.jsonl` 的 412 帧历史事件；未修改这些数据。

### 待人工确认

- 用本地 `2.0.2` 在真实游戏复现一次进度条错乱并点击“报告错误”，确认新包的 30 秒上下文、无损进度槽和识别轨迹足以直接定位错误候选。
- 在管理员 PowerShell 补做单文件启动/响应/关闭烟雾；继续保留 Windows 10、窗口/无边框/全屏及不同厂商混合缩放人工复核。

## 2026-07-15 10:23 ~ 2026-07-15 10:57

### 本阶段完成内容

- 只读审计本地 `2.0.2` 于 10:23 生成的最新主动错误包 `C:\Users\29551\AppData\Local\异环自动钓鱼V2\diagnostics\yihuan-v2-20260715T022333974433Z-MANUAL_REPORT.zip`，副本解压到 `D:\1Folder\异环自动钓鱼\try\output\latest-diagnostic-20260715-102334\`。包内包含 29.99 秒、2,131 条事件，629 条逐处理帧轨迹、251 张上下文图和 180 张原生宽度无损进度槽图，三类诊断丢弃均为 0。
- 用户确认真实绿区仍在继续向左移动，并非进度条提前结束。逐帧对齐证明第 1117 帧真实高饱和青绿色连续段约为 `x=264～454`，黄标约为 `x=1129～1138`，旧识别却选择蓝灰背景 `x=1101～1126`；第 591 帧也曾把 `x=223～286` 背景写入历史。根因是旧绿色 HSV（色相/饱和度/亮度）掩码允许饱和度低至 80，半透明蓝灰轨道、树叶和建筑背景会与真实绿区粘连或形成三线一致的假连续段。
- 按测试先行新增最小回归：当前实现返回 `green_left=0.6006`，而真实长绿区应为约 `0.1982`，确认测试命中背景候选抢占。修复保留 `H=70～105、S>=80、V>=100` 宽松掩码仅恢复黄标切断处的外边界，新增 `S>=155` 实色掩码作为横向连续段真实性证据；宽松候选必须累计获得不低于现有最小绿区宽度的实色支撑，独立候选只来自实色段。`_MINIMUM_GREEN_WIDTH_RATIO=0.012`、黄标自由位置、三线共识、历史和控制公式均未改变。
- 定向进度测试 `43 passed`；全套回归 `419 passed in 34.64s`，`compileall` 和 `git diff --check` 通过。最新包 180 张无损槽位回放中，第 579～1117 帧共 155 张全部输出真实绿区，第 1121 帧后的 21 张全部拒绝背景；第 1117 帧输出 `green=0.198198～0.340841、yellow=0.850976`。保留的 412 帧历史控制回放仍输出 `REAL_CONTROL_REPLAY_OK 412 15 ['left', 'release', 'right']`。
- 本地候选版本标识已测试先行更新为 `2.0.3`。代码与计划检查点提交为 `e8a1232`，任务在隔离工作树 `D:\1Folder\异环自动钓鱼\.worktrees\fix-progress-green-continuity\` 的 `codex/fix-progress-green-continuity` 分支执行，项目根目录保持干净 `main`。
- V2 单文件构建再次通过 419 项构建门；产物大小 63,985,836 字节，SHA256 `8C43403BF2AB848DDF265E0385EDFCB95FC638F1CDAEB1F1647F8BBF92862BA1`，内嵌清单为 `requireAdministrator uiAccess=false dpi=PerMonitorV2 fallback=true/pm`。根目录与隔离工作树 `dist` 产物的大小、哈希和清单复核一致。
- 任务分支已以合并提交 `40f95ba` 合入本地 `main`；合并后的主线再次通过 `419 passed in 34.29s` 和编译检查。隔离工作树已移除，`codex/fix-progress-green-continuity` 任务分支已删除，未遗留分支污染主线。

### 新增/修改文件与用途

- `src/auto_fishing/vision/progress.py`：增加宽松边界与高饱和实色连续段的双层绿色候选约束。
- `try/tests/test_progress.py`：增加最新真实几何与颜色量化对应的背景抢占回归。
- `src/auto_fishing/product.py`、`try/tests/test_ui_smoke.py`：将本地候选版本更新为 `2.0.3` 并锁定测试。
- `docs/superpowers/plans/2026-07-15-progress-green-continuity.md`：记录测试先行实施步骤、诊断证据和验证门。
- `AGENTS.md`、`doc/验收标准.md`：同步 `v2.0.3` 候选状态、双层绿色连续段规则、真实包回放、构建证据和人工验收边界。
- `doc/进展记录/2026-7-15.md`：记录问题、原因、解决方案、外部备份、发布物替换和回退证据。

### 错误汇报

- 问题：项目 `.venv` 不存在，首次量化命令找不到解释器。原因：当前工作区未保留项目虚拟环境。解决方案：复用上一阶段已安装固定依赖的 Python 3.13.5，以 `py -3.13` 运行；未下载新包。
- 问题：首次用 `cv2.imread` 读取中文工作区路径返回空。原因：OpenCV 该接口在 Windows 中文路径不可靠。解决方案：改用 `numpy.fromfile + cv2.imdecode`，原始诊断包保持只读。
- 问题：直接把整个绿色掩码饱和度下限提高到 160 后，旧宽绿区和高品质窄绿区两项真实夹具失败。原因：高阈值侵蚀黄标两侧抗锯齿边缘。解决方案：恢复宽松掩码负责边界，只用实色掩码提供候选支撑。
- 问题：对实色掩码全局横向膨胀 3 像素后，两项微小噪声拒绝测试失败。原因：原 3 像素噪声被扩成 5 像素并越过最小宽度。解决方案：删除全局膨胀，改为累计未扩张的实色连续长度；最终 43 项进度测试全部通过。
- 问题：合并前独立审查代理需要的 CodeRabbit 命令行工具未安装，且继续审查需要安装并登录外部服务。解决方案：未擅自安装或登录，代理保持只读且未修改文件；改为本地逐项审计生产差异、边界条件、回归覆盖和文档声明，并保留全量测试、真实包回放和构建校验作为合并门。
- 问题：隔离工作树首次执行内联诊断回放时提示 `ModuleNotFoundError: auto_fishing`。原因：该工作树未把源码以可编辑包安装到全局 Python。解决方案：显式将隔离工作树 `src` 加入本次进程 `PYTHONPATH` 后重跑，得到 `LATEST_PROGRESS_REPLAY_OK 180 155 21`；未修改 Python 环境或下载依赖。
- 问题：合并前复核发现项目根目录同名 EXE 已为 `v2.0.3`，但根目录 `dist/异环自动钓鱼V2.exe` 仍是旧 `v2.0.2`；原因是构建在隔离工作树执行，忽略目录不会随 Git 分支合并。解决方案：确认旧 `dist` 与外部备份哈希一致后，用隔离工作树已验证产物同步根目录 `dist`；最终根目录、根目录 `dist` 和隔离工作树 `dist` 三者大小、SHA256 与清单一致。
- 问题：合并后执行 `git diff --check origin/main..HEAD` 发现实施计划文件末尾有多余空白行。原因：计划文件初次提交时保留了额外空行，而干净工作树上的无范围差异检查没有覆盖已提交内容。解决方案：删除末尾空白行，并改用相对 `origin/main` 的范围检查完成最终复核。

### 高风险操作与回退

- 已在工作区外创建 `D:\0文件夹\备份\异环自动钓鱼-v2.0.3-before-20260715-1045\异环自动钓鱼V2.exe`，备份旧 `v2.0.2` 发布物：大小 63,986,763 字节，SHA256 `0C1D4491C3CC6E7D508DD367C270553E8D2048F34F1E2E30E7EDC94D626BF51F`。该目录是当前唯一同类备份，未超过最多 2 份限制。
- 构建和清单验证成功后，已用带自动回退保护的复制流程覆盖项目根目录 `D:\1Folder\异环自动钓鱼\异环自动钓鱼V2.exe` 及根目录 `dist\异环自动钓鱼V2.exe`；两者与隔离工作树 `dist` 均为 63,985,836 字节、SHA256 `8C43403BF2AB848DDF265E0385EDFCB95FC638F1CDAEB1F1647F8BBF92862BA1`，清单复核一致，因此未触发备份恢复。
- 工作区外修改仅为上述备份目录；LocalAppData 原始诊断 ZIP 保持只读，分析副本只写入工作区 `try/output/`。

### 待人工确认

- 新候选仍需真实游戏确认左移绿区持续跟踪、A/D 把黄标拉回绿区中心以及完整单轮计数；离线回放不能替代实时游戏闭环。
- 当前 PowerShell 检查结果为 `IS_ADMIN=False`，因此未启动 UAC 发布物烟雾，不使用旧版本结果冒充 `2.0.3` 证据。Windows 10、窗口/无边框/全屏和不同厂商混合缩放继续保留人工确认。

## 2026-07-15 11:22 ~ 2026-07-15 11:24

### 本阶段完成内容

- 用户确认将 `v2.0.3` 修复源码推送到私有 GitHub。推送前确认 GitHub CLI 2.93.0 已以 `q2955161835-debug` 登录、工作区干净，刷新远端后 `main` 仅本地领先 7 个提交且无远端分叉。
- 使用普通非强制推送将 `main` 从远端 `ed4da11` 更新到 `182b573`，本地与远端引用一致；随后同步 `AGENTS.md` 和 `doc/验收标准.md`，明确源码已推送但 `v2.0.3` 尚未创建标签或 GitHub Release。
- 本次未上传 Git 忽略的 EXE、未创建标签、未创建 Release，也未改写远端历史；真实游戏确认前继续保留 GitHub 最新 Release `v2.0.1`。

### 新增/修改文件与用途

- `AGENTS.md`：更新 GitHub `main` 与本地候选状态。
- `doc/验收标准.md`：区分源码推送与正式 Release 发布。
- `doc/进展记录/2026-7-15.md`：记录授权、远端同步范围和发布边界。

## 2026-07-15 13:25 ~ 2026-07-15 14:06

### 本阶段完成内容

- 只读审计 `%LOCALAPPDATA%\异环自动钓鱼V2\diagnostics\yihuan-v2-20260715T052504918888Z-E_WINDOW.zip` 和 `yihuan-v2-20260715T052511890323Z-E_WINDOW.zip`。两包分别记录 13:25:04 与 13:25:11 的“游戏窗口已失去前台”；第一包恢复运行后约 3.6 秒再次失焦。结合最小复现确认：旧 `SafeInput.tap_f()` 在 80～180 毫秒按下前延迟开始时仍为游戏前台，但延迟期间出现 Windows 弹窗后仍会发出 F 按下/抬起。
- 按测试先行新增输入边界守卫：每次新的 F/A/D 按下和结算点击前动态复核已绑定游戏仍为前台；F 延迟结束后才执行守卫。前台丢失后禁止新输入，但所有按键和鼠标释放不受守卫阻止。控制窗口取得前台按 `E_USER_PAUSE` 安全暂停且不自动生成诊断，Windows 系统弹窗或其他窗口抢占按 `E_WINDOW` 暂停并提示关闭弹窗后点击“继续”；均不自动恢复。
- 退出根因定位为两部分：正常主循环结束后，清理异常仍被重新抛成 `BaseExceptionGroup`；屏幕键盘已由 Windows 关闭时，陈旧 HWND 返回错误 1400 仍被视为关闭失败。现将错误 1400 按“已关闭”幂等成功处理，控制窗口关闭顺序改为先停止控制器、再保存设置并销毁窗口；仅清理失败会在诊断服务关闭前请求 `E_CLEANUP`，记录后正常返回，运行期异常仍按原规则抛出。
- 新增 9 项回归在旧实现上全部按预期失败，修复后全部通过；相关输入、窗口、引擎、屏幕键盘、UI 和 V2 诊断模块 `265 passed`。修改前全套基线 `419 passed in 40.41s`，修改后全套、合并前和合并后均为 `428 passed`，最后一次合并后结果为 `428 passed in 40.38s`。源码空闲启动/关闭复现返回 `CLEAN_EXIT_OK`。
- 本地候选版本测试先行更新为 `2.0.4`。V2 构建脚本再次执行全部 428 项并通过，内嵌清单为 `requireAdministrator uiAccess=false dpi=PerMonitorV2 fallback=true/pm`；根目录与 `dist` EXE 均为 63,989,100 字节、SHA256 `402DC0003D0E5FC8E1C537D6B2D4096BF3D24E15E18CC8A7BAA0E135B4C4790E`。
- 任务在 `codex/fix-popup-exit` 分支完成，阶段提交为 `700981a`、`e11736f`、`a575628` 和 `30a0d49`；以合并提交 `589ae46` 合入本地 `main`，合并后全量复测和发布物复核通过，任务分支已删除。未推送 GitHub、未创建标签或 Release。
- 合并前针对“弹窗后仍产生新输入”和“退出仍抛未处理异常”逐项审计生产差异、异常继承/捕获顺序、释放路径和测试覆盖，未发现 Important/Critical 问题。实现复用现有 `SafeInput`、前台校验和暂停入口，没有引入第二套控制状态机、重复实现或临时补丁；Windows 前台读取与系统输入无法组成单个原子系统调用，最后的系统时序边界保留真实弹窗人工复验。

### 新增/修改文件与用途

- `src/auto_fishing/platform/input.py`：增加 `InputTargetUnavailable` 与新输入前目标窗口守卫，保持释放路径无条件可用。
- `src/auto_fishing/platform/windowing.py`、`src/auto_fishing/automation/engine.py`：声明前台窗口原生签名，接线动态守卫并区分控制窗口 `E_USER_PAUSE` 与其他抢占 `E_WINDOW`。
- `src/auto_fishing/platform/on_screen_keyboard.py`：将关闭时错误 1400 视为句柄已经失效，保留错误 5 安全降级。
- `src/auto_fishing/ui/main_window.py`、`src/auto_fishing/app.py`：调整关闭顺序；清理失败请求 `E_CLEANUP` 并避免正常退出显示未处理异常。
- `src/auto_fishing/product.py`：本地候选版本更新为 `2.0.4`。
- `try/tests/test_safe_input.py`、`test_capture_window.py`、`test_engine.py`、`test_on_screen_keyboard.py`、`test_ui_smoke.py`：新增弹窗抢焦点、释放、分类、失效 HWND、退出顺序、清理诊断和版本回归。
- `docs/superpowers/plans/2026-07-15-popup-safe-input-clean-exit.md`：记录测试先行实施步骤和完成状态。
- `AGENTS.md`、`doc/验收标准.md`：同步长期输入/退出约束、`v2.0.4` 构建证据和人工验收边界。
- `doc/进展记录/2026-7-15.md`：记录问题、原因、解决方案、验证、外部工具行为和合并结果。
- `dist/异环自动钓鱼V2.exe`、`异环自动钓鱼V2.exe`：本地 `v2.0.4` 单文件候选，均受 Git 忽略。

### 错误汇报

- 问题：首次最小复现中，游戏在 F 随机延迟期间失去前台后仍记录 F 按下/抬起。原因：旧逻辑只在每帧开始检查前台，延迟结束到物理输入之间没有再次校验。解决方案：把动态前台守卫放到 `_down()` 和 `click()` 的实际输入边界，新增延迟期间失焦与释放不受阻回归。
- 问题：屏幕键盘已经消失时退出仍可能报错。原因：`PostMessageW(WM_CLOSE)` 对陈旧 HWND 返回错误 1400，旧实现将其当成真实关闭失败；其他清理失败还会在正常主循环结束后重新抛出异常组。解决方案：错误 1400 幂等返回；真实清理失败记录运行事件和 `E_CLEANUP` 后正常退出。
- 问题：项目 `.venv` 不存在。原因：当前工作区未保留项目虚拟环境。解决方案：使用已安装固定依赖的 Python 3.13.5 运行测试和构建，未下载新的 Python 包。
- 问题：第一次构建仍带 `2.0.3` 版本标识。原因：版本号更新尚未纳入本次实现。解决方案：丢弃该构建，先增加 `2.0.4` 失败测试，再更新产品版本并重新运行完整构建；最终仅同步已验证的 `2.0.4` 产物。
- 问题：独立 CodeRabbit 审查所需 CLI 原先缺失，安装 0.6.5 后仍未认证，两次登录等待均未完成。原因：外部服务没有可用授权会话。解决方案：停止认证等待，不伪造 CodeRabbit 结果；改为本地逐项审计并以回归、全量测试和发布物校验作为合并门。CLI 安装在工作区外 WSL 路径 `/home/fjj/.local/bin/coderabbit`，另创建 `/home/fjj/.local/bin/cr` 符号链接；未修改 PATH、Shell 配置或项目文件，临时下载目录已删除。
- 观察：源码空闲退出复现返回 `CLEAN_EXIT_OK`，DXcam 仍在开发控制台输出已有的单实例提示；本次没有控制台异常或非零退出，该提示不是用户所见窗口版退出报错根因，未改动 DXcam 生命周期。

### 高风险操作与回退

- 本次没有批量删除、结构性迁移或工作区外生产数据写入。代码修改前已有 Git 基线，阶段提交和合并提交均可独立回退；如需整体撤销本阶段，可在确认工作区干净后回退合并提交 `589ae46`，再从提交 `8ed4e24` 重建上一候选。
- 根目录与 `dist` EXE 由同一次可重复构建同步，未使用备份代替 Git。若本地候选人工验收失败，保留诊断包并回退源码后重新构建；不得仅替换未知来源 EXE。
- 工作区外修改仅为上述 CodeRabbit CLI 与符号链接；两份 LocalAppData 诊断 ZIP、Windows 事件日志和项目外数据均保持只读。分析副本和退出复现临时文件只写入可清理的 `try/output/`。

### 待人工确认

- 当前 PowerShell 为 `IS_ADMIN=False`，因此未启动带 `requireAdministrator` 清单的 `v2.0.4` 提升权限烟雾。需在管理员 PowerShell 执行 `& .\try\smoke_exe.ps1 -TargetPath .\dist\异环自动钓鱼V2.exe`，确认启动、响应、关闭和无残留进程。
- 在真实游戏中分别于 F 随机延迟、A/D 按住和结算点击前触发 Windows 系统弹窗，确认没有新输入、已有输入立即释放；关闭弹窗后程序不自动恢复，点击“继续”会保留计数并从第一 F 重开未完成轮。
- 继续保留 Windows 10 真机、窗口/无边框/全屏和不同厂商混合缩放人工复验；未经真实游戏确认不得推送 Release 或声称实机问题已完全解决。

## 2026-07-15 14:53 ~ 2026-07-15 14:59

### 本阶段完成内容

- 确认用户指定地址是原 GitHub 仓库改名后的地址，不是仓库迁移；将本地 `origin` 从 `yihuan-auto-fish` 更新为公开仓库 `https://github.com/q2955161835-debug/yihuan-auto-fishing.git`。目标 `main` 为本地历史祖先，三个既有 Release 与标签保持不变，未改写历史。
- 新增中文 `README.md`，包含项目边界、正式版与候选源码区分、功能、下载使用、`F8` 安全暂停、诊断隐私、源码运行、测试、构建和许可证入口；明确最新正式 Release 仍为已经用户真实游戏确认的 `v2.0.1`，`v2.0.4` 只推送候选源码。
- 新增 PolyForm Noncommercial License 1.0.0 英文标准许可证和项目 `Required Notice`。去除 Markdown 格式与项目通知后，正文与官方 `.txt` 的 682 个词逐词一致；README 明确商业用途需要另行许可，且该许可证不是 OSI 认可的开源许可证。
- README 三个本地链接全部存在；仓库主页、Release 列表、`v2.0.1` Release 和 PolyForm 官方许可证页面均返回 HTTP 200。常见 GitHub/AWS/私钥模式扫描未发现疑似真实密钥，Git 未跟踪 `.env`、EXE、ZIP 或 `config.json`。
- 全套回归 `428 passed in 39.53s`，`compileall` 和范围空白检查通过。任务提交 `c218237` 以合并提交 `57e2cca` 合入本地 `main`，任务分支已删除；随后以普通非强制推送把 GitHub `main` 从 `8ed4e24` 更新到 `57e2cca`，本地与远端一致。按用户要求直接发布 `main`，未创建 PR、标签或 Release。
- 文档改动未引入重复实现、临时补丁或代码技术债；候选 EXE 继续受 Git 忽略，未上传到源码提交。

### 新增/修改文件与用途

- `README.md`：公开项目介绍、下载使用、诊断、开发和授权入口。
- `LICENSE`：PolyForm Noncommercial License 1.0.0 标准条款与版权通知。
- `AGENTS.md`：同步改名后的公开 GitHub 地址、README/许可证入口和 `v2.0.4` 源码推送状态。
- `doc/验收标准.md`：新增公开仓库文档、链接、许可证和敏感信息验收项，并同步最终结论。
- `doc/进展记录/2026-7-15.md`：记录本阶段验证、合并、推送和异常处理。

### 错误汇报

- 问题：首次 README 本地链接检查在处理根目录文件时因父路径为空而抛错。原因：PowerShell `Split-Path README.md -Parent` 对根目录相对路径返回空字符串。解决方案：改用仓库绝对根目录拼接链接后重跑，三个本地链接全部通过。
- 问题：首次许可证逐词比对被官方 `.txt` 的链接、加粗和尖括号等 Markdown 格式干扰；第二次发现本地比下载版末尾多网页版权行。原因：网页展示内容与官方可下载许可证正文的装饰格式和页脚不同。解决方案：以官方 `.txt` 为基准删除非正文网页版权行，保留项目 `Required Notice`，归一化 Markdown 后 682 个词完全一致。

### 高风险操作与回退

- 本次没有强制推送、标签/Release 创建、批量删除、生产数据修改或工作区外文件写入。所有文件修改均已进入 Git；如需撤销，可在确认工作区干净后回退合并提交 `57e2cca` 及其后的发布记录提交。
- GitHub 推送使用普通快进更新，未上传受忽略的根目录或 `dist` 候选 EXE。远端地址改名发生于 Git 元数据，不涉及源码历史迁移。

### 待人工确认

- `v2.0.4` 的提升权限烟雾、真实 Windows 系统弹窗抢占、真实游戏完整闭环、Windows 10、窗口/无边框/全屏和不同厂商混合缩放仍待人工复验；本次未创建该版本 Release。
